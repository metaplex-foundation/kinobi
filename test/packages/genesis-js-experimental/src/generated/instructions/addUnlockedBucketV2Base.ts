/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';
import {
  Condition,
  ConditionArgs,
  getConditionDecoder,
  getConditionEncoder,
} from '../types';

export type AddUnlockedBucketV2BaseInstruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountBaseMint extends string | IAccountMeta<string> = string,
  TAccountQuoteMint extends string | IAccountMeta<string> = string,
  TAccountBucketQuoteTokenAccount extends string | IAccountMeta<string> =
    string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountRecipient extends string | IAccountMeta<string> = string,
  TAccountQuoteTokenProgram extends string | IAccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountBaseMint extends string
        ? ReadonlyAccount<TAccountBaseMint>
        : TAccountBaseMint,
      TAccountQuoteMint extends string
        ? ReadonlyAccount<TAccountQuoteMint>
        : TAccountQuoteMint,
      TAccountBucketQuoteTokenAccount extends string
        ? WritableAccount<TAccountBucketQuoteTokenAccount>
        : TAccountBucketQuoteTokenAccount,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRecipient extends string
        ? ReadonlyAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountQuoteTokenProgram extends string
        ? ReadonlyAccount<TAccountQuoteTokenProgram>
        : TAccountQuoteTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddUnlockedBucketV2BaseInstructionData = {
  discriminator: number;
  bucketIndex: number;
  padding: Array<number>;
  baseTokenAllocation: bigint;
  claimStartCondition: Condition;
  claimEndCondition: Condition;
};

export type AddUnlockedBucketV2BaseInstructionDataArgs = {
  bucketIndex: number;
  padding: Array<number>;
  baseTokenAllocation: number | bigint;
  claimStartCondition: ConditionArgs;
  claimEndCondition: ConditionArgs;
};

export function getAddUnlockedBucketV2BaseInstructionDataEncoder(): Encoder<AddUnlockedBucketV2BaseInstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['bucketIndex', getU8Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 6 })],
      ['baseTokenAllocation', getU64Encoder()],
      ['claimStartCondition', getConditionEncoder()],
      ['claimEndCondition', getConditionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: 97 })
  );
}

export function getAddUnlockedBucketV2BaseInstructionDataDecoder(): Decoder<AddUnlockedBucketV2BaseInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['bucketIndex', getU8Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 6 })],
    ['baseTokenAllocation', getU64Decoder()],
    ['claimStartCondition', getConditionDecoder()],
    ['claimEndCondition', getConditionDecoder()],
  ]);
}

export function getAddUnlockedBucketV2BaseInstructionDataCodec(): Codec<
  AddUnlockedBucketV2BaseInstructionDataArgs,
  AddUnlockedBucketV2BaseInstructionData
> {
  return combineCodec(
    getAddUnlockedBucketV2BaseInstructionDataEncoder(),
    getAddUnlockedBucketV2BaseInstructionDataDecoder()
  );
}

export type AddUnlockedBucketV2BaseInput<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
  TAccountBucketQuoteTokenAccount extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountRecipient extends string = string,
  TAccountQuoteTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the bucket account */
  bucket: Address<TAccountBucket>;
  /** The address of the base token mint account */
  baseMint: Address<TAccountBaseMint>;
  /** The mint account of the quote token */
  quoteMint: Address<TAccountQuoteMint>;
  /** The token account owned by the bucket for the quote token */
  bucketQuoteTokenAccount: Address<TAccountBucketQuoteTokenAccount>;
  /** The account paying for the storage fees */
  payer: TransactionSigner<TAccountPayer>;
  /** The authority of the genesis account */
  authority?: TransactionSigner<TAccountAuthority>;
  /** The recipient of the tokens (optional; defaults to authority) */
  recipient?: Address<TAccountRecipient>;
  /** The SPL Token program for the quote token */
  quoteTokenProgram: Address<TAccountQuoteTokenProgram>;
  /** The Associated Token program */
  associatedTokenProgram: Address<TAccountAssociatedTokenProgram>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  bucketIndex: AddUnlockedBucketV2BaseInstructionDataArgs['bucketIndex'];
  padding: AddUnlockedBucketV2BaseInstructionDataArgs['padding'];
  baseTokenAllocation: AddUnlockedBucketV2BaseInstructionDataArgs['baseTokenAllocation'];
  claimStartCondition: AddUnlockedBucketV2BaseInstructionDataArgs['claimStartCondition'];
  claimEndCondition: AddUnlockedBucketV2BaseInstructionDataArgs['claimEndCondition'];
};

export function getAddUnlockedBucketV2BaseInstruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TAccountBucketQuoteTokenAccount extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountRecipient extends string,
  TAccountQuoteTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountSystemProgram extends string,
>(
  input: AddUnlockedBucketV2BaseInput<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountBucketQuoteTokenAccount,
    TAccountPayer,
    TAccountAuthority,
    TAccountRecipient,
    TAccountQuoteTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >
): AddUnlockedBucketV2BaseInstruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountBaseMint,
  TAccountQuoteMint,
  TAccountBucketQuoteTokenAccount,
  TAccountPayer,
  TAccountAuthority,
  TAccountRecipient,
  TAccountQuoteTokenProgram,
  TAccountAssociatedTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    bucket: { value: input.bucket ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
    bucketQuoteTokenAccount: {
      value: input.bucketQuoteTokenAccount ?? null,
      isWritable: true,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: false },
    quoteTokenProgram: {
      value: input.quoteTokenProgram ?? null,
      isWritable: false,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
      getAccountMeta(accounts.bucketQuoteTokenAccount),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.quoteTokenProgram),
      getAccountMeta(accounts.associatedTokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getAddUnlockedBucketV2BaseInstructionDataEncoder().encode(
      args as AddUnlockedBucketV2BaseInstructionDataArgs
    ),
  } as AddUnlockedBucketV2BaseInstruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountBucketQuoteTokenAccount,
    TAccountPayer,
    TAccountAuthority,
    TAccountRecipient,
    TAccountQuoteTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedAddUnlockedBucketV2BaseInstruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the bucket account */
    bucket: TAccountMetas[1];
    /** The address of the base token mint account */
    baseMint: TAccountMetas[2];
    /** The mint account of the quote token */
    quoteMint: TAccountMetas[3];
    /** The token account owned by the bucket for the quote token */
    bucketQuoteTokenAccount: TAccountMetas[4];
    /** The account paying for the storage fees */
    payer: TAccountMetas[5];
    /** The authority of the genesis account */
    authority?: TAccountMetas[6] | undefined;
    /** The recipient of the tokens (optional; defaults to authority) */
    recipient?: TAccountMetas[7] | undefined;
    /** The SPL Token program for the quote token */
    quoteTokenProgram: TAccountMetas[8];
    /** The Associated Token program */
    associatedTokenProgram: TAccountMetas[9];
    /** The system program */
    systemProgram: TAccountMetas[10];
  };
  data: AddUnlockedBucketV2BaseInstructionData;
};

export function parseAddUnlockedBucketV2BaseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedAddUnlockedBucketV2BaseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      baseMint: getNextAccount(),
      quoteMint: getNextAccount(),
      bucketQuoteTokenAccount: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextOptionalAccount(),
      recipient: getNextOptionalAccount(),
      quoteTokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddUnlockedBucketV2BaseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
