/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  WritableAccount,
} from '@solana/instructions';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type FundOutflowsV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountMint extends string | IAccountMeta<string> = string,
  TAccountFeeWallet extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TAccountBucketSigner extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountFeeWallet extends string
        ? WritableAccount<TAccountFeeWallet>
        : TAccountFeeWallet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountBucketSigner extends string
        ? WritableAccount<TAccountBucketSigner>
        : TAccountBucketSigner,
      ...TRemainingAccounts,
    ]
  >;

export type FundOutflowsV1InstructionData = { discriminator: number };

export type FundOutflowsV1InstructionDataArgs = {};

export function getFundOutflowsV1InstructionDataEncoder(): Encoder<FundOutflowsV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: 11 })
  );
}

export function getFundOutflowsV1InstructionDataDecoder(): Decoder<FundOutflowsV1InstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getFundOutflowsV1InstructionDataCodec(): Codec<
  FundOutflowsV1InstructionDataArgs,
  FundOutflowsV1InstructionData
> {
  return combineCodec(
    getFundOutflowsV1InstructionDataEncoder(),
    getFundOutflowsV1InstructionDataDecoder()
  );
}

export type FundOutflowsV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountMint extends string = string,
  TAccountFeeWallet extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountBucketSigner extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the outflow bucket */
  bucket: Address<TAccountBucket>;
  /** The address of the mint account */
  mint: Address<TAccountMint>;
  /** The wallet where fees are sent */
  feeWallet: Address<TAccountFeeWallet>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** The bucket signer PDA for SOL transfers (Raydium buckets only) */
  bucketSigner?: Address<TAccountBucketSigner>;
};

export function getFundOutflowsV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountMint extends string,
  TAccountFeeWallet extends string,
  TAccountSystemProgram extends string,
  TAccountBucketSigner extends string,
>(
  input: FundOutflowsV1Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountMint,
    TAccountFeeWallet,
    TAccountSystemProgram,
    TAccountBucketSigner
  >
): FundOutflowsV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountMint,
  TAccountFeeWallet,
  TAccountSystemProgram,
  TAccountBucketSigner
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    bucket: { value: input.bucket ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    feeWallet: { value: input.feeWallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    bucketSigner: { value: input.bucketSigner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.feeWallet),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.bucketSigner),
    ],
    programAddress,
    data: getFundOutflowsV1InstructionDataEncoder().encode({}),
  } as FundOutflowsV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountMint,
    TAccountFeeWallet,
    TAccountSystemProgram,
    TAccountBucketSigner
  >;

  return instruction;
}

export type ParsedFundOutflowsV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the outflow bucket */
    bucket: TAccountMetas[1];
    /** The address of the mint account */
    mint: TAccountMetas[2];
    /** The wallet where fees are sent */
    feeWallet: TAccountMetas[3];
    /** The system program */
    systemProgram: TAccountMetas[4];
    /** The bucket signer PDA for SOL transfers (Raydium buckets only) */
    bucketSigner?: TAccountMetas[5] | undefined;
  };
  data: FundOutflowsV1InstructionData;
};

export function parseFundOutflowsV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedFundOutflowsV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      mint: getNextAccount(),
      feeWallet: getNextAccount(),
      systemProgram: getNextAccount(),
      bucketSigner: getNextOptionalAccount(),
    },
    data: getFundOutflowsV1InstructionDataDecoder().decode(instruction.data),
  };
}
