/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type RefundVaultDepositV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountDepositPda extends string | IAccountMeta<string> = string,
  TAccountActionsAuthority extends string | IAccountMeta<string> = string,
  TAccountRecipient extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? ReadonlyAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountDepositPda extends string
        ? WritableAccount<TAccountDepositPda>
        : TAccountDepositPda,
      TAccountActionsAuthority extends string
        ? WritableSignerAccount<TAccountActionsAuthority> &
            IAccountSignerMeta<TAccountActionsAuthority>
        : TAccountActionsAuthority,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundVaultDepositV1InstructionData = {
  discriminator: number;
  index: number;
};

export type RefundVaultDepositV1InstructionDataArgs = { index: number };

export function getRefundVaultDepositV1InstructionDataEncoder(): Encoder<RefundVaultDepositV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['index', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: 33 })
  );
}

export function getRefundVaultDepositV1InstructionDataDecoder(): Decoder<RefundVaultDepositV1InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['index', getU8Decoder()],
  ]);
}

export function getRefundVaultDepositV1InstructionDataCodec(): Codec<
  RefundVaultDepositV1InstructionDataArgs,
  RefundVaultDepositV1InstructionData
> {
  return combineCodec(
    getRefundVaultDepositV1InstructionDataEncoder(),
    getRefundVaultDepositV1InstructionDataDecoder()
  );
}

export type RefundVaultDepositV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountDepositPda extends string = string,
  TAccountActionsAuthority extends string = string,
  TAccountRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the bucket */
  bucket: Address<TAccountBucket>;
  /** The PDA to track the user's deposit */
  depositPda: Address<TAccountDepositPda>;
  /** The authority of the vault bucket actions */
  actionsAuthority: TransactionSigner<TAccountActionsAuthority>;
  /** The recipient of the refund */
  recipient: Address<TAccountRecipient>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  index: RefundVaultDepositV1InstructionDataArgs['index'];
};

export function getRefundVaultDepositV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountDepositPda extends string,
  TAccountActionsAuthority extends string,
  TAccountRecipient extends string,
  TAccountSystemProgram extends string,
>(
  input: RefundVaultDepositV1Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountDepositPda,
    TAccountActionsAuthority,
    TAccountRecipient,
    TAccountSystemProgram
  >
): RefundVaultDepositV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountDepositPda,
  TAccountActionsAuthority,
  TAccountRecipient,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: false },
    bucket: { value: input.bucket ?? null, isWritable: true },
    depositPda: { value: input.depositPda ?? null, isWritable: true },
    actionsAuthority: {
      value: input.actionsAuthority ?? null,
      isWritable: true,
    },
    recipient: { value: input.recipient ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.depositPda),
      getAccountMeta(accounts.actionsAuthority),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRefundVaultDepositV1InstructionDataEncoder().encode(
      args as RefundVaultDepositV1InstructionDataArgs
    ),
  } as RefundVaultDepositV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountDepositPda,
    TAccountActionsAuthority,
    TAccountRecipient,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedRefundVaultDepositV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the bucket */
    bucket: TAccountMetas[1];
    /** The PDA to track the user's deposit */
    depositPda: TAccountMetas[2];
    /** The authority of the vault bucket actions */
    actionsAuthority: TAccountMetas[3];
    /** The recipient of the refund */
    recipient: TAccountMetas[4];
    /** The system program */
    systemProgram: TAccountMetas[5];
  };
  data: RefundVaultDepositV1InstructionData;
};

export function parseRefundVaultDepositV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRefundVaultDepositV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      depositPda: getNextAccount(),
      actionsAuthority: getNextAccount(),
      recipient: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundVaultDepositV1InstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
