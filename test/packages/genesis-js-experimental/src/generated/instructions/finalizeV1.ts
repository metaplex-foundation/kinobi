/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type FinalizeV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountMint extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountFeeWallet extends string | IAccountMeta<string> = string,
  TAccountTokenProgram extends string | IAccountMeta<string> =
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFeeWallet extends string
        ? WritableAccount<TAccountFeeWallet>
        : TAccountFeeWallet,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeV1InstructionData = {
  discriminator: number;
  burnMintAuthority: boolean;
  burnFreezeAuthority: boolean;
  padding: Array<number>;
  reserved: Uint8Array;
};

export type FinalizeV1InstructionDataArgs = {
  burnMintAuthority: boolean;
  burnFreezeAuthority: boolean;
  padding: Array<number>;
  reserved: Uint8Array;
};

export function getFinalizeV1InstructionDataEncoder(): Encoder<FinalizeV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['burnMintAuthority', getBooleanEncoder()],
      ['burnFreezeAuthority', getBooleanEncoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 5 })],
      ['reserved', getBytesEncoder({ size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: 16 })
  );
}

export function getFinalizeV1InstructionDataDecoder(): Decoder<FinalizeV1InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['burnMintAuthority', getBooleanDecoder()],
    ['burnFreezeAuthority', getBooleanDecoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 5 })],
    ['reserved', getBytesDecoder({ size: 32 })],
  ]);
}

export function getFinalizeV1InstructionDataCodec(): Codec<
  FinalizeV1InstructionDataArgs,
  FinalizeV1InstructionData
> {
  return combineCodec(
    getFinalizeV1InstructionDataEncoder(),
    getFinalizeV1InstructionDataDecoder()
  );
}

export type FinalizeV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountMint extends string = string,
  TAccountAuthority extends string = string,
  TAccountFeeWallet extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the mint account */
  mint: Address<TAccountMint>;
  /** The authority of the Genesis account */
  authority: TransactionSigner<TAccountAuthority>;
  /** The wallet where fees are sent */
  feeWallet: Address<TAccountFeeWallet>;
  /** The SPL Token program */
  tokenProgram?: Address<TAccountTokenProgram>;
  burnMintAuthority: FinalizeV1InstructionDataArgs['burnMintAuthority'];
  burnFreezeAuthority: FinalizeV1InstructionDataArgs['burnFreezeAuthority'];
  padding: FinalizeV1InstructionDataArgs['padding'];
  reserved: FinalizeV1InstructionDataArgs['reserved'];
};

export function getFinalizeV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountMint extends string,
  TAccountAuthority extends string,
  TAccountFeeWallet extends string,
  TAccountTokenProgram extends string,
>(
  input: FinalizeV1Input<
    TAccountGenesisAccount,
    TAccountMint,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountTokenProgram
  >
): FinalizeV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountMint,
  TAccountAuthority,
  TAccountFeeWallet,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    feeWallet: { value: input.feeWallet ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.feeWallet),
      getAccountMeta(accounts.tokenProgram),
    ],
    programAddress,
    data: getFinalizeV1InstructionDataEncoder().encode(
      args as FinalizeV1InstructionDataArgs
    ),
  } as FinalizeV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountMint,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountTokenProgram
  >;

  return instruction;
}

export type ParsedFinalizeV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the mint account */
    mint: TAccountMetas[1];
    /** The authority of the Genesis account */
    authority: TAccountMetas[2];
    /** The wallet where fees are sent */
    feeWallet: TAccountMetas[3];
    /** The SPL Token program */
    tokenProgram: TAccountMetas[4];
  };
  data: FinalizeV1InstructionData;
};

export function parseFinalizeV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedFinalizeV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      mint: getNextAccount(),
      authority: getNextAccount(),
      feeWallet: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getFinalizeV1InstructionDataDecoder().decode(instruction.data),
  };
}
