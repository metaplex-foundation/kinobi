/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type RefundVaultDepositV2Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountBaseMint extends string | IAccountMeta<string> = string,
  TAccountQuoteMint extends string | IAccountMeta<string> = string,
  TAccountBucketQuoteTokenAccount extends string | IAccountMeta<string> =
    string,
  TAccountRecipientQuoteTokenAccount extends string | IAccountMeta<string> =
    string,
  TAccountDepositPda extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountRecipient extends string | IAccountMeta<string> = string,
  TAccountActionsAuthority extends string | IAccountMeta<string> = string,
  TAccountQuoteTokenProgram extends string | IAccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? ReadonlyAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountBaseMint extends string
        ? ReadonlyAccount<TAccountBaseMint>
        : TAccountBaseMint,
      TAccountQuoteMint extends string
        ? ReadonlyAccount<TAccountQuoteMint>
        : TAccountQuoteMint,
      TAccountBucketQuoteTokenAccount extends string
        ? WritableAccount<TAccountBucketQuoteTokenAccount>
        : TAccountBucketQuoteTokenAccount,
      TAccountRecipientQuoteTokenAccount extends string
        ? WritableAccount<TAccountRecipientQuoteTokenAccount>
        : TAccountRecipientQuoteTokenAccount,
      TAccountDepositPda extends string
        ? WritableAccount<TAccountDepositPda>
        : TAccountDepositPda,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountRecipient extends string
        ? ReadonlyAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountActionsAuthority extends string
        ? ReadonlySignerAccount<TAccountActionsAuthority> &
            IAccountSignerMeta<TAccountActionsAuthority>
        : TAccountActionsAuthority,
      TAccountQuoteTokenProgram extends string
        ? ReadonlyAccount<TAccountQuoteTokenProgram>
        : TAccountQuoteTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundVaultDepositV2InstructionData = {
  discriminator: number;
  padding: Array<number>;
};

export type RefundVaultDepositV2InstructionDataArgs = {
  padding: Array<number>;
};

export function getRefundVaultDepositV2InstructionDataEncoder(): Encoder<RefundVaultDepositV2InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 7 })],
    ]),
    (value) => ({ ...value, discriminator: 63 })
  );
}

export function getRefundVaultDepositV2InstructionDataDecoder(): Decoder<RefundVaultDepositV2InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 7 })],
  ]);
}

export function getRefundVaultDepositV2InstructionDataCodec(): Codec<
  RefundVaultDepositV2InstructionDataArgs,
  RefundVaultDepositV2InstructionData
> {
  return combineCodec(
    getRefundVaultDepositV2InstructionDataEncoder(),
    getRefundVaultDepositV2InstructionDataDecoder()
  );
}

export type RefundVaultDepositV2Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
  TAccountBucketQuoteTokenAccount extends string = string,
  TAccountRecipientQuoteTokenAccount extends string = string,
  TAccountDepositPda extends string = string,
  TAccountPayer extends string = string,
  TAccountRecipient extends string = string,
  TAccountActionsAuthority extends string = string,
  TAccountQuoteTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the vault bucket */
  bucket: Address<TAccountBucket>;
  /** The mint account of the base token */
  baseMint: Address<TAccountBaseMint>;
  /** The mint account of the quote token */
  quoteMint: Address<TAccountQuoteMint>;
  /** The bucket's quote token account */
  bucketQuoteTokenAccount: Address<TAccountBucketQuoteTokenAccount>;
  /** The recipient's quote token account */
  recipientQuoteTokenAccount: Address<TAccountRecipientQuoteTokenAccount>;
  /** The deposit PDA account */
  depositPda: Address<TAccountDepositPda>;
  /** The account paying for ATA creation and receiving rent from closed deposit PDA */
  payer: TransactionSigner<TAccountPayer>;
  /** The recipient of the refund */
  recipient: Address<TAccountRecipient>;
  /** The actions authority for the vault bucket */
  actionsAuthority: TransactionSigner<TAccountActionsAuthority>;
  /** The SPL Token program for the quote token */
  quoteTokenProgram: Address<TAccountQuoteTokenProgram>;
  /** The Associated Token program */
  associatedTokenProgram: Address<TAccountAssociatedTokenProgram>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  padding: RefundVaultDepositV2InstructionDataArgs['padding'];
};

export function getRefundVaultDepositV2Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TAccountBucketQuoteTokenAccount extends string,
  TAccountRecipientQuoteTokenAccount extends string,
  TAccountDepositPda extends string,
  TAccountPayer extends string,
  TAccountRecipient extends string,
  TAccountActionsAuthority extends string,
  TAccountQuoteTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountSystemProgram extends string,
>(
  input: RefundVaultDepositV2Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountBucketQuoteTokenAccount,
    TAccountRecipientQuoteTokenAccount,
    TAccountDepositPda,
    TAccountPayer,
    TAccountRecipient,
    TAccountActionsAuthority,
    TAccountQuoteTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >
): RefundVaultDepositV2Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountBaseMint,
  TAccountQuoteMint,
  TAccountBucketQuoteTokenAccount,
  TAccountRecipientQuoteTokenAccount,
  TAccountDepositPda,
  TAccountPayer,
  TAccountRecipient,
  TAccountActionsAuthority,
  TAccountQuoteTokenProgram,
  TAccountAssociatedTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: false },
    bucket: { value: input.bucket ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
    bucketQuoteTokenAccount: {
      value: input.bucketQuoteTokenAccount ?? null,
      isWritable: true,
    },
    recipientQuoteTokenAccount: {
      value: input.recipientQuoteTokenAccount ?? null,
      isWritable: true,
    },
    depositPda: { value: input.depositPda ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: false },
    actionsAuthority: {
      value: input.actionsAuthority ?? null,
      isWritable: false,
    },
    quoteTokenProgram: {
      value: input.quoteTokenProgram ?? null,
      isWritable: false,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
      getAccountMeta(accounts.bucketQuoteTokenAccount),
      getAccountMeta(accounts.recipientQuoteTokenAccount),
      getAccountMeta(accounts.depositPda),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.actionsAuthority),
      getAccountMeta(accounts.quoteTokenProgram),
      getAccountMeta(accounts.associatedTokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRefundVaultDepositV2InstructionDataEncoder().encode(
      args as RefundVaultDepositV2InstructionDataArgs
    ),
  } as RefundVaultDepositV2Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountBucketQuoteTokenAccount,
    TAccountRecipientQuoteTokenAccount,
    TAccountDepositPda,
    TAccountPayer,
    TAccountRecipient,
    TAccountActionsAuthority,
    TAccountQuoteTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedRefundVaultDepositV2Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the vault bucket */
    bucket: TAccountMetas[1];
    /** The mint account of the base token */
    baseMint: TAccountMetas[2];
    /** The mint account of the quote token */
    quoteMint: TAccountMetas[3];
    /** The bucket's quote token account */
    bucketQuoteTokenAccount: TAccountMetas[4];
    /** The recipient's quote token account */
    recipientQuoteTokenAccount: TAccountMetas[5];
    /** The deposit PDA account */
    depositPda: TAccountMetas[6];
    /** The account paying for ATA creation and receiving rent from closed deposit PDA */
    payer: TAccountMetas[7];
    /** The recipient of the refund */
    recipient: TAccountMetas[8];
    /** The actions authority for the vault bucket */
    actionsAuthority: TAccountMetas[9];
    /** The SPL Token program for the quote token */
    quoteTokenProgram: TAccountMetas[10];
    /** The Associated Token program */
    associatedTokenProgram: TAccountMetas[11];
    /** The system program */
    systemProgram: TAccountMetas[12];
  };
  data: RefundVaultDepositV2InstructionData;
};

export function parseRefundVaultDepositV2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRefundVaultDepositV2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 13) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      baseMint: getNextAccount(),
      quoteMint: getNextAccount(),
      bucketQuoteTokenAccount: getNextAccount(),
      recipientQuoteTokenAccount: getNextAccount(),
      depositPda: getNextAccount(),
      payer: getNextAccount(),
      recipient: getNextAccount(),
      actionsAuthority: getNextAccount(),
      quoteTokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundVaultDepositV2InstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
