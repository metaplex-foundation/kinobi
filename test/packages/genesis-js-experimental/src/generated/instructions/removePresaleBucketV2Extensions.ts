/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';
import {
  PresaleV2ExtensionType,
  PresaleV2ExtensionTypeArgs,
  getPresaleV2ExtensionTypeDecoder,
  getPresaleV2ExtensionTypeEncoder,
} from '../types';

export type RemovePresaleBucketV2ExtensionsInstruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? ReadonlyAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type RemovePresaleBucketV2ExtensionsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  extensionTypes: Array<PresaleV2ExtensionType>;
};

export type RemovePresaleBucketV2ExtensionsInstructionDataArgs = {
  padding: Array<number>;
  extensionTypes: Array<PresaleV2ExtensionTypeArgs>;
};

export function getRemovePresaleBucketV2ExtensionsInstructionDataEncoder(): Encoder<RemovePresaleBucketV2ExtensionsInstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 3 })],
      ['extensionTypes', getArrayEncoder(getPresaleV2ExtensionTypeEncoder())],
    ]),
    (value) => ({ ...value, discriminator: 79 })
  );
}

export function getRemovePresaleBucketV2ExtensionsInstructionDataDecoder(): Decoder<RemovePresaleBucketV2ExtensionsInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 3 })],
    ['extensionTypes', getArrayDecoder(getPresaleV2ExtensionTypeDecoder())],
  ]);
}

export function getRemovePresaleBucketV2ExtensionsInstructionDataCodec(): Codec<
  RemovePresaleBucketV2ExtensionsInstructionDataArgs,
  RemovePresaleBucketV2ExtensionsInstructionData
> {
  return combineCodec(
    getRemovePresaleBucketV2ExtensionsInstructionDataEncoder(),
    getRemovePresaleBucketV2ExtensionsInstructionDataDecoder()
  );
}

export type RemovePresaleBucketV2ExtensionsInput<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the bucket */
  bucket: Address<TAccountBucket>;
  /** The account paying for the transaction fees */
  payer: TransactionSigner<TAccountPayer>;
  /** The authority of the genesis account */
  authority?: TransactionSigner<TAccountAuthority>;
  padding: RemovePresaleBucketV2ExtensionsInstructionDataArgs['padding'];
  extensionTypes: RemovePresaleBucketV2ExtensionsInstructionDataArgs['extensionTypes'];
};

export function getRemovePresaleBucketV2ExtensionsInstruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
>(
  input: RemovePresaleBucketV2ExtensionsInput<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountPayer,
    TAccountAuthority
  >
): RemovePresaleBucketV2ExtensionsInstruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountPayer,
  TAccountAuthority
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: false },
    bucket: { value: input.bucket ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
    ],
    programAddress,
    data: getRemovePresaleBucketV2ExtensionsInstructionDataEncoder().encode(
      args as RemovePresaleBucketV2ExtensionsInstructionDataArgs
    ),
  } as RemovePresaleBucketV2ExtensionsInstruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountPayer,
    TAccountAuthority
  >;

  return instruction;
}

export type ParsedRemovePresaleBucketV2ExtensionsInstruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the bucket */
    bucket: TAccountMetas[1];
    /** The account paying for the transaction fees */
    payer: TAccountMetas[2];
    /** The authority of the genesis account */
    authority?: TAccountMetas[3] | undefined;
  };
  data: RemovePresaleBucketV2ExtensionsInstructionData;
};

export function parseRemovePresaleBucketV2ExtensionsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRemovePresaleBucketV2ExtensionsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextOptionalAccount(),
    },
    data: getRemovePresaleBucketV2ExtensionsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
