/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  FeeOverrideType,
  FeeOverrideTypeArgs,
  getFeeDiscriminantsDecoder,
  getFeeDiscriminantsEncoder,
  getFeeOverrideTypeDecoder,
  getFeeOverrideTypeEncoder,
} from '../types';

export type FeeOverrideV2Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountBaseMint extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountBaseMint extends string
        ? WritableAccount<TAccountBaseMint>
        : TAccountBaseMint,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FeeOverrideV2InstructionData = {
  discriminator: number;
  feeOverrideType: FeeOverrideType;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  feeOverride: bigint;
};

export type FeeOverrideV2InstructionDataArgs = {
  feeOverrideType: FeeOverrideTypeArgs;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  feeOverride: number | bigint;
};

export function getFeeOverrideV2InstructionDataEncoder(): Encoder<FeeOverrideV2InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['feeOverrideType', getFeeOverrideTypeEncoder()],
      ['feeType', getFeeDiscriminantsEncoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 5 })],
      ['feeOverride', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: 47 })
  );
}

export function getFeeOverrideV2InstructionDataDecoder(): Decoder<FeeOverrideV2InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['feeOverrideType', getFeeOverrideTypeDecoder()],
    ['feeType', getFeeDiscriminantsDecoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 5 })],
    ['feeOverride', getU64Decoder()],
  ]);
}

export function getFeeOverrideV2InstructionDataCodec(): Codec<
  FeeOverrideV2InstructionDataArgs,
  FeeOverrideV2InstructionData
> {
  return combineCodec(
    getFeeOverrideV2InstructionDataEncoder(),
    getFeeOverrideV2InstructionDataDecoder()
  );
}

export type FeeOverrideV2Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountBaseMint extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the launchpool bucket */
  bucket: Address<TAccountBucket>;
  /** The address of the base token mint account */
  baseMint: Address<TAccountBaseMint>;
  /** The global Metaplex authority overriding the default fees */
  authority: TransactionSigner<TAccountAuthority>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  feeOverrideType: FeeOverrideV2InstructionDataArgs['feeOverrideType'];
  feeType: FeeOverrideV2InstructionDataArgs['feeType'];
  padding: FeeOverrideV2InstructionDataArgs['padding'];
  feeOverride: FeeOverrideV2InstructionDataArgs['feeOverride'];
};

export function getFeeOverrideV2Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountBaseMint extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
>(
  input: FeeOverrideV2Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountAuthority,
    TAccountSystemProgram
  >
): FeeOverrideV2Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountBaseMint,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    bucket: { value: input.bucket ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getFeeOverrideV2InstructionDataEncoder().encode(
      args as FeeOverrideV2InstructionDataArgs
    ),
  } as FeeOverrideV2Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountBaseMint,
    TAccountAuthority,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedFeeOverrideV2Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the launchpool bucket */
    bucket: TAccountMetas[1];
    /** The address of the base token mint account */
    baseMint: TAccountMetas[2];
    /** The global Metaplex authority overriding the default fees */
    authority: TAccountMetas[3];
    /** The system program */
    systemProgram: TAccountMetas[4];
  };
  data: FeeOverrideV2InstructionData;
};

export function parseFeeOverrideV2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedFeeOverrideV2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      baseMint: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFeeOverrideV2InstructionDataDecoder().decode(instruction.data),
  };
}
