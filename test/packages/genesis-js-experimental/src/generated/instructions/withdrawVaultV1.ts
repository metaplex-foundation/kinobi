/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type WithdrawVaultV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountDepositPda extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountFeeWallet extends string | IAccountMeta<string> = string,
  TAccountBackendSigner extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? ReadonlyAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountDepositPda extends string
        ? WritableAccount<TAccountDepositPda>
        : TAccountDepositPda,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFeeWallet extends string
        ? WritableAccount<TAccountFeeWallet>
        : TAccountFeeWallet,
      TAccountBackendSigner extends string
        ? ReadonlySignerAccount<TAccountBackendSigner> &
            IAccountSignerMeta<TAccountBackendSigner>
        : TAccountBackendSigner,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawVaultV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
  amount: bigint;
};

export type WithdrawVaultV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
  amount: number | bigint;
};

export function getWithdrawVaultV1InstructionDataEncoder(): Encoder<WithdrawVaultV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['index', getU8Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 6 })],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: 28 })
  );
}

export function getWithdrawVaultV1InstructionDataDecoder(): Decoder<WithdrawVaultV1InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['index', getU8Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 6 })],
    ['amount', getU64Decoder()],
  ]);
}

export function getWithdrawVaultV1InstructionDataCodec(): Codec<
  WithdrawVaultV1InstructionDataArgs,
  WithdrawVaultV1InstructionData
> {
  return combineCodec(
    getWithdrawVaultV1InstructionDataEncoder(),
    getWithdrawVaultV1InstructionDataDecoder()
  );
}

export type WithdrawVaultV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountDepositPda extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountFeeWallet extends string = string,
  TAccountBackendSigner extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the bucket */
  bucket: Address<TAccountBucket>;
  /** The PDA to track the user's deposit */
  depositPda: Address<TAccountDepositPda>;
  /** The account paying for the storage fees */
  payer: TransactionSigner<TAccountPayer>;
  /** The authority of deposit PDA */
  authority?: TransactionSigner<TAccountAuthority>;
  /** The wallet where cancel fees are sent */
  feeWallet: Address<TAccountFeeWallet>;
  /** Backend authorisation signer */
  backendSigner?: TransactionSigner<TAccountBackendSigner>;
  index: WithdrawVaultV1InstructionDataArgs['index'];
  padding: WithdrawVaultV1InstructionDataArgs['padding'];
  amount: WithdrawVaultV1InstructionDataArgs['amount'];
};

export function getWithdrawVaultV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountDepositPda extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountFeeWallet extends string,
  TAccountBackendSigner extends string,
>(
  input: WithdrawVaultV1Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountDepositPda,
    TAccountPayer,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountBackendSigner
  >
): WithdrawVaultV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountDepositPda,
  TAccountPayer,
  TAccountAuthority,
  TAccountFeeWallet,
  TAccountBackendSigner
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: false },
    bucket: { value: input.bucket ?? null, isWritable: true },
    depositPda: { value: input.depositPda ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    feeWallet: { value: input.feeWallet ?? null, isWritable: true },
    backendSigner: { value: input.backendSigner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.depositPda),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.feeWallet),
      getAccountMeta(accounts.backendSigner),
    ],
    programAddress,
    data: getWithdrawVaultV1InstructionDataEncoder().encode(
      args as WithdrawVaultV1InstructionDataArgs
    ),
  } as WithdrawVaultV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountDepositPda,
    TAccountPayer,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountBackendSigner
  >;

  return instruction;
}

export type ParsedWithdrawVaultV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the bucket */
    bucket: TAccountMetas[1];
    /** The PDA to track the user's deposit */
    depositPda: TAccountMetas[2];
    /** The account paying for the storage fees */
    payer: TAccountMetas[3];
    /** The authority of deposit PDA */
    authority?: TAccountMetas[4] | undefined;
    /** The wallet where cancel fees are sent */
    feeWallet: TAccountMetas[5];
    /** Backend authorisation signer */
    backendSigner?: TAccountMetas[6] | undefined;
  };
  data: WithdrawVaultV1InstructionData;
};

export function parseWithdrawVaultV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedWithdrawVaultV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      depositPda: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextOptionalAccount(),
      feeWallet: getNextAccount(),
      backendSigner: getNextOptionalAccount(),
    },
    data: getWithdrawVaultV1InstructionDataDecoder().decode(instruction.data),
  };
}
