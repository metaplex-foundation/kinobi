/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type AddVaultBucketV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountFeeWallet extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TAccountBackendSigner extends string | IAccountMeta<string> = string,
  TAccountActionsAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFeeWallet extends string
        ? WritableAccount<TAccountFeeWallet>
        : TAccountFeeWallet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountBackendSigner extends string
        ? ReadonlyAccount<TAccountBackendSigner>
        : TAccountBackendSigner,
      TAccountActionsAuthority extends string
        ? ReadonlyAccount<TAccountActionsAuthority>
        : TAccountActionsAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type AddVaultBucketV1InstructionData = {
  discriminator: number;
  index: number;
  allowWithdrawals: boolean;
  padding: Array<number>;
  start: bigint;
  end: bigint;
};

export type AddVaultBucketV1InstructionDataArgs = {
  index: number;
  allowWithdrawals: boolean;
  padding: Array<number>;
  start: number | bigint;
  end: number | bigint;
};

export function getAddVaultBucketV1InstructionDataEncoder(): Encoder<AddVaultBucketV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['index', getU8Encoder()],
      ['allowWithdrawals', getBooleanEncoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 5 })],
      ['start', getI64Encoder()],
      ['end', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: 26 })
  );
}

export function getAddVaultBucketV1InstructionDataDecoder(): Decoder<AddVaultBucketV1InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['index', getU8Decoder()],
    ['allowWithdrawals', getBooleanDecoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 5 })],
    ['start', getI64Decoder()],
    ['end', getI64Decoder()],
  ]);
}

export function getAddVaultBucketV1InstructionDataCodec(): Codec<
  AddVaultBucketV1InstructionDataArgs,
  AddVaultBucketV1InstructionData
> {
  return combineCodec(
    getAddVaultBucketV1InstructionDataEncoder(),
    getAddVaultBucketV1InstructionDataDecoder()
  );
}

export type AddVaultBucketV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountFeeWallet extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountBackendSigner extends string = string,
  TAccountActionsAuthority extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the bucket account */
  bucket: Address<TAccountBucket>;
  /** The account paying for the storage fees */
  payer: TransactionSigner<TAccountPayer>;
  /** The authority of the mint */
  authority?: TransactionSigner<TAccountAuthority>;
  /** The wallet where fees are sent */
  feeWallet: Address<TAccountFeeWallet>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Backend authorisation signer */
  backendSigner?: Address<TAccountBackendSigner>;
  /** The authority of the vault bucket actions */
  actionsAuthority?: Address<TAccountActionsAuthority>;
  index: AddVaultBucketV1InstructionDataArgs['index'];
  allowWithdrawals: AddVaultBucketV1InstructionDataArgs['allowWithdrawals'];
  padding: AddVaultBucketV1InstructionDataArgs['padding'];
  start: AddVaultBucketV1InstructionDataArgs['start'];
  end: AddVaultBucketV1InstructionDataArgs['end'];
};

export function getAddVaultBucketV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountFeeWallet extends string,
  TAccountSystemProgram extends string,
  TAccountBackendSigner extends string,
  TAccountActionsAuthority extends string,
>(
  input: AddVaultBucketV1Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountPayer,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountSystemProgram,
    TAccountBackendSigner,
    TAccountActionsAuthority
  >
): AddVaultBucketV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountPayer,
  TAccountAuthority,
  TAccountFeeWallet,
  TAccountSystemProgram,
  TAccountBackendSigner,
  TAccountActionsAuthority
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    bucket: { value: input.bucket ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    feeWallet: { value: input.feeWallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    backendSigner: { value: input.backendSigner ?? null, isWritable: false },
    actionsAuthority: {
      value: input.actionsAuthority ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.feeWallet),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.backendSigner),
      getAccountMeta(accounts.actionsAuthority),
    ],
    programAddress,
    data: getAddVaultBucketV1InstructionDataEncoder().encode(
      args as AddVaultBucketV1InstructionDataArgs
    ),
  } as AddVaultBucketV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountPayer,
    TAccountAuthority,
    TAccountFeeWallet,
    TAccountSystemProgram,
    TAccountBackendSigner,
    TAccountActionsAuthority
  >;

  return instruction;
}

export type ParsedAddVaultBucketV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the bucket account */
    bucket: TAccountMetas[1];
    /** The account paying for the storage fees */
    payer: TAccountMetas[2];
    /** The authority of the mint */
    authority?: TAccountMetas[3] | undefined;
    /** The wallet where fees are sent */
    feeWallet: TAccountMetas[4];
    /** The system program */
    systemProgram: TAccountMetas[5];
    /** Backend authorisation signer */
    backendSigner?: TAccountMetas[6] | undefined;
    /** The authority of the vault bucket actions */
    actionsAuthority?: TAccountMetas[7] | undefined;
  };
  data: AddVaultBucketV1InstructionData;
};

export function parseAddVaultBucketV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedAddVaultBucketV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextOptionalAccount(),
      feeWallet: getNextAccount(),
      systemProgram: getNextAccount(),
      backendSigner: getNextOptionalAccount(),
      actionsAuthority: getNextOptionalAccount(),
    },
    data: getAddVaultBucketV1InstructionDataDecoder().decode(instruction.data),
  };
}
