/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  WritableAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { GENESIS_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type CloseAuctionBidV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount extends string | IAccountMeta<string> = string,
  TAccountBucket extends string | IAccountMeta<string> = string,
  TAccountMint extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountBid extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> =
    '11111111111111111111111111111111',
  TAccountBackendSigner extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountGenesisAccount extends string
        ? WritableAccount<TAccountGenesisAccount>
        : TAccountGenesisAccount,
      TAccountBucket extends string
        ? WritableAccount<TAccountBucket>
        : TAccountBucket,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountPayer extends string
        ? WritableAccount<TAccountPayer>
        : TAccountPayer,
      TAccountBid extends string ? WritableAccount<TAccountBid> : TAccountBid,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountBackendSigner extends string
        ? ReadonlySignerAccount<TAccountBackendSigner> &
            IAccountSignerMeta<TAccountBackendSigner>
        : TAccountBackendSigner,
      ...TRemainingAccounts,
    ]
  >;

export type CloseAuctionBidV1InstructionData = {
  discriminator: number;
  index: number;
  bidIndex: number;
  padding: Array<number>;
};

export type CloseAuctionBidV1InstructionDataArgs = {
  index: number;
  bidIndex: number;
  padding: Array<number>;
};

export function getCloseAuctionBidV1InstructionDataEncoder(): Encoder<CloseAuctionBidV1InstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['index', getU8Encoder()],
      ['bidIndex', getU16Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 4 })],
    ]),
    (value) => ({ ...value, discriminator: 38 })
  );
}

export function getCloseAuctionBidV1InstructionDataDecoder(): Decoder<CloseAuctionBidV1InstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['index', getU8Decoder()],
    ['bidIndex', getU16Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 4 })],
  ]);
}

export function getCloseAuctionBidV1InstructionDataCodec(): Codec<
  CloseAuctionBidV1InstructionDataArgs,
  CloseAuctionBidV1InstructionData
> {
  return combineCodec(
    getCloseAuctionBidV1InstructionDataEncoder(),
    getCloseAuctionBidV1InstructionDataDecoder()
  );
}

export type CloseAuctionBidV1Input<
  TAccountGenesisAccount extends string = string,
  TAccountBucket extends string = string,
  TAccountMint extends string = string,
  TAccountPayer extends string = string,
  TAccountBid extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountBackendSigner extends string = string,
> = {
  /** The address of the genesis account */
  genesisAccount: Address<TAccountGenesisAccount>;
  /** The address of the auction bucket */
  bucket: Address<TAccountBucket>;
  /** The address of the mint */
  mint: Address<TAccountMint>;
  /** The default rent refund recipient (bidder) */
  payer: Address<TAccountPayer>;
  /** The address of the auction bid PDA */
  bid: Address<TAccountBid>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Backend authorisation signer */
  backendSigner?: TransactionSigner<TAccountBackendSigner>;
  index: CloseAuctionBidV1InstructionDataArgs['index'];
  bidIndex: CloseAuctionBidV1InstructionDataArgs['bidIndex'];
  padding: CloseAuctionBidV1InstructionDataArgs['padding'];
};

export function getCloseAuctionBidV1Instruction<
  TAccountGenesisAccount extends string,
  TAccountBucket extends string,
  TAccountMint extends string,
  TAccountPayer extends string,
  TAccountBid extends string,
  TAccountSystemProgram extends string,
  TAccountBackendSigner extends string,
>(
  input: CloseAuctionBidV1Input<
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountMint,
    TAccountPayer,
    TAccountBid,
    TAccountSystemProgram,
    TAccountBackendSigner
  >
): CloseAuctionBidV1Instruction<
  typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountGenesisAccount,
  TAccountBucket,
  TAccountMint,
  TAccountPayer,
  TAccountBid,
  TAccountSystemProgram,
  TAccountBackendSigner
> {
  // Program address.
  const programAddress = GENESIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    genesisAccount: { value: input.genesisAccount ?? null, isWritable: true },
    bucket: { value: input.bucket ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    backendSigner: { value: input.backendSigner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.genesisAccount),
      getAccountMeta(accounts.bucket),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.bid),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.backendSigner),
    ],
    programAddress,
    data: getCloseAuctionBidV1InstructionDataEncoder().encode(
      args as CloseAuctionBidV1InstructionDataArgs
    ),
  } as CloseAuctionBidV1Instruction<
    typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
    TAccountGenesisAccount,
    TAccountBucket,
    TAccountMint,
    TAccountPayer,
    TAccountBid,
    TAccountSystemProgram,
    TAccountBackendSigner
  >;

  return instruction;
}

export type ParsedCloseAuctionBidV1Instruction<
  TProgram extends string = typeof GENESIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the genesis account */
    genesisAccount: TAccountMetas[0];
    /** The address of the auction bucket */
    bucket: TAccountMetas[1];
    /** The address of the mint */
    mint: TAccountMetas[2];
    /** The default rent refund recipient (bidder) */
    payer: TAccountMetas[3];
    /** The address of the auction bid PDA */
    bid: TAccountMetas[4];
    /** The system program */
    systemProgram: TAccountMetas[5];
    /** Backend authorisation signer */
    backendSigner?: TAccountMetas[6] | undefined;
  };
  data: CloseAuctionBidV1InstructionData;
};

export function parseCloseAuctionBidV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedCloseAuctionBidV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GENESIS_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      genesisAccount: getNextAccount(),
      bucket: getNextAccount(),
      mint: getNextAccount(),
      payer: getNextAccount(),
      bid: getNextAccount(),
      systemProgram: getNextAccount(),
      backendSigner: getNextOptionalAccount(),
    },
    data: getCloseAuctionBidV1InstructionDataDecoder().decode(instruction.data),
  };
}
