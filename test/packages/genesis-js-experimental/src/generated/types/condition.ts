/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Address,
  getAddressDecoder,
  getAddressEncoder,
} from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  GetDataEnumKind,
  GetDataEnumKindContent,
  Option,
  OptionOrNullable,
  assertIsFixedSize,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getDataEnumDecoder,
  getDataEnumEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
} from '@solana/codecs';
import {
  BucketTimes,
  BucketTimesArgs,
  getBucketTimesDecoder,
  getBucketTimesEncoder,
} from '.';

export type Condition =
  | {
      __kind: 'Uninitialized';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'TimeAbsolute';
      padding: Array<number>;
      time: bigint;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'TimeRelative';
      bucketTime: BucketTimes;
      padding: Array<number>;
      timeOffset: bigint;
      bucket: Address;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Triggered';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Never';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Always';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    };

export type ConditionArgs =
  | {
      __kind: 'Uninitialized';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'TimeAbsolute';
      padding: Array<number>;
      time: number | bigint;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'TimeRelative';
      bucketTime: BucketTimesArgs;
      padding: Array<number>;
      timeOffset: number | bigint;
      bucket: Address;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Triggered';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Never';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Always';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    };

export function getConditionEncoder(): Encoder<ConditionArgs> {
  return getDataEnumEncoder([
    [
      'Uninitialized',
      getStructEncoder([
        ['padding', getArrayEncoder(getU8Encoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder31 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder31,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder31.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder31.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder31.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder31.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'TimeAbsolute',
      getStructEncoder([
        ['padding', getArrayEncoder(getU8Encoder(), { size: 47 })],
        ['time', getI64Encoder()],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder32 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder32,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder32.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder32.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder32.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder32.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'TimeRelative',
      getStructEncoder([
        ['bucketTime', getBucketTimesEncoder()],
        ['padding', getArrayEncoder(getU8Encoder(), { size: 14 })],
        ['timeOffset', getI64Encoder()],
        ['bucket', getAddressEncoder()],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder33 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder33,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder33.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder33.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder33.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder33.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Triggered',
      getStructEncoder([
        ['padding', getArrayEncoder(getU8Encoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder34 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder34,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder34.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder34.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder34.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder34.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Never',
      getStructEncoder([
        ['padding', getArrayEncoder(getU8Encoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder35 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder35,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder35.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder35.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder35.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder35.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Always',
      getStructEncoder([
        ['padding', getArrayEncoder(getU8Encoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionEncoder36 = getI64Encoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionEncoder36,
              'Fixed-size options require an encoder with a fixed size.'
            );
            if (__fixedSizeOptionEncoder36.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the encoder fixed size.'
              );
            }
            const normalize = (input: OptionOrNullable<number | bigint>) => {
              if (input == null) {
                return null;
              }
              if (typeof input === 'object' && '__option' in input) {
                return input.__option === 'None' ? null : input.value;
              }
              return input;
            };
            return {
              fixedSize: __fixedSizeOptionEncoder36.fixedSize,
              encode: (value: OptionOrNullable<number | bigint>) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  return sentinel.slice();
                }
                return __fixedSizeOptionEncoder36.encode(normalized);
              },
              write: (
                value: OptionOrNullable<number | bigint>,
                bytes: Uint8Array,
                offset: number
              ) => {
                const normalized = normalize(value);
                if (normalized == null) {
                  bytes.set(sentinel, offset);
                  return offset + sentinel.length;
                }
                return __fixedSizeOptionEncoder36.write(
                  normalized,
                  bytes,
                  offset
                );
              },
            } satisfies Encoder<OptionOrNullable<number | bigint>>;
          })(),
        ],
      ]),
    ],
  ]);
}

export function getConditionDecoder(): Decoder<Condition> {
  return getDataEnumDecoder([
    [
      'Uninitialized',
      getStructDecoder([
        ['padding', getArrayDecoder(getU8Decoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder31 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder31,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder31.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder31.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder31.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder31.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder31,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'TimeAbsolute',
      getStructDecoder([
        ['padding', getArrayDecoder(getU8Decoder(), { size: 47 })],
        ['time', getI64Decoder()],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder32 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder32,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder32.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder32.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder32.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder32.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder32,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'TimeRelative',
      getStructDecoder([
        ['bucketTime', getBucketTimesDecoder()],
        ['padding', getArrayDecoder(getU8Decoder(), { size: 14 })],
        ['timeOffset', getI64Decoder()],
        ['bucket', getAddressDecoder()],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder33 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder33,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder33.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder33.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder33.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder33.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder33,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Triggered',
      getStructDecoder([
        ['padding', getArrayDecoder(getU8Decoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder34 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder34,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder34.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder34.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder34.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder34.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder34,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Never',
      getStructDecoder([
        ['padding', getArrayDecoder(getU8Decoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder35 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder35,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder35.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder35.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder35.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder35.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder35,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
    [
      'Always',
      getStructDecoder([
        ['padding', getArrayDecoder(getU8Decoder(), { size: 55 })],
        [
          'triggeredTimestamp',
          (() => {
            const __fixedSizeOptionDecoder36 = getI64Decoder();
            const sentinel = new Uint8Array([
              255, 255, 255, 255, 255, 255, 255, 127,
            ]);
            assertIsFixedSize(
              __fixedSizeOptionDecoder36,
              'Fixed-size options require a decoder with a fixed size.'
            );
            if (__fixedSizeOptionDecoder36.fixedSize !== sentinel.length) {
              throw new Error(
                'Fixed-size option sentinel length must match the decoder fixed size.'
              );
            }
            const isSentinel = (bytes: Uint8Array, offset: number) => {
              const slice = bytes.slice(
                offset,
                offset + __fixedSizeOptionDecoder36.fixedSize
              );
              return slice.every((byte, i) => byte === sentinel[i]);
            };
            const none = (): Option<bigint> => ({ __option: 'None' });
            const some = (value: bigint): Option<bigint> => ({
              __option: 'Some',
              value,
            });
            const readOption = (bytes: Uint8Array, offset = 0) => {
              if (isSentinel(bytes, offset)) {
                return [
                  none(),
                  offset + __fixedSizeOptionDecoder36.fixedSize,
                ] as [Option<bigint>, number];
              }
              const [value, newOffset] = __fixedSizeOptionDecoder36.read(
                bytes,
                offset
              ) as [bigint, number];
              return [some(value), newOffset] as [Option<bigint>, number];
            };
            return {
              ...__fixedSizeOptionDecoder36,
              decode: (bytes: Uint8Array, offset = 0) => {
                const [value] = readOption(bytes, offset);
                return value;
              },
              read: readOption,
            } satisfies Decoder<Option<bigint>>;
          })(),
        ],
      ]),
    ],
  ]);
}

export function getConditionCodec(): Codec<ConditionArgs, Condition> {
  return combineCodec(getConditionEncoder(), getConditionDecoder());
}

// Data Enum Helpers.
export function condition(
  kind: 'Uninitialized',
  data: GetDataEnumKindContent<ConditionArgs, 'Uninitialized'>
): GetDataEnumKind<ConditionArgs, 'Uninitialized'>;
export function condition(
  kind: 'TimeAbsolute',
  data: GetDataEnumKindContent<ConditionArgs, 'TimeAbsolute'>
): GetDataEnumKind<ConditionArgs, 'TimeAbsolute'>;
export function condition(
  kind: 'TimeRelative',
  data: GetDataEnumKindContent<ConditionArgs, 'TimeRelative'>
): GetDataEnumKind<ConditionArgs, 'TimeRelative'>;
export function condition(
  kind: 'Triggered',
  data: GetDataEnumKindContent<ConditionArgs, 'Triggered'>
): GetDataEnumKind<ConditionArgs, 'Triggered'>;
export function condition(
  kind: 'Never',
  data: GetDataEnumKindContent<ConditionArgs, 'Never'>
): GetDataEnumKind<ConditionArgs, 'Never'>;
export function condition(
  kind: 'Always',
  data: GetDataEnumKindContent<ConditionArgs, 'Always'>
): GetDataEnumKind<ConditionArgs, 'Always'>;
export function condition<K extends ConditionArgs['__kind'], Data>(
  kind: K,
  data?: Data
) {
  return Array.isArray(data)
    ? { __kind: kind, fields: data }
    : { __kind: kind, ...(data ?? {}) };
}

export function isCondition<K extends Condition['__kind']>(
  kind: K,
  value: Condition
): value is Condition & { __kind: K } {
  return value.__kind === kind;
}
