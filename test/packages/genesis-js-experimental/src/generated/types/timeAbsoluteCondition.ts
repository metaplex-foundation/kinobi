/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Codec,
  Decoder,
  Encoder,
  Option,
  OptionOrNullable,
  assertIsFixedSize,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
} from '@solana/codecs';
import {
  ConditionTypes,
  ConditionTypesArgs,
  getConditionTypesDecoder,
  getConditionTypesEncoder,
} from '.';

export type TimeAbsoluteCondition = {
  conditionType: ConditionTypes;
  padding: Array<number>;
  time: bigint;
  triggeredTimestamp: Option<bigint>;
};

export type TimeAbsoluteConditionArgs = {
  conditionType: ConditionTypesArgs;
  padding: Array<number>;
  time: number | bigint;
  triggeredTimestamp: OptionOrNullable<number | bigint>;
};

export function getTimeAbsoluteConditionEncoder(): Encoder<TimeAbsoluteConditionArgs> {
  return getStructEncoder([
    ['conditionType', getConditionTypesEncoder()],
    ['padding', getArrayEncoder(getU8Encoder(), { size: 47 })],
    ['time', getI64Encoder()],
    [
      'triggeredTimestamp',
      (() => {
        const __fixedSizeOptionEncoder6 = getI64Encoder();
        const sentinel = new Uint8Array([
          255, 255, 255, 255, 255, 255, 255, 127,
        ]);
        assertIsFixedSize(
          __fixedSizeOptionEncoder6,
          'Fixed-size options require an encoder with a fixed size.'
        );
        if (__fixedSizeOptionEncoder6.fixedSize !== sentinel.length) {
          throw new Error(
            'Fixed-size option sentinel length must match the encoder fixed size.'
          );
        }
        const normalize = (input: OptionOrNullable<number | bigint>) => {
          if (input == null) {
            return null;
          }
          if (typeof input === 'object' && '__option' in input) {
            return input.__option === 'None' ? null : input.value;
          }
          return input;
        };
        return {
          fixedSize: __fixedSizeOptionEncoder6.fixedSize,
          encode: (value: OptionOrNullable<number | bigint>) => {
            const normalized = normalize(value);
            if (normalized == null) {
              return sentinel.slice();
            }
            return __fixedSizeOptionEncoder6.encode(normalized);
          },
          write: (
            value: OptionOrNullable<number | bigint>,
            bytes: Uint8Array,
            offset: number
          ) => {
            const normalized = normalize(value);
            if (normalized == null) {
              bytes.set(sentinel, offset);
              return offset + sentinel.length;
            }
            return __fixedSizeOptionEncoder6.write(normalized, bytes, offset);
          },
        } satisfies Encoder<OptionOrNullable<number | bigint>>;
      })(),
    ],
  ]);
}

export function getTimeAbsoluteConditionDecoder(): Decoder<TimeAbsoluteCondition> {
  return getStructDecoder([
    ['conditionType', getConditionTypesDecoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 47 })],
    ['time', getI64Decoder()],
    [
      'triggeredTimestamp',
      (() => {
        const __fixedSizeOptionDecoder6 = getI64Decoder();
        const sentinel = new Uint8Array([
          255, 255, 255, 255, 255, 255, 255, 127,
        ]);
        assertIsFixedSize(
          __fixedSizeOptionDecoder6,
          'Fixed-size options require a decoder with a fixed size.'
        );
        if (__fixedSizeOptionDecoder6.fixedSize !== sentinel.length) {
          throw new Error(
            'Fixed-size option sentinel length must match the decoder fixed size.'
          );
        }
        const isSentinel = (bytes: Uint8Array, offset: number) => {
          const slice = bytes.slice(
            offset,
            offset + __fixedSizeOptionDecoder6.fixedSize
          );
          return slice.every((byte, i) => byte === sentinel[i]);
        };
        const none = (): Option<bigint> => ({ __option: 'None' });
        const some = (value: bigint): Option<bigint> => ({
          __option: 'Some',
          value,
        });
        const readOption = (bytes: Uint8Array, offset = 0) => {
          if (isSentinel(bytes, offset)) {
            return [none(), offset + __fixedSizeOptionDecoder6.fixedSize] as [
              Option<bigint>,
              number,
            ];
          }
          const [value, newOffset] = __fixedSizeOptionDecoder6.read(
            bytes,
            offset
          ) as [bigint, number];
          return [some(value), newOffset] as [Option<bigint>, number];
        };
        return {
          ...__fixedSizeOptionDecoder6,
          decode: (bytes: Uint8Array, offset = 0) => {
            const [value] = readOption(bytes, offset);
            return value;
          },
          read: readOption,
        } satisfies Decoder<Option<bigint>>;
      })(),
    ],
  ]);
}

export function getTimeAbsoluteConditionCodec(): Codec<
  TimeAbsoluteConditionArgs,
  TimeAbsoluteCondition
> {
  return combineCodec(
    getTimeAbsoluteConditionEncoder(),
    getTimeAbsoluteConditionDecoder()
  );
}
