/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Option,
  OptionOrNullable,
  PublicKey,
  isOption,
  none,
  some,
} from '@metaplex-foundation/umi';
import {
  GetDataEnumKind,
  GetDataEnumKindContent,
  Serializer,
  array,
  dataEnum,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { BucketTimes, BucketTimesArgs, getBucketTimesSerializer } from '.';

export type Condition =
  | {
      __kind: 'Uninitialized';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'TimeAbsolute';
      padding: Array<number>;
      time: bigint;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'TimeRelative';
      bucketTime: BucketTimes;
      padding: Array<number>;
      timeOffset: bigint;
      bucket: PublicKey;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Triggered';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Never';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    }
  | {
      __kind: 'Always';
      padding: Array<number>;
      triggeredTimestamp: Option<bigint>;
    };

export type ConditionArgs =
  | {
      __kind: 'Uninitialized';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'TimeAbsolute';
      padding: Array<number>;
      time: number | bigint;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'TimeRelative';
      bucketTime: BucketTimesArgs;
      padding: Array<number>;
      timeOffset: number | bigint;
      bucket: PublicKey;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Triggered';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Never';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    }
  | {
      __kind: 'Always';
      padding: Array<number>;
      triggeredTimestamp: OptionOrNullable<number | bigint>;
    };

export function getConditionSerializer(): Serializer<ConditionArgs, Condition> {
  return dataEnum<Condition>(
    [
      [
        'Uninitialized',
        struct<GetDataEnumKindContent<Condition, 'Uninitialized'>>([
          ['padding', array(u8(), { size: 55 })],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
      [
        'TimeAbsolute',
        struct<GetDataEnumKindContent<Condition, 'TimeAbsolute'>>([
          ['padding', array(u8(), { size: 47 })],
          ['time', i64()],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
      [
        'TimeRelative',
        struct<GetDataEnumKindContent<Condition, 'TimeRelative'>>([
          ['bucketTime', getBucketTimesSerializer()],
          ['padding', array(u8(), { size: 14 })],
          ['timeOffset', i64()],
          ['bucket', publicKeySerializer()],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
      [
        'Triggered',
        struct<GetDataEnumKindContent<Condition, 'Triggered'>>([
          ['padding', array(u8(), { size: 55 })],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
      [
        'Never',
        struct<GetDataEnumKindContent<Condition, 'Never'>>([
          ['padding', array(u8(), { size: 55 })],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
      [
        'Always',
        struct<GetDataEnumKindContent<Condition, 'Always'>>([
          ['padding', array(u8(), { size: 55 })],
          [
            'triggeredTimestamp',
            (() => {
              const inner = i64();
              const sentinel = new Uint8Array([
                255, 255, 255, 255, 255, 255, 255, 127,
              ]);
              if (inner.fixedSize == null) {
                throw new Error(
                  'Fixed-size options require an inner serializer with a fixed size.'
                );
              }
              if (inner.fixedSize !== sentinel.length) {
                throw new Error(
                  'Fixed-size option sentinel length must match the inner serializer fixed size.'
                );
              }
              const { fixedSize } = inner;
              const normalize = (input: OptionOrNullable<number | bigint>) => {
                if (input == null) {
                  return null;
                }
                if (isOption(input)) {
                  return input.__option === 'None' ? null : input.value;
                }
                return input;
              };
              return {
                description: 'Option<bigint>',
                fixedSize,
                maxSize: fixedSize,
                serialize: (value: OptionOrNullable<number | bigint>) => {
                  const normalized = normalize(value);
                  return normalized == null
                    ? sentinel.slice()
                    : inner.serialize(normalized);
                },
                deserialize: (bytes: Uint8Array, offset = 0) => {
                  const slice = bytes.slice(offset, offset + fixedSize);
                  const isSentinel = slice.every(
                    (byte, i) => byte === sentinel[i]
                  );
                  if (isSentinel) {
                    return [none<bigint>(), offset + fixedSize] as [
                      Option<bigint>,
                      number,
                    ];
                  }
                  const [value, newOffset] = inner.deserialize(bytes, offset);
                  return [some(value), newOffset] as [Option<bigint>, number];
                },
              } as Serializer<
                OptionOrNullable<number | bigint>,
                Option<bigint>
              >;
            })(),
          ],
        ]),
      ],
    ],
    { description: 'Condition' }
  ) as Serializer<ConditionArgs, Condition>;
}

// Data Enum Helpers.
export function condition(
  kind: 'Uninitialized',
  data: GetDataEnumKindContent<ConditionArgs, 'Uninitialized'>
): GetDataEnumKind<ConditionArgs, 'Uninitialized'>;
export function condition(
  kind: 'TimeAbsolute',
  data: GetDataEnumKindContent<ConditionArgs, 'TimeAbsolute'>
): GetDataEnumKind<ConditionArgs, 'TimeAbsolute'>;
export function condition(
  kind: 'TimeRelative',
  data: GetDataEnumKindContent<ConditionArgs, 'TimeRelative'>
): GetDataEnumKind<ConditionArgs, 'TimeRelative'>;
export function condition(
  kind: 'Triggered',
  data: GetDataEnumKindContent<ConditionArgs, 'Triggered'>
): GetDataEnumKind<ConditionArgs, 'Triggered'>;
export function condition(
  kind: 'Never',
  data: GetDataEnumKindContent<ConditionArgs, 'Never'>
): GetDataEnumKind<ConditionArgs, 'Never'>;
export function condition(
  kind: 'Always',
  data: GetDataEnumKindContent<ConditionArgs, 'Always'>
): GetDataEnumKind<ConditionArgs, 'Always'>;
export function condition<K extends ConditionArgs['__kind']>(
  kind: K,
  data?: any
): Extract<ConditionArgs, { __kind: K }> {
  return Array.isArray(data)
    ? { __kind: kind, fields: data }
    : { __kind: kind, ...(data ?? {}) };
}
export function isCondition<K extends Condition['__kind']>(
  kind: K,
  value: Condition
): value is Condition & { __kind: K } {
  return value.__kind === kind;
}
