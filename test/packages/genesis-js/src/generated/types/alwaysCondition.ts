/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Option,
  OptionOrNullable,
  isOption,
  none,
  some,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ConditionTypes,
  ConditionTypesArgs,
  getConditionTypesSerializer,
} from '.';

export type AlwaysCondition = {
  conditionType: ConditionTypes;
  padding: Array<number>;
  triggeredTimestamp: Option<bigint>;
};

export type AlwaysConditionArgs = {
  conditionType: ConditionTypesArgs;
  padding: Array<number>;
  triggeredTimestamp: OptionOrNullable<number | bigint>;
};

export function getAlwaysConditionSerializer(): Serializer<
  AlwaysConditionArgs,
  AlwaysCondition
> {
  return struct<AlwaysCondition>(
    [
      ['conditionType', getConditionTypesSerializer()],
      ['padding', array(u8(), { size: 55 })],
      [
        'triggeredTimestamp',
        (() => {
          const inner = i64();
          const sentinel = new Uint8Array([
            255, 255, 255, 255, 255, 255, 255, 127,
          ]);
          if (inner.fixedSize == null) {
            throw new Error(
              'Fixed-size options require an inner serializer with a fixed size.'
            );
          }
          if (inner.fixedSize !== sentinel.length) {
            throw new Error(
              'Fixed-size option sentinel length must match the inner serializer fixed size.'
            );
          }
          const { fixedSize } = inner;
          const normalize = (input: OptionOrNullable<number | bigint>) => {
            if (input == null) {
              return null;
            }
            if (isOption(input)) {
              return input.__option === 'None' ? null : input.value;
            }
            return input;
          };
          return {
            description: 'Option<bigint>',
            fixedSize,
            maxSize: fixedSize,
            serialize: (value: OptionOrNullable<number | bigint>) => {
              const normalized = normalize(value);
              return normalized == null
                ? sentinel.slice()
                : inner.serialize(normalized);
            },
            deserialize: (bytes: Uint8Array, offset = 0) => {
              const slice = bytes.slice(offset, offset + fixedSize);
              const isSentinel = slice.every((byte, i) => byte === sentinel[i]);
              if (isSentinel) {
                return [none<bigint>(), offset + fixedSize] as [
                  Option<bigint>,
                  number,
                ];
              }
              const [value, newOffset] = inner.deserialize(bytes, offset);
              return [some(value), newOffset] as [Option<bigint>, number];
            },
          } as Serializer<OptionOrNullable<number | bigint>, Option<bigint>>;
        })(),
      ],
    ],
    { description: 'AlwaysCondition' }
  ) as Serializer<AlwaysConditionArgs, AlwaysCondition>;
}
