/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type VaultDepositV1 = Account<VaultDepositV1AccountData>;

export type VaultDepositV1AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amount: bigint;
  consumedAmount: bigint;
};

export type VaultDepositV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amount: number | bigint;
  consumedAmount: number | bigint;
};

export function getVaultDepositV1AccountDataSerializer(): Serializer<
  VaultDepositV1AccountDataArgs,
  VaultDepositV1AccountData
> {
  return struct<VaultDepositV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['padding', array(u8(), { size: 2 })],
      ['index', u32()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amount', u64()],
      ['consumedAmount', u64()],
    ],
    { description: 'VaultDepositV1AccountData' }
  ) as Serializer<VaultDepositV1AccountDataArgs, VaultDepositV1AccountData>;
}

export function deserializeVaultDepositV1(
  rawAccount: RpcAccount
): VaultDepositV1 {
  return deserializeAccount(
    rawAccount,
    getVaultDepositV1AccountDataSerializer()
  );
}

export async function fetchVaultDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultDepositV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'VaultDepositV1');
  return deserializeVaultDepositV1(maybeAccount);
}

export async function safeFetchVaultDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultDepositV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeVaultDepositV1(maybeAccount) : null;
}

export async function fetchAllVaultDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'VaultDepositV1');
    return deserializeVaultDepositV1(maybeAccount);
  });
}

export async function safeFetchAllVaultDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeVaultDepositV1(maybeAccount as RpcAccount)
    );
}

export function getVaultDepositV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      index: number;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amount: number | bigint;
      consumedAmount: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 2 })],
      index: [4, u32()],
      depositor: [8, publicKeySerializer()],
      bucket: [40, publicKeySerializer()],
      timestamp: [72, i64()],
      amount: [80, u64()],
      consumedAmount: [88, u64()],
    })
    .deserializeUsing<VaultDepositV1>((account) =>
      deserializeVaultDepositV1(account)
    );
}

export function getVaultDepositV1Size(): number {
  return 96;
}
