/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  UnlockedBucketState,
  UnlockedBucketStateArgs,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getUnlockedBucketStateSerializer,
} from '../types';

export type UnlockedBucketV1 = Account<UnlockedBucketV1AccountData>;

export type UnlockedBucketV1AccountData = {
  key: Key;
  bump: number;
  state: UnlockedBucketState;
  index: number;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  solAllocationBps: number;
  genesis: PublicKey;
  tokenAllocation: bigint;
  solBalance: bigint;
  tokenBalance: bigint;
  start: bigint;
  recipient: PublicKey;
  backendSigner: PublicKey;
  fee: bigint;
  reserved: Array<number>;
};

export type UnlockedBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: UnlockedBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  solAllocationBps: number;
  genesis: PublicKey;
  tokenAllocation: number | bigint;
  solBalance: number | bigint;
  tokenBalance: number | bigint;
  start: number | bigint;
  recipient: PublicKey;
  backendSigner: PublicKey;
  fee: number | bigint;
  reserved: Array<number>;
};

export function getUnlockedBucketV1AccountDataSerializer(): Serializer<
  UnlockedBucketV1AccountDataArgs,
  UnlockedBucketV1AccountData
> {
  return struct<UnlockedBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getUnlockedBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 1 })],
      ['solAllocationBps', u16()],
      ['genesis', publicKeySerializer()],
      ['tokenAllocation', u64()],
      ['solBalance', u64()],
      ['tokenBalance', u64()],
      ['start', i64()],
      ['recipient', publicKeySerializer()],
      ['backendSigner', publicKeySerializer()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ],
    { description: 'UnlockedBucketV1AccountData' }
  ) as Serializer<UnlockedBucketV1AccountDataArgs, UnlockedBucketV1AccountData>;
}

export function deserializeUnlockedBucketV1(
  rawAccount: RpcAccount
): UnlockedBucketV1 {
  return deserializeAccount(
    rawAccount,
    getUnlockedBucketV1AccountDataSerializer()
  );
}

export async function fetchUnlockedBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UnlockedBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'UnlockedBucketV1');
  return deserializeUnlockedBucketV1(maybeAccount);
}

export async function safeFetchUnlockedBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UnlockedBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeUnlockedBucketV1(maybeAccount) : null;
}

export async function fetchAllUnlockedBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UnlockedBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'UnlockedBucketV1');
    return deserializeUnlockedBucketV1(maybeAccount);
  });
}

export async function safeFetchAllUnlockedBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UnlockedBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeUnlockedBucketV1(maybeAccount as RpcAccount)
    );
}

export function getUnlockedBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: UnlockedBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      padding: Array<number>;
      solAllocationBps: number;
      genesis: PublicKey;
      tokenAllocation: number | bigint;
      solBalance: number | bigint;
      tokenBalance: number | bigint;
      start: number | bigint;
      recipient: PublicKey;
      backendSigner: PublicKey;
      fee: number | bigint;
      reserved: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getUnlockedBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      padding: [5, array(u8(), { size: 1 })],
      solAllocationBps: [6, u16()],
      genesis: [8, publicKeySerializer()],
      tokenAllocation: [40, u64()],
      solBalance: [48, u64()],
      tokenBalance: [56, u64()],
      start: [64, i64()],
      recipient: [72, publicKeySerializer()],
      backendSigner: [104, publicKeySerializer()],
      fee: [136, u64()],
      reserved: [144, array(u8(), { size: 128 })],
    })
    .deserializeUsing<UnlockedBucketV1>((account) =>
      deserializeUnlockedBucketV1(account)
    );
}

export function getUnlockedBucketV1Size(): number {
  return 272;
}
