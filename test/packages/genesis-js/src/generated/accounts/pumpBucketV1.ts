/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  PumpBucketState,
  PumpBucketStateArgs,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getPumpBucketStateSerializer,
} from '../types';

export type PumpBucketV1 = Account<PumpBucketV1AccountData>;

export type PumpBucketV1AccountData = {
  key: Key;
  bump: number;
  state: PumpBucketState;
  index: number;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: bigint;
  solBalance: bigint;
  tokenBalance: bigint;
  start: bigint;
  backendSigner: PublicKey;
  fee: bigint;
  reserved: Array<number>;
};

export type PumpBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: PumpBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: number | bigint;
  solBalance: number | bigint;
  tokenBalance: number | bigint;
  start: number | bigint;
  backendSigner: PublicKey;
  fee: number | bigint;
  reserved: Array<number>;
};

export function getPumpBucketV1AccountDataSerializer(): Serializer<
  PumpBucketV1AccountDataArgs,
  PumpBucketV1AccountData
> {
  return struct<PumpBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getPumpBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 3 })],
      ['genesis', publicKeySerializer()],
      ['tokenAllocation', u64()],
      ['solBalance', u64()],
      ['tokenBalance', u64()],
      ['start', i64()],
      ['backendSigner', publicKeySerializer()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ],
    { description: 'PumpBucketV1AccountData' }
  ) as Serializer<PumpBucketV1AccountDataArgs, PumpBucketV1AccountData>;
}

export function deserializePumpBucketV1(rawAccount: RpcAccount): PumpBucketV1 {
  return deserializeAccount(rawAccount, getPumpBucketV1AccountDataSerializer());
}

export async function fetchPumpBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PumpBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'PumpBucketV1');
  return deserializePumpBucketV1(maybeAccount);
}

export async function safeFetchPumpBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PumpBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializePumpBucketV1(maybeAccount) : null;
}

export async function fetchAllPumpBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PumpBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'PumpBucketV1');
    return deserializePumpBucketV1(maybeAccount);
  });
}

export async function safeFetchAllPumpBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PumpBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializePumpBucketV1(maybeAccount as RpcAccount));
}

export function getPumpBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: PumpBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      padding: Array<number>;
      genesis: PublicKey;
      tokenAllocation: number | bigint;
      solBalance: number | bigint;
      tokenBalance: number | bigint;
      start: number | bigint;
      backendSigner: PublicKey;
      fee: number | bigint;
      reserved: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getPumpBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      padding: [5, array(u8(), { size: 3 })],
      genesis: [8, publicKeySerializer()],
      tokenAllocation: [40, u64()],
      solBalance: [48, u64()],
      tokenBalance: [56, u64()],
      start: [64, i64()],
      backendSigner: [72, publicKeySerializer()],
      fee: [104, u64()],
      reserved: [112, array(u8(), { size: 128 })],
    })
    .deserializeUsing<PumpBucketV1>((account) =>
      deserializePumpBucketV1(account)
    );
}

export function getPumpBucketV1Size(): number {
  return 240;
}
