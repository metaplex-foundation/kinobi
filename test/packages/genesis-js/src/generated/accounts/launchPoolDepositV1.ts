/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type LaunchPoolDepositV1 = Account<LaunchPoolDepositV1AccountData>;

export type LaunchPoolDepositV1AccountData = {
  key: Key;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amount: bigint;
  weightedSol: bigint;
};

export type LaunchPoolDepositV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amount: number | bigint;
  weightedSol: number | bigint;
};

export function getLaunchPoolDepositV1AccountDataSerializer(): Serializer<
  LaunchPoolDepositV1AccountDataArgs,
  LaunchPoolDepositV1AccountData
> {
  return struct<LaunchPoolDepositV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['claimed', bool()],
      ['padding', array(u8(), { size: 1 })],
      ['index', u32()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amount', u64()],
      ['weightedSol', u64()],
    ],
    { description: 'LaunchPoolDepositV1AccountData' }
  ) as Serializer<
    LaunchPoolDepositV1AccountDataArgs,
    LaunchPoolDepositV1AccountData
  >;
}

export function deserializeLaunchPoolDepositV1(
  rawAccount: RpcAccount
): LaunchPoolDepositV1 {
  return deserializeAccount(
    rawAccount,
    getLaunchPoolDepositV1AccountDataSerializer()
  );
}

export async function fetchLaunchPoolDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<LaunchPoolDepositV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'LaunchPoolDepositV1');
  return deserializeLaunchPoolDepositV1(maybeAccount);
}

export async function safeFetchLaunchPoolDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<LaunchPoolDepositV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeLaunchPoolDepositV1(maybeAccount)
    : null;
}

export async function fetchAllLaunchPoolDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<LaunchPoolDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'LaunchPoolDepositV1');
    return deserializeLaunchPoolDepositV1(maybeAccount);
  });
}

export async function safeFetchAllLaunchPoolDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<LaunchPoolDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeLaunchPoolDepositV1(maybeAccount as RpcAccount)
    );
}

export function getLaunchPoolDepositV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      claimed: boolean;
      padding: Array<number>;
      index: number;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amount: number | bigint;
      weightedSol: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      claimed: [2, bool()],
      padding: [3, array(u8(), { size: 1 })],
      index: [4, u32()],
      depositor: [8, publicKeySerializer()],
      bucket: [40, publicKeySerializer()],
      timestamp: [72, i64()],
      amount: [80, u64()],
      weightedSol: [88, u64()],
    })
    .deserializeUsing<LaunchPoolDepositV1>((account) =>
      deserializeLaunchPoolDepositV1(account)
    );
}

export function getLaunchPoolDepositV1Size(): number {
  return 96;
}
