/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Allocator, AllocatorArgs, getAllocatorSerializer } from '../types';

export type AuctionAVLTreeV1 = Account<AuctionAVLTreeV1AccountData>;

export type AuctionAVLTreeV1AccountData = {
  key: number;
  bump: number;
  padding: Array<number>;
  allocator: Allocator;
};

export type AuctionAVLTreeV1AccountDataArgs = {
  key: number;
  bump: number;
  padding: Array<number>;
  allocator: AllocatorArgs;
};

export function getAuctionAVLTreeV1AccountDataSerializer(): Serializer<
  AuctionAVLTreeV1AccountDataArgs,
  AuctionAVLTreeV1AccountData
> {
  return struct<AuctionAVLTreeV1AccountData>(
    [
      ['key', u8()],
      ['bump', u8()],
      ['padding', array(u8(), { size: 6 })],
      ['allocator', getAllocatorSerializer()],
    ],
    { description: 'AuctionAVLTreeV1AccountData' }
  ) as Serializer<AuctionAVLTreeV1AccountDataArgs, AuctionAVLTreeV1AccountData>;
}

export function deserializeAuctionAVLTreeV1(
  rawAccount: RpcAccount
): AuctionAVLTreeV1 {
  return deserializeAccount(
    rawAccount,
    getAuctionAVLTreeV1AccountDataSerializer()
  );
}

export async function fetchAuctionAVLTreeV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AuctionAVLTreeV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AuctionAVLTreeV1');
  return deserializeAuctionAVLTreeV1(maybeAccount);
}

export async function safeFetchAuctionAVLTreeV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AuctionAVLTreeV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeAuctionAVLTreeV1(maybeAccount) : null;
}

export async function fetchAllAuctionAVLTreeV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AuctionAVLTreeV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AuctionAVLTreeV1');
    return deserializeAuctionAVLTreeV1(maybeAccount);
  });
}

export async function safeFetchAllAuctionAVLTreeV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AuctionAVLTreeV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeAuctionAVLTreeV1(maybeAccount as RpcAccount)
    );
}

export function getAuctionAVLTreeV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: number;
      bump: number;
      padding: Array<number>;
      allocator: AllocatorArgs;
    }>({
      key: [0, u8()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 6 })],
      allocator: [8, getAllocatorSerializer()],
    })
    .registerNestedFieldsFromStruct<AllocatorArgs>('allocator', 8, [
      ['fields', array(u32(), { size: 8 })],
    ])
    .deserializeUsing<AuctionAVLTreeV1>((account) =>
      deserializeAuctionAVLTreeV1(account)
    );
}

export function getAuctionAVLTreeV1Size(): number {
  return 40;
}
