/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type VaultDepositV2 = Account<VaultDepositV2AccountData>;

export type VaultDepositV2AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amountQuoteToken: bigint;
  amountQuoteTokenConsumed: bigint;
};

export type VaultDepositV2AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amountQuoteToken: number | bigint;
  amountQuoteTokenConsumed: number | bigint;
};

export function getVaultDepositV2AccountDataSerializer(): Serializer<
  VaultDepositV2AccountDataArgs,
  VaultDepositV2AccountData
> {
  return struct<VaultDepositV2AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['padding', array(u8(), { size: 2 })],
      ['index', u32()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amountQuoteToken', u64()],
      ['amountQuoteTokenConsumed', u64()],
    ],
    { description: 'VaultDepositV2AccountData' }
  ) as Serializer<VaultDepositV2AccountDataArgs, VaultDepositV2AccountData>;
}

export function deserializeVaultDepositV2(
  rawAccount: RpcAccount
): VaultDepositV2 {
  return deserializeAccount(
    rawAccount,
    getVaultDepositV2AccountDataSerializer()
  );
}

export async function fetchVaultDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultDepositV2> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'VaultDepositV2');
  return deserializeVaultDepositV2(maybeAccount);
}

export async function safeFetchVaultDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultDepositV2 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeVaultDepositV2(maybeAccount) : null;
}

export async function fetchAllVaultDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'VaultDepositV2');
    return deserializeVaultDepositV2(maybeAccount);
  });
}

export async function safeFetchAllVaultDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeVaultDepositV2(maybeAccount as RpcAccount)
    );
}

export function getVaultDepositV2GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      index: number;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amountQuoteToken: number | bigint;
      amountQuoteTokenConsumed: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 2 })],
      index: [4, u32()],
      depositor: [8, publicKeySerializer()],
      bucket: [40, publicKeySerializer()],
      timestamp: [72, i64()],
      amountQuoteToken: [80, u64()],
      amountQuoteTokenConsumed: [88, u64()],
    })
    .deserializeUsing<VaultDepositV2>((account) =>
      deserializeVaultDepositV2(account)
    );
}

export function getVaultDepositV2Size(): number {
  return 96;
}
