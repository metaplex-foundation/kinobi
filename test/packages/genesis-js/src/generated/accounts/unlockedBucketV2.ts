/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  BucketBase,
  BucketBaseArgs,
  Condition,
  ConditionArgs,
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  UnlockedV2Extensions,
  UnlockedV2ExtensionsArgs,
  getBucketBaseSerializer,
  getConditionSerializer,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getUnlockedV2ExtensionsSerializer,
} from '../types';

export type UnlockedBucketV2 = Account<UnlockedBucketV2AccountData>;

export type UnlockedBucketV2AccountData = {
  key: Key;
  claimed: boolean;
  claimFeeType: FeeDiscriminants;
  padding: Array<number>;
  claimFee: bigint;
  bucket: BucketBase;
  recipient: PublicKey;
  claimStartCondition: Condition;
  claimEndCondition: Condition;
  reserved: Array<number>;
  extensions: UnlockedV2Extensions;
};

export type UnlockedBucketV2AccountDataArgs = {
  key: KeyArgs;
  claimed: boolean;
  claimFeeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  claimFee: number | bigint;
  bucket: BucketBaseArgs;
  recipient: PublicKey;
  claimStartCondition: ConditionArgs;
  claimEndCondition: ConditionArgs;
  reserved: Array<number>;
  extensions: UnlockedV2ExtensionsArgs;
};

export function getUnlockedBucketV2AccountDataSerializer(): Serializer<
  UnlockedBucketV2AccountDataArgs,
  UnlockedBucketV2AccountData
> {
  return struct<UnlockedBucketV2AccountData>(
    [
      ['key', getKeySerializer()],
      ['claimed', bool()],
      ['claimFeeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 5 })],
      ['claimFee', u64()],
      ['bucket', getBucketBaseSerializer()],
      ['recipient', publicKeySerializer()],
      ['claimStartCondition', getConditionSerializer()],
      ['claimEndCondition', getConditionSerializer()],
      ['reserved', array(u8(), { size: 128 })],
      ['extensions', getUnlockedV2ExtensionsSerializer()],
    ],
    { description: 'UnlockedBucketV2AccountData' }
  ) as Serializer<UnlockedBucketV2AccountDataArgs, UnlockedBucketV2AccountData>;
}

export function deserializeUnlockedBucketV2(
  rawAccount: RpcAccount
): UnlockedBucketV2 {
  return deserializeAccount(
    rawAccount,
    getUnlockedBucketV2AccountDataSerializer()
  );
}

export async function fetchUnlockedBucketV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UnlockedBucketV2> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'UnlockedBucketV2');
  return deserializeUnlockedBucketV2(maybeAccount);
}

export async function safeFetchUnlockedBucketV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UnlockedBucketV2 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeUnlockedBucketV2(maybeAccount) : null;
}

export async function fetchAllUnlockedBucketV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UnlockedBucketV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'UnlockedBucketV2');
    return deserializeUnlockedBucketV2(maybeAccount);
  });
}

export async function safeFetchAllUnlockedBucketV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UnlockedBucketV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeUnlockedBucketV2(maybeAccount as RpcAccount)
    );
}

export function getUnlockedBucketV2GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      claimed: boolean;
      claimFeeType: FeeDiscriminantsArgs;
      padding: Array<number>;
      claimFee: number | bigint;
      bucket: BucketBaseArgs;
      recipient: PublicKey;
      claimStartCondition: ConditionArgs;
      claimEndCondition: ConditionArgs;
      reserved: Array<number>;
      extensions: UnlockedV2ExtensionsArgs;
    }>({
      key: [0, getKeySerializer()],
      claimed: [1, bool()],
      claimFeeType: [2, getFeeDiscriminantsSerializer()],
      padding: [3, array(u8(), { size: 5 })],
      claimFee: [8, u64()],
      bucket: [16, getBucketBaseSerializer()],
      recipient: [216, publicKeySerializer()],
      claimStartCondition: [248, getConditionSerializer()],
      claimEndCondition: [312, getConditionSerializer()],
      reserved: [376, array(u8(), { size: 128 })],
      extensions: [504, getUnlockedV2ExtensionsSerializer()],
    })
    .registerNestedFieldsFromStruct<BucketBaseArgs>('bucket', 16, [
      ['bump', u8()],
      ['bucketIndex', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 5 })],
      ['genesis', publicKeySerializer()],
      ['baseTokenAllocation', u64()],
      ['baseTokenBalance', u64()],
      ['quoteTokenBalance', u64()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ])
    .registerNestedFieldsFromStruct<UnlockedV2ExtensionsArgs>(
      'extensions',
      504,
      [
        ['reserved', array(u8(), { size: 8 })],
        ['backendSigner', publicKeySerializer()],
        ['padding', array(u8(), { size: 128 })],
      ]
    )
    .deserializeUsing<UnlockedBucketV2>((account) =>
      deserializeUnlockedBucketV2(account)
    );
}

export function getUnlockedBucketV2Size(): number {
  return 672;
}
