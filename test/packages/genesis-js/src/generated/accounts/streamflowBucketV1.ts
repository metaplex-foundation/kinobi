/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  bytes,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  StreamflowBucketState,
  StreamflowBucketStateArgs,
  StreamflowConfig,
  StreamflowConfigArgs,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getStreamflowBucketStateSerializer,
  getStreamflowConfigSerializer,
} from '../types';

export type StreamflowBucketV1 = Account<StreamflowBucketV1AccountData>;

export type StreamflowBucketV1AccountData = {
  key: Key;
  bump: number;
  state: StreamflowBucketState;
  index: number;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: bigint;
  solBalance: bigint;
  tokenBalance: bigint;
  recipient: PublicKey;
  config: StreamflowConfig;
  backendSigner: PublicKey;
  fee: bigint;
  reserved: Array<number>;
};

export type StreamflowBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: StreamflowBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: number | bigint;
  solBalance: number | bigint;
  tokenBalance: number | bigint;
  recipient: PublicKey;
  config: StreamflowConfigArgs;
  backendSigner: PublicKey;
  fee: number | bigint;
  reserved: Array<number>;
};

export function getStreamflowBucketV1AccountDataSerializer(): Serializer<
  StreamflowBucketV1AccountDataArgs,
  StreamflowBucketV1AccountData
> {
  return struct<StreamflowBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getStreamflowBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 3 })],
      ['genesis', publicKeySerializer()],
      ['tokenAllocation', u64()],
      ['solBalance', u64()],
      ['tokenBalance', u64()],
      ['recipient', publicKeySerializer()],
      ['config', getStreamflowConfigSerializer()],
      ['backendSigner', publicKeySerializer()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ],
    { description: 'StreamflowBucketV1AccountData' }
  ) as Serializer<
    StreamflowBucketV1AccountDataArgs,
    StreamflowBucketV1AccountData
  >;
}

export function deserializeStreamflowBucketV1(
  rawAccount: RpcAccount
): StreamflowBucketV1 {
  return deserializeAccount(
    rawAccount,
    getStreamflowBucketV1AccountDataSerializer()
  );
}

export async function fetchStreamflowBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<StreamflowBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'StreamflowBucketV1');
  return deserializeStreamflowBucketV1(maybeAccount);
}

export async function safeFetchStreamflowBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<StreamflowBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeStreamflowBucketV1(maybeAccount)
    : null;
}

export async function fetchAllStreamflowBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<StreamflowBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'StreamflowBucketV1');
    return deserializeStreamflowBucketV1(maybeAccount);
  });
}

export async function safeFetchAllStreamflowBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<StreamflowBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeStreamflowBucketV1(maybeAccount as RpcAccount)
    );
}

export function getStreamflowBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: StreamflowBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      padding: Array<number>;
      genesis: PublicKey;
      tokenAllocation: number | bigint;
      solBalance: number | bigint;
      tokenBalance: number | bigint;
      recipient: PublicKey;
      config: StreamflowConfigArgs;
      backendSigner: PublicKey;
      fee: number | bigint;
      reserved: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getStreamflowBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      padding: [5, array(u8(), { size: 3 })],
      genesis: [8, publicKeySerializer()],
      tokenAllocation: [40, u64()],
      solBalance: [48, u64()],
      tokenBalance: [56, u64()],
      recipient: [64, publicKeySerializer()],
      config: [96, getStreamflowConfigSerializer()],
      backendSigner: [216, publicKeySerializer()],
      fee: [248, u64()],
      reserved: [256, array(u8(), { size: 128 })],
    })
    .registerNestedFieldsFromStruct<StreamflowConfigArgs>('config', 96, [
      ['startTime', u64()],
      ['period', u64()],
      ['amountPerPeriod', u64()],
      ['cliff', u64()],
      ['cliffAmount', u64()],
      ['streamName', bytes({ size: 64 })],
      ['withdrawFrequency', u64()],
      ['cancelableBySender', bool()],
      ['cancelableByRecipient', bool()],
      ['automaticWithdrawal', bool()],
      ['transferableBySender', bool()],
      ['transferableByRecipient', bool()],
      ['canTopup', bool()],
      ['pausable', bool()],
      ['canUpdateRate', bool()],
    ])
    .deserializeUsing<StreamflowBucketV1>((account) =>
      deserializeStreamflowBucketV1(account)
    );
}

export function getStreamflowBucketV1Size(): number {
  return 384;
}
