/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type AuctionBidV1 = Account<AuctionBidV1AccountData>;

export type AuctionBidV1AccountData = {
  key: Key;
  bump: number;
  index: number;
  padding: Array<number>;
  bucket: PublicKey;
  bidder: PublicKey;
  priceUlamports: bigint;
  tokenQuantity: bigint;
  solAmount: bigint;
  timestamp: bigint;
  bidNumber: bigint;
  tokensClaimed: boolean;
  refundClaimed: boolean;
  padding2: Array<number>;
};

export type AuctionBidV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  index: number;
  padding: Array<number>;
  bucket: PublicKey;
  bidder: PublicKey;
  priceUlamports: number | bigint;
  tokenQuantity: number | bigint;
  solAmount: number | bigint;
  timestamp: number | bigint;
  bidNumber: number | bigint;
  tokensClaimed: boolean;
  refundClaimed: boolean;
  padding2: Array<number>;
};

export function getAuctionBidV1AccountDataSerializer(): Serializer<
  AuctionBidV1AccountDataArgs,
  AuctionBidV1AccountData
> {
  return struct<AuctionBidV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['index', u16()],
      ['padding', array(u8(), { size: 4 })],
      ['bucket', publicKeySerializer()],
      ['bidder', publicKeySerializer()],
      ['priceUlamports', u64()],
      ['tokenQuantity', u64()],
      ['solAmount', u64()],
      ['timestamp', i64()],
      ['bidNumber', u64()],
      ['tokensClaimed', bool()],
      ['refundClaimed', bool()],
      ['padding2', array(u8(), { size: 6 })],
    ],
    { description: 'AuctionBidV1AccountData' }
  ) as Serializer<AuctionBidV1AccountDataArgs, AuctionBidV1AccountData>;
}

export function deserializeAuctionBidV1(rawAccount: RpcAccount): AuctionBidV1 {
  return deserializeAccount(rawAccount, getAuctionBidV1AccountDataSerializer());
}

export async function fetchAuctionBidV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AuctionBidV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AuctionBidV1');
  return deserializeAuctionBidV1(maybeAccount);
}

export async function safeFetchAuctionBidV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AuctionBidV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeAuctionBidV1(maybeAccount) : null;
}

export async function fetchAllAuctionBidV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AuctionBidV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AuctionBidV1');
    return deserializeAuctionBidV1(maybeAccount);
  });
}

export async function safeFetchAllAuctionBidV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AuctionBidV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeAuctionBidV1(maybeAccount as RpcAccount));
}

export function getAuctionBidV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      index: number;
      padding: Array<number>;
      bucket: PublicKey;
      bidder: PublicKey;
      priceUlamports: number | bigint;
      tokenQuantity: number | bigint;
      solAmount: number | bigint;
      timestamp: number | bigint;
      bidNumber: number | bigint;
      tokensClaimed: boolean;
      refundClaimed: boolean;
      padding2: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      index: [2, u16()],
      padding: [4, array(u8(), { size: 4 })],
      bucket: [8, publicKeySerializer()],
      bidder: [40, publicKeySerializer()],
      priceUlamports: [72, u64()],
      tokenQuantity: [80, u64()],
      solAmount: [88, u64()],
      timestamp: [96, i64()],
      bidNumber: [104, u64()],
      tokensClaimed: [112, bool()],
      refundClaimed: [113, bool()],
      padding2: [114, array(u8(), { size: 6 })],
    })
    .deserializeUsing<AuctionBidV1>((account) =>
      deserializeAuctionBidV1(account)
    );
}

export function getAuctionBidV1Size(): number {
  return 120;
}
