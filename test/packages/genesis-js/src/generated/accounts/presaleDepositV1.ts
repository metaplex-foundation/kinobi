/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type PresaleDepositV1 = Account<PresaleDepositV1AccountData>;

export type PresaleDepositV1AccountData = {
  key: Key;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amount: bigint;
};

export type PresaleDepositV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amount: number | bigint;
};

export function getPresaleDepositV1AccountDataSerializer(): Serializer<
  PresaleDepositV1AccountDataArgs,
  PresaleDepositV1AccountData
> {
  return struct<PresaleDepositV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['claimed', bool()],
      ['padding', array(u8(), { size: 1 })],
      ['index', u32()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amount', u64()],
    ],
    { description: 'PresaleDepositV1AccountData' }
  ) as Serializer<PresaleDepositV1AccountDataArgs, PresaleDepositV1AccountData>;
}

export function deserializePresaleDepositV1(
  rawAccount: RpcAccount
): PresaleDepositV1 {
  return deserializeAccount(
    rawAccount,
    getPresaleDepositV1AccountDataSerializer()
  );
}

export async function fetchPresaleDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PresaleDepositV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'PresaleDepositV1');
  return deserializePresaleDepositV1(maybeAccount);
}

export async function safeFetchPresaleDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PresaleDepositV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializePresaleDepositV1(maybeAccount) : null;
}

export async function fetchAllPresaleDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PresaleDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'PresaleDepositV1');
    return deserializePresaleDepositV1(maybeAccount);
  });
}

export async function safeFetchAllPresaleDepositV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PresaleDepositV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializePresaleDepositV1(maybeAccount as RpcAccount)
    );
}

export function getPresaleDepositV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      claimed: boolean;
      padding: Array<number>;
      index: number;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amount: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      claimed: [2, bool()],
      padding: [3, array(u8(), { size: 1 })],
      index: [4, u32()],
      depositor: [8, publicKeySerializer()],
      bucket: [40, publicKeySerializer()],
      timestamp: [72, i64()],
      amount: [80, u64()],
    })
    .deserializeUsing<PresaleDepositV1>((account) =>
      deserializePresaleDepositV1(account)
    );
}

export function getPresaleDepositV1Size(): number {
  return 88;
}
