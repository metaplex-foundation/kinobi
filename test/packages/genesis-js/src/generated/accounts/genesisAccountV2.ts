/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type GenesisAccountV2 = Account<GenesisAccountV2AccountData>;

export type GenesisAccountV2AccountData = {
  key: Key;
  bump: number;
  index: number;
  finalized: boolean;
  padding: Array<number>;
  authority: PublicKey;
  baseMint: PublicKey;
  quoteMint: PublicKey;
  totalSupplyBaseToken: bigint;
  totalAllocatedSupplyBaseToken: bigint;
  totalProceedsQuoteToken: bigint;
  fundingMode: number;
  launchType: number;
  endPadding: Array<number>;
  bucketCount: number;
};

export type GenesisAccountV2AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  index: number;
  finalized: boolean;
  padding: Array<number>;
  authority: PublicKey;
  baseMint: PublicKey;
  quoteMint: PublicKey;
  totalSupplyBaseToken: number | bigint;
  totalAllocatedSupplyBaseToken: number | bigint;
  totalProceedsQuoteToken: number | bigint;
  fundingMode: number;
  launchType: number;
  endPadding: Array<number>;
  bucketCount: number;
};

export function getGenesisAccountV2AccountDataSerializer(): Serializer<
  GenesisAccountV2AccountDataArgs,
  GenesisAccountV2AccountData
> {
  return struct<GenesisAccountV2AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['index', u16()],
      ['finalized', bool()],
      ['padding', array(u8(), { size: 3 })],
      ['authority', publicKeySerializer()],
      ['baseMint', publicKeySerializer()],
      ['quoteMint', publicKeySerializer()],
      ['totalSupplyBaseToken', u64()],
      ['totalAllocatedSupplyBaseToken', u64()],
      ['totalProceedsQuoteToken', u64()],
      ['fundingMode', u8()],
      ['launchType', u8()],
      ['endPadding', array(u8(), { size: 2 })],
      ['bucketCount', u32()],
    ],
    { description: 'GenesisAccountV2AccountData' }
  ) as Serializer<GenesisAccountV2AccountDataArgs, GenesisAccountV2AccountData>;
}

export function deserializeGenesisAccountV2(
  rawAccount: RpcAccount
): GenesisAccountV2 {
  return deserializeAccount(
    rawAccount,
    getGenesisAccountV2AccountDataSerializer()
  );
}

export async function fetchGenesisAccountV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<GenesisAccountV2> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'GenesisAccountV2');
  return deserializeGenesisAccountV2(maybeAccount);
}

export async function safeFetchGenesisAccountV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<GenesisAccountV2 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeGenesisAccountV2(maybeAccount) : null;
}

export async function fetchAllGenesisAccountV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<GenesisAccountV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'GenesisAccountV2');
    return deserializeGenesisAccountV2(maybeAccount);
  });
}

export async function safeFetchAllGenesisAccountV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<GenesisAccountV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeGenesisAccountV2(maybeAccount as RpcAccount)
    );
}

export function getGenesisAccountV2GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      index: number;
      finalized: boolean;
      padding: Array<number>;
      authority: PublicKey;
      baseMint: PublicKey;
      quoteMint: PublicKey;
      totalSupplyBaseToken: number | bigint;
      totalAllocatedSupplyBaseToken: number | bigint;
      totalProceedsQuoteToken: number | bigint;
      fundingMode: number;
      launchType: number;
      endPadding: Array<number>;
      bucketCount: number;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      index: [2, u16()],
      finalized: [4, bool()],
      padding: [5, array(u8(), { size: 3 })],
      authority: [8, publicKeySerializer()],
      baseMint: [40, publicKeySerializer()],
      quoteMint: [72, publicKeySerializer()],
      totalSupplyBaseToken: [104, u64()],
      totalAllocatedSupplyBaseToken: [112, u64()],
      totalProceedsQuoteToken: [120, u64()],
      fundingMode: [128, u8()],
      launchType: [129, u8()],
      endPadding: [130, array(u8(), { size: 2 })],
      bucketCount: [132, u32()],
    })
    .deserializeUsing<GenesisAccountV2>((account) =>
      deserializeGenesisAccountV2(account)
    );
}

export function getGenesisAccountV2Size(): number {
  return 136;
}
