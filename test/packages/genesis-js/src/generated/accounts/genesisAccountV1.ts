/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  BucketEntry,
  BucketEntryArgs,
  Key,
  KeyArgs,
  getBucketEntrySerializer,
  getKeySerializer,
} from '../types';

export type GenesisAccountV1 = Account<GenesisAccountV1AccountData>;

export type GenesisAccountV1AccountData = {
  key: Key;
  bump: number;
  finalized: boolean;
  padding: Array<number>;
  authority: PublicKey;
  mint: PublicKey;
  totalSupply: bigint;
  totalProceeds: bigint;
  endPadding: Array<number>;
  buckets: Array<BucketEntry>;
};

export type GenesisAccountV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  finalized: boolean;
  padding: Array<number>;
  authority: PublicKey;
  mint: PublicKey;
  totalSupply: number | bigint;
  totalProceeds: number | bigint;
  endPadding: Array<number>;
  buckets: Array<BucketEntryArgs>;
};

export function getGenesisAccountV1AccountDataSerializer(): Serializer<
  GenesisAccountV1AccountDataArgs,
  GenesisAccountV1AccountData
> {
  return struct<GenesisAccountV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['finalized', bool()],
      ['padding', array(u8(), { size: 5 })],
      ['authority', publicKeySerializer()],
      ['mint', publicKeySerializer()],
      ['totalSupply', u64()],
      ['totalProceeds', u64()],
      ['endPadding', array(u8(), { size: 4 })],
      ['buckets', array(getBucketEntrySerializer())],
    ],
    { description: 'GenesisAccountV1AccountData' }
  ) as Serializer<GenesisAccountV1AccountDataArgs, GenesisAccountV1AccountData>;
}

export function deserializeGenesisAccountV1(
  rawAccount: RpcAccount
): GenesisAccountV1 {
  return deserializeAccount(
    rawAccount,
    getGenesisAccountV1AccountDataSerializer()
  );
}

export async function fetchGenesisAccountV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<GenesisAccountV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'GenesisAccountV1');
  return deserializeGenesisAccountV1(maybeAccount);
}

export async function safeFetchGenesisAccountV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<GenesisAccountV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeGenesisAccountV1(maybeAccount) : null;
}

export async function fetchAllGenesisAccountV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<GenesisAccountV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'GenesisAccountV1');
    return deserializeGenesisAccountV1(maybeAccount);
  });
}

export async function safeFetchAllGenesisAccountV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<GenesisAccountV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeGenesisAccountV1(maybeAccount as RpcAccount)
    );
}

export function getGenesisAccountV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      finalized: boolean;
      padding: Array<number>;
      authority: PublicKey;
      mint: PublicKey;
      totalSupply: number | bigint;
      totalProceeds: number | bigint;
      endPadding: Array<number>;
      buckets: Array<BucketEntryArgs>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      finalized: [2, bool()],
      padding: [3, array(u8(), { size: 5 })],
      authority: [8, publicKeySerializer()],
      mint: [40, publicKeySerializer()],
      totalSupply: [72, u64()],
      totalProceeds: [80, u64()],
      endPadding: [88, array(u8(), { size: 4 })],
      buckets: [92, array(getBucketEntrySerializer())],
    })
    .deserializeUsing<GenesisAccountV1>((account) =>
      deserializeGenesisAccountV1(account)
    );
}
