/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  RaydiumCpmmBucketState,
  RaydiumCpmmBucketStateArgs,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getRaydiumCpmmBucketStateSerializer,
} from '../types';

export type RaydiumCpmmBucketV1 = Account<RaydiumCpmmBucketV1AccountData>;

export type RaydiumCpmmBucketV1AccountData = {
  key: Key;
  bump: number;
  state: RaydiumCpmmBucketState;
  index: number;
  feeType: FeeDiscriminants;
  bucketSignerBump: number;
  solAllocationBps: number;
  genesis: PublicKey;
  bucketSigner: PublicKey;
  tokenAllocation: bigint;
  solInSigner: bigint;
  tokenBalance: bigint;
  start: bigint;
  backendSigner: PublicKey;
  fee: bigint;
  reserved: Array<number>;
};

export type RaydiumCpmmBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: RaydiumCpmmBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  bucketSignerBump: number;
  solAllocationBps: number;
  genesis: PublicKey;
  bucketSigner: PublicKey;
  tokenAllocation: number | bigint;
  solInSigner: number | bigint;
  tokenBalance: number | bigint;
  start: number | bigint;
  backendSigner: PublicKey;
  fee: number | bigint;
  reserved: Array<number>;
};

export function getRaydiumCpmmBucketV1AccountDataSerializer(): Serializer<
  RaydiumCpmmBucketV1AccountDataArgs,
  RaydiumCpmmBucketV1AccountData
> {
  return struct<RaydiumCpmmBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getRaydiumCpmmBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['bucketSignerBump', u8()],
      ['solAllocationBps', u16()],
      ['genesis', publicKeySerializer()],
      ['bucketSigner', publicKeySerializer()],
      ['tokenAllocation', u64()],
      ['solInSigner', u64()],
      ['tokenBalance', u64()],
      ['start', i64()],
      ['backendSigner', publicKeySerializer()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ],
    { description: 'RaydiumCpmmBucketV1AccountData' }
  ) as Serializer<
    RaydiumCpmmBucketV1AccountDataArgs,
    RaydiumCpmmBucketV1AccountData
  >;
}

export function deserializeRaydiumCpmmBucketV1(
  rawAccount: RpcAccount
): RaydiumCpmmBucketV1 {
  return deserializeAccount(
    rawAccount,
    getRaydiumCpmmBucketV1AccountDataSerializer()
  );
}

export async function fetchRaydiumCpmmBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<RaydiumCpmmBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'RaydiumCpmmBucketV1');
  return deserializeRaydiumCpmmBucketV1(maybeAccount);
}

export async function safeFetchRaydiumCpmmBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<RaydiumCpmmBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeRaydiumCpmmBucketV1(maybeAccount)
    : null;
}

export async function fetchAllRaydiumCpmmBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<RaydiumCpmmBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'RaydiumCpmmBucketV1');
    return deserializeRaydiumCpmmBucketV1(maybeAccount);
  });
}

export async function safeFetchAllRaydiumCpmmBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<RaydiumCpmmBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeRaydiumCpmmBucketV1(maybeAccount as RpcAccount)
    );
}

export function getRaydiumCpmmBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: RaydiumCpmmBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      bucketSignerBump: number;
      solAllocationBps: number;
      genesis: PublicKey;
      bucketSigner: PublicKey;
      tokenAllocation: number | bigint;
      solInSigner: number | bigint;
      tokenBalance: number | bigint;
      start: number | bigint;
      backendSigner: PublicKey;
      fee: number | bigint;
      reserved: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getRaydiumCpmmBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      bucketSignerBump: [5, u8()],
      solAllocationBps: [6, u16()],
      genesis: [8, publicKeySerializer()],
      bucketSigner: [40, publicKeySerializer()],
      tokenAllocation: [72, u64()],
      solInSigner: [80, u64()],
      tokenBalance: [88, u64()],
      start: [96, i64()],
      backendSigner: [104, publicKeySerializer()],
      fee: [136, u64()],
      reserved: [144, array(u8(), { size: 128 })],
    })
    .deserializeUsing<RaydiumCpmmBucketV1>((account) =>
      deserializeRaydiumCpmmBucketV1(account)
    );
}

export function getRaydiumCpmmBucketV1Size(): number {
  return 272;
}
