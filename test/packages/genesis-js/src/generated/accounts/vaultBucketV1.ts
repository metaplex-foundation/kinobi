/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  VaultBucketState,
  VaultBucketStateArgs,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
  getVaultBucketStateSerializer,
} from '../types';

export type VaultBucketV1 = Account<VaultBucketV1AccountData>;

export type VaultBucketV1AccountData = {
  key: Key;
  bump: number;
  state: VaultBucketState;
  index: number;
  feeType: FeeDiscriminants;
  depositFeeType: FeeDiscriminants;
  withdrawFeeType: FeeDiscriminants;
  allowWithdrawals: boolean;
  genesis: PublicKey;
  quoteDepositTotal: bigint;
  depositCount: bigint;
  consumedCount: bigint;
  start: bigint;
  end: bigint;
  fee: bigint;
  depositFee: bigint;
  withdrawFee: bigint;
  backendSigner: PublicKey;
  actionsAuthority: PublicKey;
  nextSwapToBondingCurveOrdinal: bigint;
};

export type VaultBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: VaultBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  depositFeeType: FeeDiscriminantsArgs;
  withdrawFeeType: FeeDiscriminantsArgs;
  allowWithdrawals: boolean;
  genesis: PublicKey;
  quoteDepositTotal: number | bigint;
  depositCount: number | bigint;
  consumedCount: number | bigint;
  start: number | bigint;
  end: number | bigint;
  fee: number | bigint;
  depositFee: number | bigint;
  withdrawFee: number | bigint;
  backendSigner: PublicKey;
  actionsAuthority: PublicKey;
  nextSwapToBondingCurveOrdinal: number | bigint;
};

export function getVaultBucketV1AccountDataSerializer(): Serializer<
  VaultBucketV1AccountDataArgs,
  VaultBucketV1AccountData
> {
  return struct<VaultBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getVaultBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['depositFeeType', getFeeDiscriminantsSerializer()],
      ['withdrawFeeType', getFeeDiscriminantsSerializer()],
      ['allowWithdrawals', bool()],
      ['genesis', publicKeySerializer()],
      ['quoteDepositTotal', u64()],
      ['depositCount', u64()],
      ['consumedCount', u64()],
      ['start', i64()],
      ['end', i64()],
      ['fee', u64()],
      ['depositFee', u64()],
      ['withdrawFee', u64()],
      ['backendSigner', publicKeySerializer()],
      ['actionsAuthority', publicKeySerializer()],
      ['nextSwapToBondingCurveOrdinal', u64()],
    ],
    { description: 'VaultBucketV1AccountData' }
  ) as Serializer<VaultBucketV1AccountDataArgs, VaultBucketV1AccountData>;
}

export function deserializeVaultBucketV1(
  rawAccount: RpcAccount
): VaultBucketV1 {
  return deserializeAccount(
    rawAccount,
    getVaultBucketV1AccountDataSerializer()
  );
}

export async function fetchVaultBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'VaultBucketV1');
  return deserializeVaultBucketV1(maybeAccount);
}

export async function safeFetchVaultBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<VaultBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeVaultBucketV1(maybeAccount) : null;
}

export async function fetchAllVaultBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'VaultBucketV1');
    return deserializeVaultBucketV1(maybeAccount);
  });
}

export async function safeFetchAllVaultBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<VaultBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeVaultBucketV1(maybeAccount as RpcAccount)
    );
}

export function getVaultBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: VaultBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      depositFeeType: FeeDiscriminantsArgs;
      withdrawFeeType: FeeDiscriminantsArgs;
      allowWithdrawals: boolean;
      genesis: PublicKey;
      quoteDepositTotal: number | bigint;
      depositCount: number | bigint;
      consumedCount: number | bigint;
      start: number | bigint;
      end: number | bigint;
      fee: number | bigint;
      depositFee: number | bigint;
      withdrawFee: number | bigint;
      backendSigner: PublicKey;
      actionsAuthority: PublicKey;
      nextSwapToBondingCurveOrdinal: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getVaultBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      depositFeeType: [5, getFeeDiscriminantsSerializer()],
      withdrawFeeType: [6, getFeeDiscriminantsSerializer()],
      allowWithdrawals: [7, bool()],
      genesis: [8, publicKeySerializer()],
      quoteDepositTotal: [40, u64()],
      depositCount: [48, u64()],
      consumedCount: [56, u64()],
      start: [64, i64()],
      end: [72, i64()],
      fee: [80, u64()],
      depositFee: [88, u64()],
      withdrawFee: [96, u64()],
      backendSigner: [104, publicKeySerializer()],
      actionsAuthority: [136, publicKeySerializer()],
      nextSwapToBondingCurveOrdinal: [168, u64()],
    })
    .deserializeUsing<VaultBucketV1>((account) =>
      deserializeVaultBucketV1(account)
    );
}

export function getVaultBucketV1Size(): number {
  return 176;
}
