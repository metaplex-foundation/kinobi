/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type LaunchPoolDepositV2 = Account<LaunchPoolDepositV2AccountData>;

export type LaunchPoolDepositV2AccountData = {
  key: Key;
  bump: number;
  claimed: boolean;
  refunded: boolean;
  index: number;
  amountClaimed: bigint;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amountQuoteToken: bigint;
  weightedQuoteToken: bigint;
};

export type LaunchPoolDepositV2AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  claimed: boolean;
  refunded: boolean;
  index: number;
  amountClaimed: number | bigint;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amountQuoteToken: number | bigint;
  weightedQuoteToken: number | bigint;
};

export function getLaunchPoolDepositV2AccountDataSerializer(): Serializer<
  LaunchPoolDepositV2AccountDataArgs,
  LaunchPoolDepositV2AccountData
> {
  return struct<LaunchPoolDepositV2AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['claimed', bool()],
      ['refunded', bool()],
      ['index', u32()],
      ['amountClaimed', u64()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amountQuoteToken', u64()],
      ['weightedQuoteToken', u64()],
    ],
    { description: 'LaunchPoolDepositV2AccountData' }
  ) as Serializer<
    LaunchPoolDepositV2AccountDataArgs,
    LaunchPoolDepositV2AccountData
  >;
}

export function deserializeLaunchPoolDepositV2(
  rawAccount: RpcAccount
): LaunchPoolDepositV2 {
  return deserializeAccount(
    rawAccount,
    getLaunchPoolDepositV2AccountDataSerializer()
  );
}

export async function fetchLaunchPoolDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<LaunchPoolDepositV2> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'LaunchPoolDepositV2');
  return deserializeLaunchPoolDepositV2(maybeAccount);
}

export async function safeFetchLaunchPoolDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<LaunchPoolDepositV2 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeLaunchPoolDepositV2(maybeAccount)
    : null;
}

export async function fetchAllLaunchPoolDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<LaunchPoolDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'LaunchPoolDepositV2');
    return deserializeLaunchPoolDepositV2(maybeAccount);
  });
}

export async function safeFetchAllLaunchPoolDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<LaunchPoolDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeLaunchPoolDepositV2(maybeAccount as RpcAccount)
    );
}

export function getLaunchPoolDepositV2GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      claimed: boolean;
      refunded: boolean;
      index: number;
      amountClaimed: number | bigint;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amountQuoteToken: number | bigint;
      weightedQuoteToken: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      claimed: [2, bool()],
      refunded: [3, bool()],
      index: [4, u32()],
      amountClaimed: [8, u64()],
      depositor: [16, publicKeySerializer()],
      bucket: [48, publicKeySerializer()],
      timestamp: [80, i64()],
      amountQuoteToken: [88, u64()],
      weightedQuoteToken: [96, u64()],
    })
    .deserializeUsing<LaunchPoolDepositV2>((account) =>
      deserializeLaunchPoolDepositV2(account)
    );
}

export function getLaunchPoolDepositV2Size(): number {
  return 104;
}
