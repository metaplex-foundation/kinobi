/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type PresaleDepositV2 = Account<PresaleDepositV2AccountData>;

export type PresaleDepositV2AccountData = {
  key: Key;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  amountClaimed: bigint;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: bigint;
  amountQuoteToken: bigint;
};

export type PresaleDepositV2AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  claimed: boolean;
  padding: Array<number>;
  index: number;
  amountClaimed: number | bigint;
  depositor: PublicKey;
  bucket: PublicKey;
  timestamp: number | bigint;
  amountQuoteToken: number | bigint;
};

export function getPresaleDepositV2AccountDataSerializer(): Serializer<
  PresaleDepositV2AccountDataArgs,
  PresaleDepositV2AccountData
> {
  return struct<PresaleDepositV2AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['claimed', bool()],
      ['padding', array(u8(), { size: 1 })],
      ['index', u32()],
      ['amountClaimed', u64()],
      ['depositor', publicKeySerializer()],
      ['bucket', publicKeySerializer()],
      ['timestamp', i64()],
      ['amountQuoteToken', u64()],
    ],
    { description: 'PresaleDepositV2AccountData' }
  ) as Serializer<PresaleDepositV2AccountDataArgs, PresaleDepositV2AccountData>;
}

export function deserializePresaleDepositV2(
  rawAccount: RpcAccount
): PresaleDepositV2 {
  return deserializeAccount(
    rawAccount,
    getPresaleDepositV2AccountDataSerializer()
  );
}

export async function fetchPresaleDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PresaleDepositV2> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'PresaleDepositV2');
  return deserializePresaleDepositV2(maybeAccount);
}

export async function safeFetchPresaleDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<PresaleDepositV2 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializePresaleDepositV2(maybeAccount) : null;
}

export async function fetchAllPresaleDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PresaleDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'PresaleDepositV2');
    return deserializePresaleDepositV2(maybeAccount);
  });
}

export async function safeFetchAllPresaleDepositV2(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<PresaleDepositV2[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializePresaleDepositV2(maybeAccount as RpcAccount)
    );
}

export function getPresaleDepositV2GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      claimed: boolean;
      padding: Array<number>;
      index: number;
      amountClaimed: number | bigint;
      depositor: PublicKey;
      bucket: PublicKey;
      timestamp: number | bigint;
      amountQuoteToken: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      claimed: [2, bool()],
      padding: [3, array(u8(), { size: 1 })],
      index: [4, u32()],
      amountClaimed: [8, u64()],
      depositor: [16, publicKeySerializer()],
      bucket: [48, publicKeySerializer()],
      timestamp: [80, i64()],
      amountQuoteToken: [88, u64()],
    })
    .deserializeUsing<PresaleDepositV2>((account) =>
      deserializePresaleDepositV2(account)
    );
}

export function getPresaleDepositV2Size(): number {
  return 96;
}
