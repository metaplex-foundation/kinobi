/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  DistributionBucketState,
  DistributionBucketStateArgs,
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  Key,
  KeyArgs,
  getDistributionBucketStateSerializer,
  getFeeDiscriminantsSerializer,
  getKeySerializer,
} from '../types';

export type DistributionBucketV1 = Account<DistributionBucketV1AccountData>;

export type DistributionBucketV1AccountData = {
  key: Key;
  bump: number;
  state: DistributionBucketState;
  index: number;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: bigint;
  solBalance: bigint;
  tokenBalance: bigint;
  start: bigint;
  distributionAddress: PublicKey;
  backendSigner: PublicKey;
  fee: bigint;
  reserved: Array<number>;
};

export type DistributionBucketV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  state: DistributionBucketStateArgs;
  index: number;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  genesis: PublicKey;
  tokenAllocation: number | bigint;
  solBalance: number | bigint;
  tokenBalance: number | bigint;
  start: number | bigint;
  distributionAddress: PublicKey;
  backendSigner: PublicKey;
  fee: number | bigint;
  reserved: Array<number>;
};

export function getDistributionBucketV1AccountDataSerializer(): Serializer<
  DistributionBucketV1AccountDataArgs,
  DistributionBucketV1AccountData
> {
  return struct<DistributionBucketV1AccountData>(
    [
      ['key', getKeySerializer()],
      ['bump', u8()],
      ['state', getDistributionBucketStateSerializer()],
      ['index', u8()],
      ['feeType', getFeeDiscriminantsSerializer()],
      ['padding', array(u8(), { size: 3 })],
      ['genesis', publicKeySerializer()],
      ['tokenAllocation', u64()],
      ['solBalance', u64()],
      ['tokenBalance', u64()],
      ['start', i64()],
      ['distributionAddress', publicKeySerializer()],
      ['backendSigner', publicKeySerializer()],
      ['fee', u64()],
      ['reserved', array(u8(), { size: 128 })],
    ],
    { description: 'DistributionBucketV1AccountData' }
  ) as Serializer<
    DistributionBucketV1AccountDataArgs,
    DistributionBucketV1AccountData
  >;
}

export function deserializeDistributionBucketV1(
  rawAccount: RpcAccount
): DistributionBucketV1 {
  return deserializeAccount(
    rawAccount,
    getDistributionBucketV1AccountDataSerializer()
  );
}

export async function fetchDistributionBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<DistributionBucketV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'DistributionBucketV1');
  return deserializeDistributionBucketV1(maybeAccount);
}

export async function safeFetchDistributionBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<DistributionBucketV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeDistributionBucketV1(maybeAccount)
    : null;
}

export async function fetchAllDistributionBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<DistributionBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'DistributionBucketV1');
    return deserializeDistributionBucketV1(maybeAccount);
  });
}

export async function safeFetchAllDistributionBucketV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<DistributionBucketV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeDistributionBucketV1(maybeAccount as RpcAccount)
    );
}

export function getDistributionBucketV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      state: DistributionBucketStateArgs;
      index: number;
      feeType: FeeDiscriminantsArgs;
      padding: Array<number>;
      genesis: PublicKey;
      tokenAllocation: number | bigint;
      solBalance: number | bigint;
      tokenBalance: number | bigint;
      start: number | bigint;
      distributionAddress: PublicKey;
      backendSigner: PublicKey;
      fee: number | bigint;
      reserved: Array<number>;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      state: [2, getDistributionBucketStateSerializer()],
      index: [3, u8()],
      feeType: [4, getFeeDiscriminantsSerializer()],
      padding: [5, array(u8(), { size: 3 })],
      genesis: [8, publicKeySerializer()],
      tokenAllocation: [40, u64()],
      solBalance: [48, u64()],
      tokenBalance: [56, u64()],
      start: [64, i64()],
      distributionAddress: [72, publicKeySerializer()],
      backendSigner: [104, publicKeySerializer()],
      fee: [136, u64()],
      reserved: [144, array(u8(), { size: 128 })],
    })
    .deserializeUsing<DistributionBucketV1>((account) =>
      deserializeDistributionBucketV1(account)
    );
}

export function getDistributionBucketV1Size(): number {
  return 272;
}
