/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawVaultV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The PDA to track the user's deposit */
  depositPda: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of deposit PDA */
  authority?: Signer;
  /** The wallet where cancel fees are sent */
  feeWallet: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: Signer;
};

// Data.
export type WithdrawVaultV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
  amount: bigint;
};

export type WithdrawVaultV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
  amount: number | bigint;
};

export function getWithdrawVaultV1InstructionDataSerializer(): Serializer<
  WithdrawVaultV1InstructionDataArgs,
  WithdrawVaultV1InstructionData
> {
  return mapSerializer<
    WithdrawVaultV1InstructionDataArgs,
    any,
    WithdrawVaultV1InstructionData
  >(
    struct<WithdrawVaultV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['amount', u64()],
      ],
      { description: 'WithdrawVaultV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 28 })
  ) as Serializer<
    WithdrawVaultV1InstructionDataArgs,
    WithdrawVaultV1InstructionData
  >;
}

// Args.
export type WithdrawVaultV1InstructionArgs = WithdrawVaultV1InstructionDataArgs;

// Instruction discriminator.
export const withdrawVaultV1InstructionDiscriminator = 28;

// Instruction.
export function withdrawVaultV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: WithdrawVaultV1InstructionAccounts & WithdrawVaultV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    depositPda: {
      index: 2,
      isWritable: true as boolean,
      value: input.depositPda ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 4,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    feeWallet: {
      index: 5,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    backendSigner: {
      index: 6,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: WithdrawVaultV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawVaultV1InstructionDataSerializer().serialize(
    resolvedArgs as WithdrawVaultV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
