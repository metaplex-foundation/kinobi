/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  LaunchPoolV2ExtensionType,
  LaunchPoolV2ExtensionTypeArgs,
  getLaunchPoolV2ExtensionTypeSerializer,
} from '../types';

// Accounts.
export type RemoveLaunchPoolBucketV2ExtensionsInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The account paying for the transaction fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
};

// Data.
export type RemoveLaunchPoolBucketV2ExtensionsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  extensionTypes: Array<LaunchPoolV2ExtensionType>;
};

export type RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs = {
  padding: Array<number>;
  extensionTypes: Array<LaunchPoolV2ExtensionTypeArgs>;
};

export function getRemoveLaunchPoolBucketV2ExtensionsInstructionDataSerializer(): Serializer<
  RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs,
  RemoveLaunchPoolBucketV2ExtensionsInstructionData
> {
  return mapSerializer<
    RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs,
    any,
    RemoveLaunchPoolBucketV2ExtensionsInstructionData
  >(
    struct<RemoveLaunchPoolBucketV2ExtensionsInstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['extensionTypes', array(getLaunchPoolV2ExtensionTypeSerializer())],
      ],
      { description: 'RemoveLaunchPoolBucketV2ExtensionsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 74 })
  ) as Serializer<
    RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs,
    RemoveLaunchPoolBucketV2ExtensionsInstructionData
  >;
}

// Args.
export type RemoveLaunchPoolBucketV2ExtensionsInstructionArgs =
  RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs;

// Instruction discriminator.
export const removeLaunchPoolBucketV2ExtensionsInstructionDiscriminator = 74;

// Instruction.
export function removeLaunchPoolBucketV2Extensions(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RemoveLaunchPoolBucketV2ExtensionsInstructionAccounts &
    RemoveLaunchPoolBucketV2ExtensionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveLaunchPoolBucketV2ExtensionsInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveLaunchPoolBucketV2ExtensionsInstructionDataSerializer().serialize(
      resolvedArgs as RemoveLaunchPoolBucketV2ExtensionsInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
