/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type FinalizeV2InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the base token mint account */
  baseMint: PublicKey | Pda;
  /** The authority of the Genesis account */
  authority?: Signer;
};

// Data.
export type FinalizeV2InstructionData = {
  discriminator: number;
  padding: Array<number>;
};

export type FinalizeV2InstructionDataArgs = { padding: Array<number> };

export function getFinalizeV2InstructionDataSerializer(): Serializer<
  FinalizeV2InstructionDataArgs,
  FinalizeV2InstructionData
> {
  return mapSerializer<
    FinalizeV2InstructionDataArgs,
    any,
    FinalizeV2InstructionData
  >(
    struct<FinalizeV2InstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 7 })],
      ],
      { description: 'FinalizeV2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 46 })
  ) as Serializer<FinalizeV2InstructionDataArgs, FinalizeV2InstructionData>;
}

// Args.
export type FinalizeV2InstructionArgs = FinalizeV2InstructionDataArgs;

// Instruction discriminator.
export const finalizeV2InstructionDiscriminator = 46;

// Instruction.
export function finalizeV2(
  context: Pick<Context, 'identity' | 'programs'>,
  input: FinalizeV2InstructionAccounts & FinalizeV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    baseMint: {
      index: 1,
      isWritable: true as boolean,
      value: input.baseMint ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: FinalizeV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFinalizeV2InstructionDataSerializer().serialize(
    resolvedArgs as FinalizeV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
