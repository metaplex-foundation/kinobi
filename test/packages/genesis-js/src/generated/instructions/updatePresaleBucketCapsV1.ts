/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdatePresaleBucketCapsV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the presale bucket */
  bucket: PublicKey | Pda;
  /** The address of the mint account */
  mint: PublicKey | Pda;
  /** The authority of the Genesis account */
  authority?: Signer;
  /** Backend authorisation signer */
  backendSigner?: Signer;
};

// Data.
export type UpdatePresaleBucketCapsV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
  newAllocationSolCap: bigint;
  newAllowlistSolCap: bigint;
};

export type UpdatePresaleBucketCapsV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
  newAllocationSolCap: number | bigint;
  newAllowlistSolCap: number | bigint;
};

export function getUpdatePresaleBucketCapsV1InstructionDataSerializer(): Serializer<
  UpdatePresaleBucketCapsV1InstructionDataArgs,
  UpdatePresaleBucketCapsV1InstructionData
> {
  return mapSerializer<
    UpdatePresaleBucketCapsV1InstructionDataArgs,
    any,
    UpdatePresaleBucketCapsV1InstructionData
  >(
    struct<UpdatePresaleBucketCapsV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['newAllocationSolCap', u64()],
        ['newAllowlistSolCap', u64()],
      ],
      { description: 'UpdatePresaleBucketCapsV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 41 })
  ) as Serializer<
    UpdatePresaleBucketCapsV1InstructionDataArgs,
    UpdatePresaleBucketCapsV1InstructionData
  >;
}

// Args.
export type UpdatePresaleBucketCapsV1InstructionArgs =
  UpdatePresaleBucketCapsV1InstructionDataArgs;

// Instruction discriminator.
export const updatePresaleBucketCapsV1InstructionDiscriminator = 41;

// Instruction.
export function updatePresaleBucketCapsV1(
  context: Pick<Context, 'identity' | 'programs'>,
  input: UpdatePresaleBucketCapsV1InstructionAccounts &
    UpdatePresaleBucketCapsV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    backendSigner: {
      index: 4,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePresaleBucketCapsV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getUpdatePresaleBucketCapsV1InstructionDataSerializer().serialize(
      resolvedArgs as UpdatePresaleBucketCapsV1InstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
