/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import { Condition, ConditionArgs, getConditionSerializer } from '../types';

// Accounts.
export type AddUnlockedBucketV2BaseInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket account */
  bucket: PublicKey | Pda;
  /** The address of the base token mint account */
  baseMint: PublicKey | Pda;
  /** The mint account of the quote token */
  quoteMint: PublicKey | Pda;
  /** The token account owned by the bucket for the quote token */
  bucketQuoteTokenAccount: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
  /** The recipient of the tokens (optional; defaults to authority) */
  recipient?: PublicKey | Pda;
  /** The SPL Token program for the quote token */
  quoteTokenProgram: PublicKey | Pda;
  /** The Associated Token program */
  associatedTokenProgram: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AddUnlockedBucketV2BaseInstructionData = {
  discriminator: number;
  bucketIndex: number;
  padding: Array<number>;
  baseTokenAllocation: bigint;
  claimStartCondition: Condition;
  claimEndCondition: Condition;
};

export type AddUnlockedBucketV2BaseInstructionDataArgs = {
  bucketIndex: number;
  padding: Array<number>;
  baseTokenAllocation: number | bigint;
  claimStartCondition: ConditionArgs;
  claimEndCondition: ConditionArgs;
};

export function getAddUnlockedBucketV2BaseInstructionDataSerializer(): Serializer<
  AddUnlockedBucketV2BaseInstructionDataArgs,
  AddUnlockedBucketV2BaseInstructionData
> {
  return mapSerializer<
    AddUnlockedBucketV2BaseInstructionDataArgs,
    any,
    AddUnlockedBucketV2BaseInstructionData
  >(
    struct<AddUnlockedBucketV2BaseInstructionData>(
      [
        ['discriminator', u8()],
        ['bucketIndex', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['baseTokenAllocation', u64()],
        ['claimStartCondition', getConditionSerializer()],
        ['claimEndCondition', getConditionSerializer()],
      ],
      { description: 'AddUnlockedBucketV2BaseInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 97 })
  ) as Serializer<
    AddUnlockedBucketV2BaseInstructionDataArgs,
    AddUnlockedBucketV2BaseInstructionData
  >;
}

// Args.
export type AddUnlockedBucketV2BaseInstructionArgs =
  AddUnlockedBucketV2BaseInstructionDataArgs;

// Instruction discriminator.
export const addUnlockedBucketV2BaseInstructionDiscriminator = 97;

// Instruction.
export function addUnlockedBucketV2Base(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddUnlockedBucketV2BaseInstructionAccounts &
    AddUnlockedBucketV2BaseInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    baseMint: {
      index: 2,
      isWritable: false as boolean,
      value: input.baseMint ?? null,
    },
    quoteMint: {
      index: 3,
      isWritable: false as boolean,
      value: input.quoteMint ?? null,
    },
    bucketQuoteTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.bucketQuoteTokenAccount ?? null,
    },
    payer: {
      index: 5,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 6,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    recipient: {
      index: 7,
      isWritable: false as boolean,
      value: input.recipient ?? null,
    },
    quoteTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.quoteTokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddUnlockedBucketV2BaseInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddUnlockedBucketV2BaseInstructionDataSerializer().serialize(
    resolvedArgs as AddUnlockedBucketV2BaseInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
