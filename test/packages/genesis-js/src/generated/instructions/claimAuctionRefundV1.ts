/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u16,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ClaimAuctionRefundV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the auction bucket */
  bucket: PublicKey | Pda;
  /** The address of the mint */
  mint: PublicKey | Pda;
  /** The default refund recipient (bidder) */
  payer?: PublicKey | Pda;
  /** The address of the auction bid PDA */
  bid: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: Signer;
};

// Data.
export type ClaimAuctionRefundV1InstructionData = {
  discriminator: number;
  index: number;
  bidIndex: number;
  padding: Array<number>;
};

export type ClaimAuctionRefundV1InstructionDataArgs = {
  index: number;
  bidIndex: number;
  padding: Array<number>;
};

export function getClaimAuctionRefundV1InstructionDataSerializer(): Serializer<
  ClaimAuctionRefundV1InstructionDataArgs,
  ClaimAuctionRefundV1InstructionData
> {
  return mapSerializer<
    ClaimAuctionRefundV1InstructionDataArgs,
    any,
    ClaimAuctionRefundV1InstructionData
  >(
    struct<ClaimAuctionRefundV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['bidIndex', u16()],
        ['padding', array(u8(), { size: 4 })],
      ],
      { description: 'ClaimAuctionRefundV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 37 })
  ) as Serializer<
    ClaimAuctionRefundV1InstructionDataArgs,
    ClaimAuctionRefundV1InstructionData
  >;
}

// Args.
export type ClaimAuctionRefundV1InstructionArgs =
  ClaimAuctionRefundV1InstructionDataArgs;

// Instruction discriminator.
export const claimAuctionRefundV1InstructionDiscriminator = 37;

// Instruction.
export function claimAuctionRefundV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: ClaimAuctionRefundV1InstructionAccounts &
    ClaimAuctionRefundV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    bid: { index: 4, isWritable: true as boolean, value: input.bid ?? null },
    backendSigner: {
      index: 5,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ClaimAuctionRefundV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer.publicKey;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getClaimAuctionRefundV1InstructionDataSerializer().serialize(
    resolvedArgs as ClaimAuctionRefundV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
