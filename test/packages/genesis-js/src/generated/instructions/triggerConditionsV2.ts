/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type TriggerConditionsV2InstructionAccounts = {
  /** The Genesis account */
  genesisAccount: PublicKey | Pda;
  /** The bucket whose conditions to resolve */
  bucket: PublicKey | Pda;
  /** The base token mint (used to verify genesis PDA derivation) */
  baseMint: PublicKey | Pda;
  /** Payer */
  payer?: Signer;
};

// Data.
export type TriggerConditionsV2InstructionData = {
  discriminator: number;
  padding: Array<number>;
};

export type TriggerConditionsV2InstructionDataArgs = { padding: Array<number> };

export function getTriggerConditionsV2InstructionDataSerializer(): Serializer<
  TriggerConditionsV2InstructionDataArgs,
  TriggerConditionsV2InstructionData
> {
  return mapSerializer<
    TriggerConditionsV2InstructionDataArgs,
    any,
    TriggerConditionsV2InstructionData
  >(
    struct<TriggerConditionsV2InstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 7 })],
      ],
      { description: 'TriggerConditionsV2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 43 })
  ) as Serializer<
    TriggerConditionsV2InstructionDataArgs,
    TriggerConditionsV2InstructionData
  >;
}

// Args.
export type TriggerConditionsV2InstructionArgs =
  TriggerConditionsV2InstructionDataArgs;

// Instruction discriminator.
export const triggerConditionsV2InstructionDiscriminator = 43;

// Instruction.
export function triggerConditionsV2(
  context: Pick<Context, 'payer' | 'programs'>,
  input: TriggerConditionsV2InstructionAccounts &
    TriggerConditionsV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    baseMint: {
      index: 2,
      isWritable: false as boolean,
      value: input.baseMint ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TriggerConditionsV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTriggerConditionsV2InstructionDataSerializer().serialize(
    resolvedArgs as TriggerConditionsV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
