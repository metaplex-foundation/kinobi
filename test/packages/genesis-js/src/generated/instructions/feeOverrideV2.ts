/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  FeeDiscriminants,
  FeeDiscriminantsArgs,
  FeeOverrideType,
  FeeOverrideTypeArgs,
  getFeeDiscriminantsSerializer,
  getFeeOverrideTypeSerializer,
} from '../types';

// Accounts.
export type FeeOverrideV2InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the launchpool bucket */
  bucket: PublicKey | Pda;
  /** The address of the base token mint account */
  baseMint: PublicKey | Pda;
  /** The global Metaplex authority overriding the default fees */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type FeeOverrideV2InstructionData = {
  discriminator: number;
  feeOverrideType: FeeOverrideType;
  feeType: FeeDiscriminants;
  padding: Array<number>;
  feeOverride: bigint;
};

export type FeeOverrideV2InstructionDataArgs = {
  feeOverrideType: FeeOverrideTypeArgs;
  feeType: FeeDiscriminantsArgs;
  padding: Array<number>;
  feeOverride: number | bigint;
};

export function getFeeOverrideV2InstructionDataSerializer(): Serializer<
  FeeOverrideV2InstructionDataArgs,
  FeeOverrideV2InstructionData
> {
  return mapSerializer<
    FeeOverrideV2InstructionDataArgs,
    any,
    FeeOverrideV2InstructionData
  >(
    struct<FeeOverrideV2InstructionData>(
      [
        ['discriminator', u8()],
        ['feeOverrideType', getFeeOverrideTypeSerializer()],
        ['feeType', getFeeDiscriminantsSerializer()],
        ['padding', array(u8(), { size: 5 })],
        ['feeOverride', u64()],
      ],
      { description: 'FeeOverrideV2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 47 })
  ) as Serializer<
    FeeOverrideV2InstructionDataArgs,
    FeeOverrideV2InstructionData
  >;
}

// Args.
export type FeeOverrideV2InstructionArgs = FeeOverrideV2InstructionDataArgs;

// Instruction discriminator.
export const feeOverrideV2InstructionDiscriminator = 47;

// Instruction.
export function feeOverrideV2(
  context: Pick<Context, 'identity' | 'programs'>,
  input: FeeOverrideV2InstructionAccounts & FeeOverrideV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    baseMint: {
      index: 2,
      isWritable: true as boolean,
      value: input.baseMint ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: FeeOverrideV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFeeOverrideV2InstructionDataSerializer().serialize(
    resolvedArgs as FeeOverrideV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
