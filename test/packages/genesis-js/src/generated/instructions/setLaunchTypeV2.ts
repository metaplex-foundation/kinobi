/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import { LaunchType, LaunchTypeArgs, getLaunchTypeSerializer } from '../types';

// Accounts.
export type SetLaunchTypeV2InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the base token mint account */
  baseMint: PublicKey | Pda;
  /** The backend signer setting the launch type */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type SetLaunchTypeV2InstructionData = {
  discriminator: number;
  launchType: LaunchType;
  padding: Array<number>;
};

export type SetLaunchTypeV2InstructionDataArgs = {
  launchType: LaunchTypeArgs;
  padding: Array<number>;
};

export function getSetLaunchTypeV2InstructionDataSerializer(): Serializer<
  SetLaunchTypeV2InstructionDataArgs,
  SetLaunchTypeV2InstructionData
> {
  return mapSerializer<
    SetLaunchTypeV2InstructionDataArgs,
    any,
    SetLaunchTypeV2InstructionData
  >(
    struct<SetLaunchTypeV2InstructionData>(
      [
        ['discriminator', u8()],
        ['launchType', getLaunchTypeSerializer()],
        ['padding', array(u8(), { size: 6 })],
      ],
      { description: 'SetLaunchTypeV2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 48 })
  ) as Serializer<
    SetLaunchTypeV2InstructionDataArgs,
    SetLaunchTypeV2InstructionData
  >;
}

// Args.
export type SetLaunchTypeV2InstructionArgs = SetLaunchTypeV2InstructionDataArgs;

// Instruction discriminator.
export const setLaunchTypeV2InstructionDiscriminator = 48;

// Instruction.
export function setLaunchTypeV2(
  context: Pick<Context, 'identity' | 'programs'>,
  input: SetLaunchTypeV2InstructionAccounts & SetLaunchTypeV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    baseMint: {
      index: 1,
      isWritable: false as boolean,
      value: input.baseMint ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetLaunchTypeV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetLaunchTypeV2InstructionDataSerializer().serialize(
    resolvedArgs as SetLaunchTypeV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
