/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  i64,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddVaultBucketV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket account */
  bucket: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of the mint */
  authority?: Signer;
  /** The wallet where fees are sent */
  feeWallet: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: PublicKey | Pda;
  /** The authority of the vault bucket actions */
  actionsAuthority?: PublicKey | Pda;
};

// Data.
export type AddVaultBucketV1InstructionData = {
  discriminator: number;
  index: number;
  allowWithdrawals: boolean;
  padding: Array<number>;
  start: bigint;
  end: bigint;
};

export type AddVaultBucketV1InstructionDataArgs = {
  index: number;
  allowWithdrawals: boolean;
  padding: Array<number>;
  start: number | bigint;
  end: number | bigint;
};

export function getAddVaultBucketV1InstructionDataSerializer(): Serializer<
  AddVaultBucketV1InstructionDataArgs,
  AddVaultBucketV1InstructionData
> {
  return mapSerializer<
    AddVaultBucketV1InstructionDataArgs,
    any,
    AddVaultBucketV1InstructionData
  >(
    struct<AddVaultBucketV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['allowWithdrawals', bool()],
        ['padding', array(u8(), { size: 5 })],
        ['start', i64()],
        ['end', i64()],
      ],
      { description: 'AddVaultBucketV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 26 })
  ) as Serializer<
    AddVaultBucketV1InstructionDataArgs,
    AddVaultBucketV1InstructionData
  >;
}

// Args.
export type AddVaultBucketV1InstructionArgs =
  AddVaultBucketV1InstructionDataArgs;

// Instruction discriminator.
export const addVaultBucketV1InstructionDiscriminator = 26;

// Instruction.
export function addVaultBucketV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddVaultBucketV1InstructionAccounts & AddVaultBucketV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    feeWallet: {
      index: 4,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    backendSigner: {
      index: 6,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
    actionsAuthority: {
      index: 7,
      isWritable: false as boolean,
      value: input.actionsAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddVaultBucketV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddVaultBucketV1InstructionDataSerializer().serialize(
    resolvedArgs as AddVaultBucketV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
