/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawPresaleV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The address of the mint */
  mint: PublicKey | Pda;
  /** The PDA to track the user's deposit */
  depositPda: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of deposit PDA */
  authority?: Signer;
  /** The token account owned by the recipient */
  recipientTokenAccount: PublicKey | Pda;
  /** The wallet where cancel fees are sent */
  feeWallet: PublicKey | Pda;
  /** The SPL Token program */
  tokenProgram?: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: Signer;
};

// Data.
export type WithdrawPresaleV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
};

export type WithdrawPresaleV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
};

export function getWithdrawPresaleV1InstructionDataSerializer(): Serializer<
  WithdrawPresaleV1InstructionDataArgs,
  WithdrawPresaleV1InstructionData
> {
  return mapSerializer<
    WithdrawPresaleV1InstructionDataArgs,
    any,
    WithdrawPresaleV1InstructionData
  >(
    struct<WithdrawPresaleV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['padding', array(u8(), { size: 6 })],
      ],
      { description: 'WithdrawPresaleV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 22 })
  ) as Serializer<
    WithdrawPresaleV1InstructionDataArgs,
    WithdrawPresaleV1InstructionData
  >;
}

// Args.
export type WithdrawPresaleV1InstructionArgs =
  WithdrawPresaleV1InstructionDataArgs;

// Instruction discriminator.
export const withdrawPresaleV1InstructionDiscriminator = 22;

// Instruction.
export function withdrawPresaleV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: WithdrawPresaleV1InstructionAccounts & WithdrawPresaleV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    depositPda: {
      index: 3,
      isWritable: true as boolean,
      value: input.depositPda ?? null,
    },
    payer: {
      index: 4,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 5,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    recipientTokenAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.recipientTokenAccount ?? null,
    },
    feeWallet: {
      index: 7,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    tokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    backendSigner: {
      index: 9,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: WithdrawPresaleV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawPresaleV1InstructionDataSerializer().serialize(
    resolvedArgs as WithdrawPresaleV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
