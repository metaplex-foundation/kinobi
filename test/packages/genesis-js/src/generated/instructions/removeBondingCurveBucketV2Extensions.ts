/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  BondingCurveV2ExtensionType,
  BondingCurveV2ExtensionTypeArgs,
  getBondingCurveV2ExtensionTypeSerializer,
} from '../types';

// Accounts.
export type RemoveBondingCurveBucketV2ExtensionsInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The account paying for the transaction fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
};

// Data.
export type RemoveBondingCurveBucketV2ExtensionsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  extensionTypes: Array<BondingCurveV2ExtensionType>;
};

export type RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs = {
  padding: Array<number>;
  extensionTypes: Array<BondingCurveV2ExtensionTypeArgs>;
};

export function getRemoveBondingCurveBucketV2ExtensionsInstructionDataSerializer(): Serializer<
  RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs,
  RemoveBondingCurveBucketV2ExtensionsInstructionData
> {
  return mapSerializer<
    RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs,
    any,
    RemoveBondingCurveBucketV2ExtensionsInstructionData
  >(
    struct<RemoveBondingCurveBucketV2ExtensionsInstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['extensionTypes', array(getBondingCurveV2ExtensionTypeSerializer())],
      ],
      { description: 'RemoveBondingCurveBucketV2ExtensionsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 89 })
  ) as Serializer<
    RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs,
    RemoveBondingCurveBucketV2ExtensionsInstructionData
  >;
}

// Args.
export type RemoveBondingCurveBucketV2ExtensionsInstructionArgs =
  RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs;

// Instruction discriminator.
export const removeBondingCurveBucketV2ExtensionsInstructionDiscriminator = 89;

// Instruction.
export function removeBondingCurveBucketV2Extensions(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RemoveBondingCurveBucketV2ExtensionsInstructionAccounts &
    RemoveBondingCurveBucketV2ExtensionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveBondingCurveBucketV2ExtensionsInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveBondingCurveBucketV2ExtensionsInstructionDataSerializer().serialize(
      resolvedArgs as RemoveBondingCurveBucketV2ExtensionsInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
