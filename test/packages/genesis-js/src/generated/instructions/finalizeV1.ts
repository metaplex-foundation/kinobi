/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  bytes,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type FinalizeV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the mint account */
  mint: PublicKey | Pda;
  /** The authority of the Genesis account */
  authority?: Signer;
  /** The wallet where fees are sent */
  feeWallet: PublicKey | Pda;
  /** The SPL Token program */
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type FinalizeV1InstructionData = {
  discriminator: number;
  burnMintAuthority: boolean;
  burnFreezeAuthority: boolean;
  padding: Array<number>;
  reserved: Uint8Array;
};

export type FinalizeV1InstructionDataArgs = {
  burnMintAuthority: boolean;
  burnFreezeAuthority: boolean;
  padding: Array<number>;
  reserved: Uint8Array;
};

export function getFinalizeV1InstructionDataSerializer(): Serializer<
  FinalizeV1InstructionDataArgs,
  FinalizeV1InstructionData
> {
  return mapSerializer<
    FinalizeV1InstructionDataArgs,
    any,
    FinalizeV1InstructionData
  >(
    struct<FinalizeV1InstructionData>(
      [
        ['discriminator', u8()],
        ['burnMintAuthority', bool()],
        ['burnFreezeAuthority', bool()],
        ['padding', array(u8(), { size: 5 })],
        ['reserved', bytes({ size: 32 })],
      ],
      { description: 'FinalizeV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 16 })
  ) as Serializer<FinalizeV1InstructionDataArgs, FinalizeV1InstructionData>;
}

// Args.
export type FinalizeV1InstructionArgs = FinalizeV1InstructionDataArgs;

// Instruction discriminator.
export const finalizeV1InstructionDiscriminator = 16;

// Instruction.
export function finalizeV1(
  context: Pick<Context, 'identity' | 'programs'>,
  input: FinalizeV1InstructionAccounts & FinalizeV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    mint: { index: 1, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    feeWallet: {
      index: 3,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    tokenProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: FinalizeV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFinalizeV1InstructionDataSerializer().serialize(
    resolvedArgs as FinalizeV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
