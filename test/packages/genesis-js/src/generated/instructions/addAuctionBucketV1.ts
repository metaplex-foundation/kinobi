/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  mapSerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddAuctionBucketV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the auction bucket account */
  bucket: PublicKey | Pda;
  /** The auction tree PDA (will be created and initialized) */
  auctionTree: PublicKey | Pda;
  /** The address of the mint account */
  mint: PublicKey | Pda;
  /** The token account owned by the bucket */
  bucketTokenAccount: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of the mint (overrides payer as authority) */
  authority?: Signer;
  /** The wallet where fees are sent */
  feeWallet: PublicKey | Pda;
  /** The SPL Token program */
  tokenProgram?: PublicKey | Pda;
  /** The Associated Token program */
  associatedTokenProgram: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: PublicKey | Pda;
};

// Data.
export type AddAuctionBucketV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
  tokenAllocation: bigint;
  start: bigint;
  end: bigint;
  claimStart: bigint;
  minBidIncrementBps: number;
  minTokenQuantityDecibps: number;
  minPriceUlamports: bigint;
  maxBidQuantity: bigint;
  maxAuctionCapacity: number;
  extensionThresholdSeconds: number;
  extensionDurationSeconds: number;
  extensionMultiplierBps: number;
};

export type AddAuctionBucketV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
  tokenAllocation: number | bigint;
  start: number | bigint;
  end: number | bigint;
  claimStart: number | bigint;
  minBidIncrementBps: number;
  minTokenQuantityDecibps: number;
  minPriceUlamports: number | bigint;
  maxBidQuantity: number | bigint;
  maxAuctionCapacity: number;
  extensionThresholdSeconds: number;
  extensionDurationSeconds: number;
  extensionMultiplierBps: number;
};

export function getAddAuctionBucketV1InstructionDataSerializer(): Serializer<
  AddAuctionBucketV1InstructionDataArgs,
  AddAuctionBucketV1InstructionData
> {
  return mapSerializer<
    AddAuctionBucketV1InstructionDataArgs,
    any,
    AddAuctionBucketV1InstructionData
  >(
    struct<AddAuctionBucketV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['tokenAllocation', u64()],
        ['start', i64()],
        ['end', i64()],
        ['claimStart', i64()],
        ['minBidIncrementBps', u32()],
        ['minTokenQuantityDecibps', u32()],
        ['minPriceUlamports', u64()],
        ['maxBidQuantity', u64()],
        ['maxAuctionCapacity', u32()],
        ['extensionThresholdSeconds', u32()],
        ['extensionDurationSeconds', u32()],
        ['extensionMultiplierBps', u32()],
      ],
      { description: 'AddAuctionBucketV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 34 })
  ) as Serializer<
    AddAuctionBucketV1InstructionDataArgs,
    AddAuctionBucketV1InstructionData
  >;
}

// Args.
export type AddAuctionBucketV1InstructionArgs =
  AddAuctionBucketV1InstructionDataArgs;

// Instruction discriminator.
export const addAuctionBucketV1InstructionDiscriminator = 34;

// Instruction.
export function addAuctionBucketV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddAuctionBucketV1InstructionAccounts &
    AddAuctionBucketV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    auctionTree: {
      index: 2,
      isWritable: true as boolean,
      value: input.auctionTree ?? null,
    },
    mint: { index: 3, isWritable: true as boolean, value: input.mint ?? null },
    bucketTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.bucketTokenAccount ?? null,
    },
    payer: {
      index: 5,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 6,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    feeWallet: {
      index: 7,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    tokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    backendSigner: {
      index: 11,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddAuctionBucketV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddAuctionBucketV1InstructionDataSerializer().serialize(
    resolvedArgs as AddAuctionBucketV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
