/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  LinearBpsSchedule,
  LinearBpsScheduleArgs,
  getLinearBpsScheduleSerializer,
} from '../types';

// Accounts.
export type AddLaunchPoolBucketV1InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket account */
  bucket: PublicKey | Pda;
  /** The address of the mint account */
  mint: PublicKey | Pda;
  /** The token account owned by the bucket */
  bucketTokenAccount: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority of the mint */
  authority?: Signer;
  /** The wallet where fees are sent */
  feeWallet: PublicKey | Pda;
  /** The SPL Token program */
  tokenProgram?: PublicKey | Pda;
  /** The Associated Token program */
  associatedTokenProgram: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
  /** Backend authorisation signer */
  backendSigner?: PublicKey | Pda;
  /** Creator-controlled wallet that receives dynamic penalty fees (optional, defaults to fee_wallet) */
  penaltyWallet?: PublicKey | Pda;
};

// Data.
export type AddLaunchPoolBucketV1InstructionData = {
  discriminator: number;
  index: number;
  padding: Array<number>;
  totalAmount: bigint;
  start: bigint;
  end: bigint;
  claimStart: bigint;
  depositPenalty: LinearBpsSchedule;
  withdrawPenalty: LinearBpsSchedule;
  bonusSchedule: LinearBpsSchedule;
};

export type AddLaunchPoolBucketV1InstructionDataArgs = {
  index: number;
  padding: Array<number>;
  totalAmount: number | bigint;
  start: number | bigint;
  end: number | bigint;
  claimStart: number | bigint;
  depositPenalty: LinearBpsScheduleArgs;
  withdrawPenalty: LinearBpsScheduleArgs;
  bonusSchedule: LinearBpsScheduleArgs;
};

export function getAddLaunchPoolBucketV1InstructionDataSerializer(): Serializer<
  AddLaunchPoolBucketV1InstructionDataArgs,
  AddLaunchPoolBucketV1InstructionData
> {
  return mapSerializer<
    AddLaunchPoolBucketV1InstructionDataArgs,
    any,
    AddLaunchPoolBucketV1InstructionData
  >(
    struct<AddLaunchPoolBucketV1InstructionData>(
      [
        ['discriminator', u8()],
        ['index', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['totalAmount', u64()],
        ['start', i64()],
        ['end', i64()],
        ['claimStart', i64()],
        ['depositPenalty', getLinearBpsScheduleSerializer()],
        ['withdrawPenalty', getLinearBpsScheduleSerializer()],
        ['bonusSchedule', getLinearBpsScheduleSerializer()],
      ],
      { description: 'AddLaunchPoolBucketV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<
    AddLaunchPoolBucketV1InstructionDataArgs,
    AddLaunchPoolBucketV1InstructionData
  >;
}

// Args.
export type AddLaunchPoolBucketV1InstructionArgs =
  AddLaunchPoolBucketV1InstructionDataArgs;

// Instruction discriminator.
export const addLaunchPoolBucketV1InstructionDiscriminator = 1;

// Instruction.
export function addLaunchPoolBucketV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddLaunchPoolBucketV1InstructionAccounts &
    AddLaunchPoolBucketV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    mint: { index: 2, isWritable: true as boolean, value: input.mint ?? null },
    bucketTokenAccount: {
      index: 3,
      isWritable: true as boolean,
      value: input.bucketTokenAccount ?? null,
    },
    payer: {
      index: 4,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 5,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    feeWallet: {
      index: 6,
      isWritable: true as boolean,
      value: input.feeWallet ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    backendSigner: {
      index: 10,
      isWritable: false as boolean,
      value: input.backendSigner ?? null,
    },
    penaltyWallet: {
      index: 11,
      isWritable: true as boolean,
      value: input.penaltyWallet ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddLaunchPoolBucketV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddLaunchPoolBucketV1InstructionDataSerializer().serialize(
    resolvedArgs as AddLaunchPoolBucketV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
