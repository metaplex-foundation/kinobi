/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  UnlockedV2ExtensionType,
  UnlockedV2ExtensionTypeArgs,
  getUnlockedV2ExtensionTypeSerializer,
} from '../types';

// Accounts.
export type RemoveUnlockedBucketV2ExtensionsInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The account paying for the transaction fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
};

// Data.
export type RemoveUnlockedBucketV2ExtensionsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  extensionTypes: Array<UnlockedV2ExtensionType>;
};

export type RemoveUnlockedBucketV2ExtensionsInstructionDataArgs = {
  padding: Array<number>;
  extensionTypes: Array<UnlockedV2ExtensionTypeArgs>;
};

export function getRemoveUnlockedBucketV2ExtensionsInstructionDataSerializer(): Serializer<
  RemoveUnlockedBucketV2ExtensionsInstructionDataArgs,
  RemoveUnlockedBucketV2ExtensionsInstructionData
> {
  return mapSerializer<
    RemoveUnlockedBucketV2ExtensionsInstructionDataArgs,
    any,
    RemoveUnlockedBucketV2ExtensionsInstructionData
  >(
    struct<RemoveUnlockedBucketV2ExtensionsInstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['extensionTypes', array(getUnlockedV2ExtensionTypeSerializer())],
      ],
      { description: 'RemoveUnlockedBucketV2ExtensionsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 99 })
  ) as Serializer<
    RemoveUnlockedBucketV2ExtensionsInstructionDataArgs,
    RemoveUnlockedBucketV2ExtensionsInstructionData
  >;
}

// Args.
export type RemoveUnlockedBucketV2ExtensionsInstructionArgs =
  RemoveUnlockedBucketV2ExtensionsInstructionDataArgs;

// Instruction discriminator.
export const removeUnlockedBucketV2ExtensionsInstructionDiscriminator = 99;

// Instruction.
export function removeUnlockedBucketV2Extensions(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RemoveUnlockedBucketV2ExtensionsInstructionAccounts &
    RemoveUnlockedBucketV2ExtensionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveUnlockedBucketV2ExtensionsInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveUnlockedBucketV2ExtensionsInstructionDataSerializer().serialize(
      resolvedArgs as RemoveUnlockedBucketV2ExtensionsInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
