/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import { Behavior, BehaviorArgs, getBehaviorSerializer } from '../types';

// Accounts.
export type SetLaunchPoolBucketV2BehaviorsInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The account paying for the transaction fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type SetLaunchPoolBucketV2BehaviorsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  endBehaviors: Array<Behavior>;
};

export type SetLaunchPoolBucketV2BehaviorsInstructionDataArgs = {
  padding: Array<number>;
  endBehaviors: Array<BehaviorArgs>;
};

export function getSetLaunchPoolBucketV2BehaviorsInstructionDataSerializer(): Serializer<
  SetLaunchPoolBucketV2BehaviorsInstructionDataArgs,
  SetLaunchPoolBucketV2BehaviorsInstructionData
> {
  return mapSerializer<
    SetLaunchPoolBucketV2BehaviorsInstructionDataArgs,
    any,
    SetLaunchPoolBucketV2BehaviorsInstructionData
  >(
    struct<SetLaunchPoolBucketV2BehaviorsInstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['endBehaviors', array(getBehaviorSerializer())],
      ],
      { description: 'SetLaunchPoolBucketV2BehaviorsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 75 })
  ) as Serializer<
    SetLaunchPoolBucketV2BehaviorsInstructionDataArgs,
    SetLaunchPoolBucketV2BehaviorsInstructionData
  >;
}

// Args.
export type SetLaunchPoolBucketV2BehaviorsInstructionArgs =
  SetLaunchPoolBucketV2BehaviorsInstructionDataArgs;

// Instruction discriminator.
export const setLaunchPoolBucketV2BehaviorsInstructionDiscriminator = 75;

// Instruction.
export function setLaunchPoolBucketV2Behaviors(
  context: Pick<Context, 'payer' | 'programs'>,
  input: SetLaunchPoolBucketV2BehaviorsInstructionAccounts &
    SetLaunchPoolBucketV2BehaviorsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetLaunchPoolBucketV2BehaviorsInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getSetLaunchPoolBucketV2BehaviorsInstructionDataSerializer().serialize(
      resolvedArgs as SetLaunchPoolBucketV2BehaviorsInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
