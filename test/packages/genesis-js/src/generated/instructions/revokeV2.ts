/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RevokeV2InstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the base token mint account */
  baseMint: PublicKey | Pda;
  /** The authority of the Genesis account */
  authority?: Signer;
  /** The SPL Token program for the base token */
  baseTokenProgram: PublicKey | Pda;
};

// Data.
export type RevokeV2InstructionData = {
  discriminator: number;
  revokeMintAuthority: boolean;
  revokeFreezeAuthority: boolean;
  padding: Array<number>;
};

export type RevokeV2InstructionDataArgs = {
  revokeMintAuthority: boolean;
  revokeFreezeAuthority: boolean;
  padding: Array<number>;
};

export function getRevokeV2InstructionDataSerializer(): Serializer<
  RevokeV2InstructionDataArgs,
  RevokeV2InstructionData
> {
  return mapSerializer<
    RevokeV2InstructionDataArgs,
    any,
    RevokeV2InstructionData
  >(
    struct<RevokeV2InstructionData>(
      [
        ['discriminator', u8()],
        ['revokeMintAuthority', bool()],
        ['revokeFreezeAuthority', bool()],
        ['padding', array(u8(), { size: 5 })],
      ],
      { description: 'RevokeV2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 45 })
  ) as Serializer<RevokeV2InstructionDataArgs, RevokeV2InstructionData>;
}

// Args.
export type RevokeV2InstructionArgs = RevokeV2InstructionDataArgs;

// Instruction discriminator.
export const revokeV2InstructionDiscriminator = 45;

// Instruction.
export function revokeV2(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RevokeV2InstructionAccounts & RevokeV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.genesisAccount ?? null,
    },
    baseMint: {
      index: 1,
      isWritable: true as boolean,
      value: input.baseMint ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    baseTokenProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.baseTokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RevokeV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRevokeV2InstructionDataSerializer().serialize(
    resolvedArgs as RevokeV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
