/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  LaunchPoolV2Extension,
  LaunchPoolV2ExtensionArgs,
  getLaunchPoolV2ExtensionSerializer,
} from '../types';

// Accounts.
export type AddLaunchPoolBucketV2ExtensionsInstructionAccounts = {
  /** The address of the genesis account */
  genesisAccount: PublicKey | Pda;
  /** The address of the bucket */
  bucket: PublicKey | Pda;
  /** The account paying for the transaction fees */
  payer?: Signer;
  /** The authority of the genesis account */
  authority?: Signer;
};

// Data.
export type AddLaunchPoolBucketV2ExtensionsInstructionData = {
  discriminator: number;
  padding: Array<number>;
  extensions: Array<LaunchPoolV2Extension>;
};

export type AddLaunchPoolBucketV2ExtensionsInstructionDataArgs = {
  padding: Array<number>;
  extensions: Array<LaunchPoolV2ExtensionArgs>;
};

export function getAddLaunchPoolBucketV2ExtensionsInstructionDataSerializer(): Serializer<
  AddLaunchPoolBucketV2ExtensionsInstructionDataArgs,
  AddLaunchPoolBucketV2ExtensionsInstructionData
> {
  return mapSerializer<
    AddLaunchPoolBucketV2ExtensionsInstructionDataArgs,
    any,
    AddLaunchPoolBucketV2ExtensionsInstructionData
  >(
    struct<AddLaunchPoolBucketV2ExtensionsInstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['extensions', array(getLaunchPoolV2ExtensionSerializer())],
      ],
      { description: 'AddLaunchPoolBucketV2ExtensionsInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 73 })
  ) as Serializer<
    AddLaunchPoolBucketV2ExtensionsInstructionDataArgs,
    AddLaunchPoolBucketV2ExtensionsInstructionData
  >;
}

// Args.
export type AddLaunchPoolBucketV2ExtensionsInstructionArgs =
  AddLaunchPoolBucketV2ExtensionsInstructionDataArgs;

// Instruction discriminator.
export const addLaunchPoolBucketV2ExtensionsInstructionDiscriminator = 73;

// Instruction.
export function addLaunchPoolBucketV2Extensions(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddLaunchPoolBucketV2ExtensionsInstructionAccounts &
    AddLaunchPoolBucketV2ExtensionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'genesisProgram',
    'GNS1S5J5AspKXgpjz6SvKL66kPaKWAhaGRhCqPRxii2B'
  );

  // Accounts.
  const resolvedAccounts = {
    genesisAccount: {
      index: 0,
      isWritable: false as boolean,
      value: input.genesisAccount ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddLaunchPoolBucketV2ExtensionsInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getAddLaunchPoolBucketV2ExtensionsInstructionDataSerializer().serialize(
      resolvedArgs as AddLaunchPoolBucketV2ExtensionsInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
