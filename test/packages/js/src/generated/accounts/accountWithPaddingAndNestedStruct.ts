/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  PaddedConfig,
  PaddedConfigArgs,
  getPaddedConfigSerializer,
} from '../types';

export type AccountWithPaddingAndNestedStruct =
  Account<AccountWithPaddingAndNestedStructAccountData>;

export type AccountWithPaddingAndNestedStructAccountData = {
  authority: PublicKey;
  count: number;
  padding: Array<number>;
  config: PaddedConfig;
};

export type AccountWithPaddingAndNestedStructAccountDataArgs = {
  authority: PublicKey;
  count: number;
  config: PaddedConfigArgs;
};

export function getAccountWithPaddingAndNestedStructAccountDataSerializer(): Serializer<
  AccountWithPaddingAndNestedStructAccountDataArgs,
  AccountWithPaddingAndNestedStructAccountData
> {
  return mapSerializer<
    AccountWithPaddingAndNestedStructAccountDataArgs,
    any,
    AccountWithPaddingAndNestedStructAccountData
  >(
    struct<AccountWithPaddingAndNestedStructAccountData>(
      [
        ['authority', publicKeySerializer()],
        ['count', u8()],
        ['padding', array(u8(), { size: 7 })],
        ['config', getPaddedConfigSerializer()],
      ],
      { description: 'AccountWithPaddingAndNestedStructAccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    AccountWithPaddingAndNestedStructAccountDataArgs,
    AccountWithPaddingAndNestedStructAccountData
  >;
}

export function deserializeAccountWithPaddingAndNestedStruct(
  rawAccount: RpcAccount
): AccountWithPaddingAndNestedStruct {
  return deserializeAccount(
    rawAccount,
    getAccountWithPaddingAndNestedStructAccountDataSerializer()
  );
}

export async function fetchAccountWithPaddingAndNestedStruct(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccountWithPaddingAndNestedStruct> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AccountWithPaddingAndNestedStruct');
  return deserializeAccountWithPaddingAndNestedStruct(maybeAccount);
}

export async function safeFetchAccountWithPaddingAndNestedStruct(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccountWithPaddingAndNestedStruct | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeAccountWithPaddingAndNestedStruct(maybeAccount)
    : null;
}

export async function fetchAllAccountWithPaddingAndNestedStruct(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccountWithPaddingAndNestedStruct[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AccountWithPaddingAndNestedStruct');
    return deserializeAccountWithPaddingAndNestedStruct(maybeAccount);
  });
}

export async function safeFetchAllAccountWithPaddingAndNestedStruct(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccountWithPaddingAndNestedStruct[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeAccountWithPaddingAndNestedStruct(maybeAccount as RpcAccount)
    );
}

export function getAccountWithPaddingAndNestedStructGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'paddingTests',
    'PadTest11111111111111111111111111111111111'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      authority: PublicKey;
      count: number;
      padding: Array<number>;
      config: PaddedConfigArgs;
      'config.maxItems': number;
      'config.authority': PublicKey;
    }>({
      authority: [0, publicKeySerializer()],
      count: [32, u8()],
      padding: [33, array(u8(), { size: 7 })],
      config: [40, getPaddedConfigSerializer()],
      'config.maxItems': [40, u32()],
      'config.authority': [48, publicKeySerializer()],
    })
    .deserializeUsing<AccountWithPaddingAndNestedStruct>((account) =>
      deserializeAccountWithPaddingAndNestedStruct(account)
    );
}

export function getAccountWithPaddingAndNestedStructSize(): number {
  return 80;
}
