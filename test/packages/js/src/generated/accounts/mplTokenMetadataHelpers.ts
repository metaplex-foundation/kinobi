/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountsOptions,
  assertAccountExists,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import { TmKey, getTmKeySerializer } from '../types';
import {
  CollectionAuthorityRecord,
  deserializeCollectionAuthorityRecord,
} from './collectionAuthorityRecord';
import { DelegateRecord, deserializeDelegateRecord } from './delegateRecord';
import { Edition, deserializeEdition } from './edition';
import { EditionMarker, deserializeEditionMarker } from './editionMarker';
import {
  TokenOwnedEscrow,
  deserializeTokenOwnedEscrow,
} from './tokenOwnedEscrow';
import { MasterEditionV2, deserializeMasterEditionV2 } from './masterEditionV2';
import { MasterEditionV1, deserializeMasterEditionV1 } from './masterEditionV1';
import { Metadata, deserializeMetadata } from './metadata';
import {
  ReservationListV2,
  deserializeReservationListV2,
} from './reservationListV2';
import {
  ReservationListV1,
  deserializeReservationListV1,
} from './reservationListV1';
import {
  UseAuthorityRecord,
  deserializeUseAuthorityRecord,
} from './useAuthorityRecord';

export type MplTokenMetadataAccount =
  | CollectionAuthorityRecord
  | DelegateRecord
  | Edition
  | EditionMarker
  | TokenOwnedEscrow
  | MasterEditionV2
  | MasterEditionV1
  | Metadata
  | ReservationListV2
  | ReservationListV1
  | UseAuthorityRecord;

function accountDataMatches(
  data: Uint8Array,
  expected: Uint8Array,
  offset: number
): boolean {
  if (data.length < offset + expected.length) return false;
  for (let i = 0; i < expected.length; i++) {
    if (data[offset + i] !== expected[i]) return false;
  }
  return true;
}

export function deserializeMplTokenMetadataAccount(
  rawAccount: RpcAccount
): MplTokenMetadataAccount {
  const data = rawAccount.data;
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.CollectionAuthorityRecord),
      0
    )
  ) {
    return deserializeCollectionAuthorityRecord(rawAccount);
  }
  if (
    accountDataMatches(data, getTmKeySerializer().serialize(TmKey.Delegate), 0)
  ) {
    return deserializeDelegateRecord(rawAccount);
  }
  if (
    accountDataMatches(data, getTmKeySerializer().serialize(TmKey.EditionV1), 0)
  ) {
    return deserializeEdition(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.EditionMarker),
      0
    )
  ) {
    return deserializeEditionMarker(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.TokenOwnedEscrow),
      0
    )
  ) {
    return deserializeTokenOwnedEscrow(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.MasterEditionV2),
      0
    )
  ) {
    return deserializeMasterEditionV2(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.MasterEditionV1),
      0
    )
  ) {
    return deserializeMasterEditionV1(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.MetadataV1),
      0
    )
  ) {
    return deserializeMetadata(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.ReservationListV2),
      0
    )
  ) {
    return deserializeReservationListV2(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.ReservationListV1),
      0
    )
  ) {
    return deserializeReservationListV1(rawAccount);
  }
  if (
    accountDataMatches(
      data,
      getTmKeySerializer().serialize(TmKey.UseAuthorityRecord),
      0
    )
  ) {
    return deserializeUseAuthorityRecord(rawAccount);
  }
  throw new Error(
    'The provided account could not be identified as a mplTokenMetadata account.'
  );
}

export async function fetchAllMplTokenMetadataAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<MplTokenMetadataAccount[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount);
    return deserializeMplTokenMetadataAccount(maybeAccount);
  });
}

export async function safeFetchAllMplTokenMetadataAccounts(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<(MplTokenMetadataAccount | null)[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    if (!maybeAccount.exists) return null;
    return deserializeMplTokenMetadataAccount(maybeAccount);
  });
}
