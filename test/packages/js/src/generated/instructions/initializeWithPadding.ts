/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeWithPaddingInstructionAccounts = {
  target: PublicKey | Pda;
  payer?: Signer;
};

// Data.
export type InitializeWithPaddingInstructionData = {
  discriminator: number;
  count: number;
  padding: Array<number>;
};

export type InitializeWithPaddingInstructionDataArgs = { count: number };

export function getInitializeWithPaddingInstructionDataSerializer(): Serializer<
  InitializeWithPaddingInstructionDataArgs,
  InitializeWithPaddingInstructionData
> {
  return mapSerializer<
    InitializeWithPaddingInstructionDataArgs,
    any,
    InitializeWithPaddingInstructionData
  >(
    struct<InitializeWithPaddingInstructionData>(
      [
        ['discriminator', u8()],
        ['count', u32()],
        ['padding', array(u8(), { size: 4 })],
      ],
      { description: 'InitializeWithPaddingInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 0, padding: [0, 0, 0, 0] })
  ) as Serializer<
    InitializeWithPaddingInstructionDataArgs,
    InitializeWithPaddingInstructionData
  >;
}

// Args.
export type InitializeWithPaddingInstructionArgs =
  InitializeWithPaddingInstructionDataArgs;

// Args-only serializer (excludes discriminator fields).
export function getInitializeWithPaddingInstructionArgsOnlySerializer(): Serializer<
  any,
  any
> {
  return mapSerializer<any, any, any>(
    struct<any>([
      ['count', u32()],
      ['padding', array(u8(), { size: 4 })],
    ]),
    (value) => ({ ...value, padding: [0, 0, 0, 0] })
  ) as Serializer<any, any>;
}

// Instruction discriminator.
export const initializeWithPaddingInstructionDiscriminator = 0;

// Instruction.
export function initializeWithPadding(
  context: Pick<Context, 'payer' | 'programs'>,
  input: InitializeWithPaddingInstructionAccounts &
    InitializeWithPaddingInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'paddingTests',
    'PadTest11111111111111111111111111111111111'
  );

  // Accounts.
  const resolvedAccounts = {
    target: {
      index: 0,
      isWritable: true as boolean,
      value: input.target ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeWithPaddingInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeWithPaddingInstructionDataSerializer().serialize(
    resolvedArgs as InitializeWithPaddingInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
