/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  WritableAccount,
  WritableSignerAccount,
} from '@solana/instructions';
import { IAccountSignerMeta, TransactionSigner } from '@solana/signers';
import { PADDING_TESTS_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type InitializeWithPaddingInstruction<
  TProgram extends string = typeof PADDING_TESTS_PROGRAM_ADDRESS,
  TAccountTarget extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountTarget extends string
        ? WritableAccount<TAccountTarget>
        : TAccountTarget,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeWithPaddingInstructionData = {
  discriminator: number;
  count: number;
  padding: Array<number>;
};

export type InitializeWithPaddingInstructionDataArgs = { count: number };

export function getInitializeWithPaddingInstructionDataEncoder(): Encoder<InitializeWithPaddingInstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['count', getU32Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 4 })],
    ]),
    (value) => ({ ...value, discriminator: 0, padding: [0, 0, 0, 0] })
  );
}

export function getInitializeWithPaddingInstructionDataDecoder(): Decoder<InitializeWithPaddingInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['count', getU32Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 4 })],
  ]);
}

export function getInitializeWithPaddingInstructionDataCodec(): Codec<
  InitializeWithPaddingInstructionDataArgs,
  InitializeWithPaddingInstructionData
> {
  return combineCodec(
    getInitializeWithPaddingInstructionDataEncoder(),
    getInitializeWithPaddingInstructionDataDecoder()
  );
}

export type InitializeWithPaddingInput<
  TAccountTarget extends string = string,
  TAccountPayer extends string = string,
> = {
  target: Address<TAccountTarget>;
  payer: TransactionSigner<TAccountPayer>;
  count: InitializeWithPaddingInstructionDataArgs['count'];
};

export function getInitializeWithPaddingInstruction<
  TAccountTarget extends string,
  TAccountPayer extends string,
>(
  input: InitializeWithPaddingInput<TAccountTarget, TAccountPayer>
): InitializeWithPaddingInstruction<
  typeof PADDING_TESTS_PROGRAM_ADDRESS,
  TAccountTarget,
  TAccountPayer
> {
  // Program address.
  const programAddress = PADDING_TESTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    target: { value: input.target ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.target), getAccountMeta(accounts.payer)],
    programAddress,
    data: getInitializeWithPaddingInstructionDataEncoder().encode(
      args as InitializeWithPaddingInstructionDataArgs
    ),
  } as InitializeWithPaddingInstruction<
    typeof PADDING_TESTS_PROGRAM_ADDRESS,
    TAccountTarget,
    TAccountPayer
  >;

  return instruction;
}

export type ParsedInitializeWithPaddingInstruction<
  TProgram extends string = typeof PADDING_TESTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    target: TAccountMetas[0];
    payer: TAccountMetas[1];
  };
  data: InitializeWithPaddingInstructionData;
};

export function parseInitializeWithPaddingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedInitializeWithPaddingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      target: getNextAccount(),
      payer: getNextAccount(),
    },
    data: getInitializeWithPaddingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
