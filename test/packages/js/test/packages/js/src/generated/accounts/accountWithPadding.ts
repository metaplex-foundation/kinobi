/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type AccountWithPadding = Account<AccountWithPaddingAccountData>;

export type AccountWithPaddingAccountData = {
  authority: PublicKey;
  count: number;
  padding: Array<number>;
  value: bigint;
};

export type AccountWithPaddingAccountDataArgs = {
  authority: PublicKey;
  count: number;
  value: number | bigint;
};

export function getAccountWithPaddingAccountDataSerializer(): Serializer<
  AccountWithPaddingAccountDataArgs,
  AccountWithPaddingAccountData
> {
  return mapSerializer<
    AccountWithPaddingAccountDataArgs,
    any,
    AccountWithPaddingAccountData
  >(
    struct<AccountWithPaddingAccountData>(
      [
        ['authority', publicKeySerializer()],
        ['count', u8()],
        ['padding', array(u8(), { size: 3 })],
        ['value', u64()],
      ],
      { description: 'AccountWithPaddingAccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0] })
  ) as Serializer<
    AccountWithPaddingAccountDataArgs,
    AccountWithPaddingAccountData
  >;
}

export function deserializeAccountWithPadding(
  rawAccount: RpcAccount
): AccountWithPadding {
  return deserializeAccount(
    rawAccount,
    getAccountWithPaddingAccountDataSerializer()
  );
}

export async function fetchAccountWithPadding(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccountWithPadding> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AccountWithPadding');
  return deserializeAccountWithPadding(maybeAccount);
}

export async function safeFetchAccountWithPadding(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccountWithPadding | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeAccountWithPadding(maybeAccount)
    : null;
}

export async function fetchAllAccountWithPadding(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccountWithPadding[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AccountWithPadding');
    return deserializeAccountWithPadding(maybeAccount);
  });
}

export async function safeFetchAllAccountWithPadding(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccountWithPadding[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeAccountWithPadding(maybeAccount as RpcAccount)
    );
}

export function getAccountWithPaddingGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'paddingTests',
    'PadTest11111111111111111111111111111111111'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      authority: PublicKey;
      count: number;
      padding: Array<number>;
      value: number | bigint;
    }>({
      authority: [0, publicKeySerializer()],
      count: [32, u8()],
      padding: [33, array(u8(), { size: 3 })],
      value: [36, u64()],
    })
    .deserializeUsing<AccountWithPadding>((account) =>
      deserializeAccountWithPadding(account)
    );
}

export function getAccountWithPaddingSize(): number {
  return 44;
}
