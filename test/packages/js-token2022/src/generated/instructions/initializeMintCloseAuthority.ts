/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeMintCloseAuthorityInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeMintCloseAuthorityInstructionData = {
  discriminator: number;
  /** Authority that must sign the `CloseAccount` instruction on a mint. */
  closeAuthority: Option<PublicKey>;
};

export type InitializeMintCloseAuthorityInstructionDataArgs = {
  /** Authority that must sign the `CloseAccount` instruction on a mint. */
  closeAuthority: OptionOrNullable<PublicKey>;
};

export function getInitializeMintCloseAuthorityInstructionDataSerializer(): Serializer<
  InitializeMintCloseAuthorityInstructionDataArgs,
  InitializeMintCloseAuthorityInstructionData
> {
  return mapSerializer<
    InitializeMintCloseAuthorityInstructionDataArgs,
    any,
    InitializeMintCloseAuthorityInstructionData
  >(
    struct<InitializeMintCloseAuthorityInstructionData>(
      [
        ['discriminator', u8()],
        ['closeAuthority', option(publicKeySerializer())],
      ],
      { description: 'InitializeMintCloseAuthorityInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 25 })
  ) as Serializer<
    InitializeMintCloseAuthorityInstructionDataArgs,
    InitializeMintCloseAuthorityInstructionData
  >;
}

// Args.
export type InitializeMintCloseAuthorityInstructionArgs =
  InitializeMintCloseAuthorityInstructionDataArgs;

// Instruction.
export function initializeMintCloseAuthority(
  context: Pick<Context, 'programs'>,
  input: InitializeMintCloseAuthorityInstructionAccounts &
    InitializeMintCloseAuthorityInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeMintCloseAuthorityInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeMintCloseAuthorityInstructionDataSerializer().serialize(
      resolvedArgs as InitializeMintCloseAuthorityInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
