/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DisableMemoTransfersInstructionAccounts = {
  /** The token account to update. */
  token: PublicKey | Pda;
  /** The account's owner or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type DisableMemoTransfersInstructionData = {
  discriminator: number;
  memoTransfersDiscriminator: number;
};

export type DisableMemoTransfersInstructionDataArgs = {};

export function getDisableMemoTransfersInstructionDataSerializer(): Serializer<
  DisableMemoTransfersInstructionDataArgs,
  DisableMemoTransfersInstructionData
> {
  return mapSerializer<
    DisableMemoTransfersInstructionDataArgs,
    any,
    DisableMemoTransfersInstructionData
  >(
    struct<DisableMemoTransfersInstructionData>(
      [
        ['discriminator', u8()],
        ['memoTransfersDiscriminator', u8()],
      ],
      { description: 'DisableMemoTransfersInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 30, memoTransfersDiscriminator: 1 })
  ) as Serializer<
    DisableMemoTransfersInstructionDataArgs,
    DisableMemoTransfersInstructionData
  >;
}

// Instruction.
export function disableMemoTransfers(
  context: Pick<Context, 'programs'>,
  input: DisableMemoTransfersInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    owner: {
      index: 1,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDisableMemoTransfersInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
