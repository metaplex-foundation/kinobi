/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeMetadataPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeMetadataPointerInstructionData = {
  discriminator: number;
  metadataPointerDiscriminator: number;
  /** The public key for the account that can update the metadata address. */
  authority: Option<PublicKey>;
  /** The account address that holds the metadata. */
  metadataAddress: Option<PublicKey>;
};

export type InitializeMetadataPointerInstructionDataArgs = {
  /** The public key for the account that can update the metadata address. */
  authority: OptionOrNullable<PublicKey>;
  /** The account address that holds the metadata. */
  metadataAddress: OptionOrNullable<PublicKey>;
};

export function getInitializeMetadataPointerInstructionDataSerializer(): Serializer<
  InitializeMetadataPointerInstructionDataArgs,
  InitializeMetadataPointerInstructionData
> {
  return mapSerializer<
    InitializeMetadataPointerInstructionDataArgs,
    any,
    InitializeMetadataPointerInstructionData
  >(
    struct<InitializeMetadataPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['metadataPointerDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
        ['metadataAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializeMetadataPointerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 39,
      metadataPointerDiscriminator: 0,
    })
  ) as Serializer<
    InitializeMetadataPointerInstructionDataArgs,
    InitializeMetadataPointerInstructionData
  >;
}

// Args.
export type InitializeMetadataPointerInstructionArgs =
  InitializeMetadataPointerInstructionDataArgs;

// Instruction.
export function initializeMetadataPointer(
  context: Pick<Context, 'programs'>,
  input: InitializeMetadataPointerInstructionAccounts &
    InitializeMetadataPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeMetadataPointerInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeMetadataPointerInstructionDataSerializer().serialize(
      resolvedArgs as InitializeMetadataPointerInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
