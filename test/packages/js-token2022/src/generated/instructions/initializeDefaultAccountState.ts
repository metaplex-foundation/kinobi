/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AccountState,
  AccountStateArgs,
  getAccountStateSerializer,
} from '../types';

// Accounts.
export type InitializeDefaultAccountStateInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeDefaultAccountStateInstructionData = {
  discriminator: number;
  defaultAccountStateDiscriminator: number;
  /** The state each new token account should start with. */
  state: AccountState;
};

export type InitializeDefaultAccountStateInstructionDataArgs = {
  /** The state each new token account should start with. */
  state: AccountStateArgs;
};

export function getInitializeDefaultAccountStateInstructionDataSerializer(): Serializer<
  InitializeDefaultAccountStateInstructionDataArgs,
  InitializeDefaultAccountStateInstructionData
> {
  return mapSerializer<
    InitializeDefaultAccountStateInstructionDataArgs,
    any,
    InitializeDefaultAccountStateInstructionData
  >(
    struct<InitializeDefaultAccountStateInstructionData>(
      [
        ['discriminator', u8()],
        ['defaultAccountStateDiscriminator', u8()],
        ['state', getAccountStateSerializer()],
      ],
      { description: 'InitializeDefaultAccountStateInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 28,
      defaultAccountStateDiscriminator: 0,
    })
  ) as Serializer<
    InitializeDefaultAccountStateInstructionDataArgs,
    InitializeDefaultAccountStateInstructionData
  >;
}

// Args.
export type InitializeDefaultAccountStateInstructionArgs =
  InitializeDefaultAccountStateInstructionDataArgs;

// Instruction.
export function initializeDefaultAccountState(
  context: Pick<Context, 'programs'>,
  input: InitializeDefaultAccountStateInstructionAccounts &
    InitializeDefaultAccountStateInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeDefaultAccountStateInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeDefaultAccountStateInstructionDataSerializer().serialize(
      resolvedArgs as InitializeDefaultAccountStateInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
