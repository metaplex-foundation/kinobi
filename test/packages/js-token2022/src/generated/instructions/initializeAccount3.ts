/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeAccount3InstructionAccounts = {
  /** The account to initialize. */
  account: PublicKey | Pda;
  /** The mint this account will be associated with. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeAccount3InstructionData = {
  discriminator: number;
  /** The new account's owner/multisignature. */
  owner: PublicKey;
};

export type InitializeAccount3InstructionDataArgs = {
  /** The new account's owner/multisignature. */
  owner: PublicKey;
};

export function getInitializeAccount3InstructionDataSerializer(): Serializer<
  InitializeAccount3InstructionDataArgs,
  InitializeAccount3InstructionData
> {
  return mapSerializer<
    InitializeAccount3InstructionDataArgs,
    any,
    InitializeAccount3InstructionData
  >(
    struct<InitializeAccount3InstructionData>(
      [
        ['discriminator', u8()],
        ['owner', publicKeySerializer()],
      ],
      { description: 'InitializeAccount3InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 18 })
  ) as Serializer<
    InitializeAccount3InstructionDataArgs,
    InitializeAccount3InstructionData
  >;
}

// Args.
export type InitializeAccount3InstructionArgs =
  InitializeAccount3InstructionDataArgs;

// Instruction.
export function initializeAccount3(
  context: Pick<Context, 'programs'>,
  input: InitializeAccount3InstructionAccounts &
    InitializeAccount3InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    account: {
      index: 0,
      isWritable: true as boolean,
      value: input.account ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeAccount3InstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeAccount3InstructionDataSerializer().serialize(
    resolvedArgs as InitializeAccount3InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
