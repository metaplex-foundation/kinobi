/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializePermissionedBurnInstructionAccounts = {
  /** The mint account to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializePermissionedBurnInstructionData = {
  discriminator: number;
  permissionedBurnDiscriminator: number;
  /** The public key for the account that is required for token burning. */
  authority: PublicKey;
};

export type InitializePermissionedBurnInstructionDataArgs = {
  /** The public key for the account that is required for token burning. */
  authority: PublicKey;
};

export function getInitializePermissionedBurnInstructionDataSerializer(): Serializer<
  InitializePermissionedBurnInstructionDataArgs,
  InitializePermissionedBurnInstructionData
> {
  return mapSerializer<
    InitializePermissionedBurnInstructionDataArgs,
    any,
    InitializePermissionedBurnInstructionData
  >(
    struct<InitializePermissionedBurnInstructionData>(
      [
        ['discriminator', u8()],
        ['permissionedBurnDiscriminator', u8()],
        ['authority', publicKeySerializer()],
      ],
      { description: 'InitializePermissionedBurnInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 46,
      permissionedBurnDiscriminator: 0,
    })
  ) as Serializer<
    InitializePermissionedBurnInstructionDataArgs,
    InitializePermissionedBurnInstructionData
  >;
}

// Args.
export type InitializePermissionedBurnInstructionArgs =
  InitializePermissionedBurnInstructionDataArgs;

// Instruction.
export function initializePermissionedBurn(
  context: Pick<Context, 'programs'>,
  input: InitializePermissionedBurnInstructionAccounts &
    InitializePermissionedBurnInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializePermissionedBurnInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializePermissionedBurnInstructionDataSerializer().serialize(
      resolvedArgs as InitializePermissionedBurnInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
