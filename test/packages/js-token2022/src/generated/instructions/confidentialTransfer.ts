/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  EncryptedBalance,
  EncryptedBalanceArgs,
  getDecryptableBalanceSerializer,
  getEncryptedBalanceSerializer,
} from '../types';

// Accounts.
export type ConfidentialTransferInstructionAccounts = {
  /** The source SPL Token account. */
  sourceToken: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /** The destination SPL Token account. */
  destinationToken: PublicKey | Pda;
  /**
   * (Optional) Instructions sysvar if at least one of the
   * `zk_elgamal_proof` instructions are included in the same
   * transaction.
   */

  instructionsSysvar?: PublicKey | Pda;
  /** (Optional) Equality proof record account or context state account. */
  equalityRecord?: PublicKey | Pda;
  /** (Optional) Ciphertext validity proof record account or context state account. */
  ciphertextValidityRecord?: PublicKey | Pda;
  /** (Optional) Range proof record account or context state account. */
  rangeRecord?: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfidentialTransferInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /** The new source decryptable balance if the transfer succeeds. */
  newSourceDecryptableAvailableBalance: DecryptableBalance;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextLo: EncryptedBalance;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextHi: EncryptedBalance;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `Transfer` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `Transfer` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  ciphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU128Data`
   * instruction to the `Transfer` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export type ConfidentialTransferInstructionDataArgs = {
  /** The new source decryptable balance if the transfer succeeds. */
  newSourceDecryptableAvailableBalance: DecryptableBalanceArgs;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextLo: EncryptedBalanceArgs;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextHi: EncryptedBalanceArgs;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `Transfer` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `Transfer` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  ciphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU128Data`
   * instruction to the `Transfer` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export function getConfidentialTransferInstructionDataSerializer(): Serializer<
  ConfidentialTransferInstructionDataArgs,
  ConfidentialTransferInstructionData
> {
  return mapSerializer<
    ConfidentialTransferInstructionDataArgs,
    any,
    ConfidentialTransferInstructionData
  >(
    struct<ConfidentialTransferInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        [
          'newSourceDecryptableAvailableBalance',
          getDecryptableBalanceSerializer(),
        ],
        ['transferAmountAuditorCiphertextLo', getEncryptedBalanceSerializer()],
        ['transferAmountAuditorCiphertextHi', getEncryptedBalanceSerializer()],
        ['equalityProofInstructionOffset', i8()],
        ['ciphertextValidityProofInstructionOffset', i8()],
        ['rangeProofInstructionOffset', i8()],
      ],
      { description: 'ConfidentialTransferInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 7,
    })
  ) as Serializer<
    ConfidentialTransferInstructionDataArgs,
    ConfidentialTransferInstructionData
  >;
}

// Args.
export type ConfidentialTransferInstructionArgs =
  ConfidentialTransferInstructionDataArgs;

// Instruction.
export function confidentialTransfer(
  context: Pick<Context, 'programs'>,
  input: ConfidentialTransferInstructionAccounts &
    ConfidentialTransferInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    sourceToken: {
      index: 0,
      isWritable: true as boolean,
      value: input.sourceToken ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    destinationToken: {
      index: 2,
      isWritable: true as boolean,
      value: input.destinationToken ?? null,
    },
    instructionsSysvar: {
      index: 3,
      isWritable: false as boolean,
      value: input.instructionsSysvar ?? null,
    },
    equalityRecord: {
      index: 4,
      isWritable: false as boolean,
      value: input.equalityRecord ?? null,
    },
    ciphertextValidityRecord: {
      index: 5,
      isWritable: false as boolean,
      value: input.ciphertextValidityRecord ?? null,
    },
    rangeRecord: {
      index: 6,
      isWritable: false as boolean,
      value: input.rangeRecord ?? null,
    },
    authority: {
      index: 7,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfidentialTransferInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'omitted',
    programId
  );

  // Data.
  const data = getConfidentialTransferInstructionDataSerializer().serialize(
    resolvedArgs as ConfidentialTransferInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
