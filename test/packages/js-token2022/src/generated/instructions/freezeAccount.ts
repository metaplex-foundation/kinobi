/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type FreezeAccountInstructionAccounts = {
  /** The account to freeze. */
  account: PublicKey | Pda;
  /** The token mint. */
  mint: PublicKey | Pda;
  /** The mint freeze authority or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type FreezeAccountInstructionData = { discriminator: number };

export type FreezeAccountInstructionDataArgs = {};

export function getFreezeAccountInstructionDataSerializer(): Serializer<
  FreezeAccountInstructionDataArgs,
  FreezeAccountInstructionData
> {
  return mapSerializer<
    FreezeAccountInstructionDataArgs,
    any,
    FreezeAccountInstructionData
  >(
    struct<FreezeAccountInstructionData>([['discriminator', u8()]], {
      description: 'FreezeAccountInstructionData',
    }),
    (value) => ({ ...value, discriminator: 10 })
  ) as Serializer<
    FreezeAccountInstructionDataArgs,
    FreezeAccountInstructionData
  >;
}

// Instruction.
export function freezeAccount(
  context: Pick<Context, 'programs'>,
  input: FreezeAccountInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    account: {
      index: 0,
      isWritable: true as boolean,
      value: input.account ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    owner: {
      index: 2,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFreezeAccountInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
