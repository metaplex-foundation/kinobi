/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DisableHarvestToMintInstructionAccounts = {
  /** The token mint. */
  mint: PublicKey | Pda;
  /** The confidential transfer fee authority */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type DisableHarvestToMintInstructionData = {
  discriminator: number;
  confidentialTransferFeeDiscriminator: number;
};

export type DisableHarvestToMintInstructionDataArgs = {};

export function getDisableHarvestToMintInstructionDataSerializer(): Serializer<
  DisableHarvestToMintInstructionDataArgs,
  DisableHarvestToMintInstructionData
> {
  return mapSerializer<
    DisableHarvestToMintInstructionDataArgs,
    any,
    DisableHarvestToMintInstructionData
  >(
    struct<DisableHarvestToMintInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferFeeDiscriminator', u8()],
      ],
      { description: 'DisableHarvestToMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 37,
      confidentialTransferFeeDiscriminator: 5,
    })
  ) as Serializer<
    DisableHarvestToMintInstructionDataArgs,
    DisableHarvestToMintInstructionData
  >;
}

// Instruction.
export function disableHarvestToMint(
  context: Pick<Context, 'programs'>,
  input: DisableHarvestToMintInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDisableHarvestToMintInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
