/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type UpdateConfidentialTransferMintInstructionAccounts = {
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
  /** Confidential transfer mint authority. */
  authority: Signer;
};

// Data.
export type UpdateConfidentialTransferMintInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /**
   * Determines if newly configured accounts must be approved by the
   * `authority` before they may be used by the user.
   */
  autoApproveNewAccounts: boolean;
  /** New authority to decode any transfer amount in a confidential transfer. */
  auditorElgamalPubkey: Option<PublicKey>;
};

export type UpdateConfidentialTransferMintInstructionDataArgs = {
  /**
   * Determines if newly configured accounts must be approved by the
   * `authority` before they may be used by the user.
   */
  autoApproveNewAccounts: boolean;
  /** New authority to decode any transfer amount in a confidential transfer. */
  auditorElgamalPubkey: OptionOrNullable<PublicKey>;
};

export function getUpdateConfidentialTransferMintInstructionDataSerializer(): Serializer<
  UpdateConfidentialTransferMintInstructionDataArgs,
  UpdateConfidentialTransferMintInstructionData
> {
  return mapSerializer<
    UpdateConfidentialTransferMintInstructionDataArgs,
    any,
    UpdateConfidentialTransferMintInstructionData
  >(
    struct<UpdateConfidentialTransferMintInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['autoApproveNewAccounts', bool()],
        ['auditorElgamalPubkey', zeroableOption(publicKeySerializer())],
      ],
      { description: 'UpdateConfidentialTransferMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 1,
    })
  ) as Serializer<
    UpdateConfidentialTransferMintInstructionDataArgs,
    UpdateConfidentialTransferMintInstructionData
  >;
}

// Args.
export type UpdateConfidentialTransferMintInstructionArgs =
  UpdateConfidentialTransferMintInstructionDataArgs;

// Instruction.
export function updateConfidentialTransferMint(
  context: Pick<Context, 'programs'>,
  input: UpdateConfidentialTransferMintInstructionAccounts &
    UpdateConfidentialTransferMintInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateConfidentialTransferMintInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getUpdateConfidentialTransferMintInstructionDataSerializer().serialize(
      resolvedArgs as UpdateConfidentialTransferMintInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
