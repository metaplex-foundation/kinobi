/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ConfidentialDepositInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfidentialDepositInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /** The amount of tokens to deposit. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export type ConfidentialDepositInstructionDataArgs = {
  /** The amount of tokens to deposit. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export function getConfidentialDepositInstructionDataSerializer(): Serializer<
  ConfidentialDepositInstructionDataArgs,
  ConfidentialDepositInstructionData
> {
  return mapSerializer<
    ConfidentialDepositInstructionDataArgs,
    any,
    ConfidentialDepositInstructionData
  >(
    struct<ConfidentialDepositInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
      ],
      { description: 'ConfidentialDepositInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 5,
    })
  ) as Serializer<
    ConfidentialDepositInstructionDataArgs,
    ConfidentialDepositInstructionData
  >;
}

// Args.
export type ConfidentialDepositInstructionArgs =
  ConfidentialDepositInstructionDataArgs;

// Instruction.
export function confidentialDeposit(
  context: Pick<Context, 'programs'>,
  input: ConfidentialDepositInstructionAccounts &
    ConfidentialDepositInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfidentialDepositInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getConfidentialDepositInstructionDataSerializer().serialize(
    resolvedArgs as ConfidentialDepositInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
