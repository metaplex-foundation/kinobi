/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateTokenGroupMaxSizeInstructionAccounts = {
  group: PublicKey | Pda;
  updateAuthority: Signer;
};

// Data.
export type UpdateTokenGroupMaxSizeInstructionData = {
  discriminator: Uint8Array;
  /** New max size for the group */
  maxSize: bigint;
};

export type UpdateTokenGroupMaxSizeInstructionDataArgs = {
  /** New max size for the group */
  maxSize: number | bigint;
};

export function getUpdateTokenGroupMaxSizeInstructionDataSerializer(): Serializer<
  UpdateTokenGroupMaxSizeInstructionDataArgs,
  UpdateTokenGroupMaxSizeInstructionData
> {
  return mapSerializer<
    UpdateTokenGroupMaxSizeInstructionDataArgs,
    any,
    UpdateTokenGroupMaxSizeInstructionData
  >(
    struct<UpdateTokenGroupMaxSizeInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['maxSize', u64()],
      ],
      { description: 'UpdateTokenGroupMaxSizeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 37, 171, 143, 248, 30, 18, 110]),
    })
  ) as Serializer<
    UpdateTokenGroupMaxSizeInstructionDataArgs,
    UpdateTokenGroupMaxSizeInstructionData
  >;
}

// Args.
export type UpdateTokenGroupMaxSizeInstructionArgs =
  UpdateTokenGroupMaxSizeInstructionDataArgs;

// Instruction.
export function updateTokenGroupMaxSize(
  context: Pick<Context, 'programs'>,
  input: UpdateTokenGroupMaxSizeInstructionAccounts &
    UpdateTokenGroupMaxSizeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    updateAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.updateAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateTokenGroupMaxSizeInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateTokenGroupMaxSizeInstructionDataSerializer().serialize(
    resolvedArgs as UpdateTokenGroupMaxSizeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
