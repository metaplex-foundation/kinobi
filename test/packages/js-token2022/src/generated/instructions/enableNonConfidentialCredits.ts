/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type EnableNonConfidentialCreditsInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type EnableNonConfidentialCreditsInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
};

export type EnableNonConfidentialCreditsInstructionDataArgs = {};

export function getEnableNonConfidentialCreditsInstructionDataSerializer(): Serializer<
  EnableNonConfidentialCreditsInstructionDataArgs,
  EnableNonConfidentialCreditsInstructionData
> {
  return mapSerializer<
    EnableNonConfidentialCreditsInstructionDataArgs,
    any,
    EnableNonConfidentialCreditsInstructionData
  >(
    struct<EnableNonConfidentialCreditsInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
      ],
      { description: 'EnableNonConfidentialCreditsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 11,
    })
  ) as Serializer<
    EnableNonConfidentialCreditsInstructionDataArgs,
    EnableNonConfidentialCreditsInstructionData
  >;
}

// Instruction.
export function enableNonConfidentialCredits(
  context: Pick<Context, 'programs'>,
  input: EnableNonConfidentialCreditsInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getEnableNonConfidentialCreditsInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
