/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseAccountInstructionAccounts = {
  /** The account to close. */
  account: PublicKey | Pda;
  /** The destination account. */
  destination: PublicKey | Pda;
  /** The account's owner or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type CloseAccountInstructionData = { discriminator: number };

export type CloseAccountInstructionDataArgs = {};

export function getCloseAccountInstructionDataSerializer(): Serializer<
  CloseAccountInstructionDataArgs,
  CloseAccountInstructionData
> {
  return mapSerializer<
    CloseAccountInstructionDataArgs,
    any,
    CloseAccountInstructionData
  >(
    struct<CloseAccountInstructionData>([['discriminator', u8()]], {
      description: 'CloseAccountInstructionData',
    }),
    (value) => ({ ...value, discriminator: 9 })
  ) as Serializer<CloseAccountInstructionDataArgs, CloseAccountInstructionData>;
}

// Instruction.
export function closeAccount(
  context: Pick<Context, 'programs'>,
  input: CloseAccountInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    account: {
      index: 0,
      isWritable: true as boolean,
      value: input.account ?? null,
    },
    destination: {
      index: 1,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    owner: {
      index: 2,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseAccountInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
