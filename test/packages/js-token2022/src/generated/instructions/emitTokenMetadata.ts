/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  none,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  option,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type EmitTokenMetadataInstructionAccounts = {
  metadata: PublicKey | Pda;
};

// Data.
export type EmitTokenMetadataInstructionData = {
  discriminator: Uint8Array;
  /** Start of range of data to emit */
  start: Option<bigint>;
  /** End of range of data to emit */
  end: Option<bigint>;
};

export type EmitTokenMetadataInstructionDataArgs = {
  /** Start of range of data to emit */
  start?: OptionOrNullable<number | bigint>;
  /** End of range of data to emit */
  end?: OptionOrNullable<number | bigint>;
};

export function getEmitTokenMetadataInstructionDataSerializer(): Serializer<
  EmitTokenMetadataInstructionDataArgs,
  EmitTokenMetadataInstructionData
> {
  return mapSerializer<
    EmitTokenMetadataInstructionDataArgs,
    any,
    EmitTokenMetadataInstructionData
  >(
    struct<EmitTokenMetadataInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['start', option(u64())],
        ['end', option(u64())],
      ],
      { description: 'EmitTokenMetadataInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([250, 166, 180, 250, 13, 12, 184, 70]),
      start: value.start ?? none(),
      end: value.end ?? none(),
    })
  ) as Serializer<
    EmitTokenMetadataInstructionDataArgs,
    EmitTokenMetadataInstructionData
  >;
}

// Args.
export type EmitTokenMetadataInstructionArgs =
  EmitTokenMetadataInstructionDataArgs;

// Instruction.
export function emitTokenMetadata(
  context: Pick<Context, 'programs'>,
  input: EmitTokenMetadataInstructionAccounts & EmitTokenMetadataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    metadata: {
      index: 0,
      isWritable: false as boolean,
      value: input.metadata ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: EmitTokenMetadataInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getEmitTokenMetadataInstructionDataSerializer().serialize(
    resolvedArgs as EmitTokenMetadataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
