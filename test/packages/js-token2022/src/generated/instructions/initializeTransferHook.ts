/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeTransferHookInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeTransferHookInstructionData = {
  discriminator: number;
  transferHookDiscriminator: number;
  /** The public key for the account that can update the program id */
  authority: Option<PublicKey>;
  /** The program id that performs logic during transfers */
  programId: Option<PublicKey>;
};

export type InitializeTransferHookInstructionDataArgs = {
  /** The public key for the account that can update the program id */
  authority: OptionOrNullable<PublicKey>;
  /** The program id that performs logic during transfers */
  programId: OptionOrNullable<PublicKey>;
};

export function getInitializeTransferHookInstructionDataSerializer(): Serializer<
  InitializeTransferHookInstructionDataArgs,
  InitializeTransferHookInstructionData
> {
  return mapSerializer<
    InitializeTransferHookInstructionDataArgs,
    any,
    InitializeTransferHookInstructionData
  >(
    struct<InitializeTransferHookInstructionData>(
      [
        ['discriminator', u8()],
        ['transferHookDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
        ['programId', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializeTransferHookInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 36, transferHookDiscriminator: 0 })
  ) as Serializer<
    InitializeTransferHookInstructionDataArgs,
    InitializeTransferHookInstructionData
  >;
}

// Args.
export type InitializeTransferHookInstructionArgs =
  InitializeTransferHookInstructionDataArgs;

// Instruction.
export function initializeTransferHook(
  context: Pick<Context, 'programs'>,
  input: InitializeTransferHookInstructionAccounts &
    InitializeTransferHookInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeTransferHookInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeTransferHookInstructionDataSerializer().serialize(
    resolvedArgs as InitializeTransferHookInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
