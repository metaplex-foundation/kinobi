/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type TransferCheckedInstructionAccounts = {
  /** The source account. */
  source: PublicKey | Pda;
  /** The token mint. */
  mint: PublicKey | Pda;
  /** The destination account. */
  destination: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority?: PublicKey | Pda | Signer;
};

// Data.
export type TransferCheckedInstructionData = {
  discriminator: number;
  /** The amount of tokens to transfer. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export type TransferCheckedInstructionDataArgs = {
  /** The amount of tokens to transfer. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export function getTransferCheckedInstructionDataSerializer(): Serializer<
  TransferCheckedInstructionDataArgs,
  TransferCheckedInstructionData
> {
  return mapSerializer<
    TransferCheckedInstructionDataArgs,
    any,
    TransferCheckedInstructionData
  >(
    struct<TransferCheckedInstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
      ],
      { description: 'TransferCheckedInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 12 })
  ) as Serializer<
    TransferCheckedInstructionDataArgs,
    TransferCheckedInstructionData
  >;
}

// Args.
export type TransferCheckedInstructionArgs = TransferCheckedInstructionDataArgs;

// Instruction.
export function transferChecked(
  context: Pick<Context, 'identity' | 'programs'>,
  input: TransferCheckedInstructionAccounts & TransferCheckedInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    source: {
      index: 0,
      isWritable: true as boolean,
      value: input.source ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    destination: {
      index: 2,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TransferCheckedInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTransferCheckedInstructionDataSerializer().serialize(
    resolvedArgs as TransferCheckedInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
