/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  EncryptedBalance,
  EncryptedBalanceArgs,
  getDecryptableBalanceSerializer,
  getEncryptedBalanceSerializer,
} from '../types';

// Accounts.
export type ConfidentialTransferWithFeeInstructionAccounts = {
  /** The source SPL Token account. */
  sourceToken: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /** The destination SPL Token account. */
  destinationToken: PublicKey | Pda;
  /**
   * (Optional) Instructions sysvar if at least one of the
   * `zk_elgamal_proof` instructions are included in the same
   * transaction.
   */

  instructionsSysvar?: PublicKey | Pda;
  /** (Optional) Equality proof record account or context state account. */
  equalityRecord?: PublicKey | Pda;
  /**
   * (Optional) Transfer amount ciphertext validity proof record
   * account or context state account.
   */

  transferAmountCiphertextValidityRecord?: PublicKey | Pda;
  /** (Optional) Fee sigma proof record account or context state account. */
  feeSigmaRecord?: PublicKey | Pda;
  /** (Optional) Fee ciphertext validity proof record account or context state account. */
  feeCiphertextValidityRecord?: PublicKey | Pda;
  /** (Optional) Range proof record account or context state account. */
  rangeRecord?: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfidentialTransferWithFeeInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /** The new source decryptable balance if the transfer succeeds. */
  newSourceDecryptableAvailableBalance: DecryptableBalance;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextLo: EncryptedBalance;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextHi: EncryptedBalance;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `TransferWithFee` instruction in the transaction. If the offset
   * is `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  transferAmountCiphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::VerifyPercentageWithFee`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  feeSigmaProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext2HandlesValidity`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  feeCiphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU256Data`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  rangeProofInstructionOffset: number;
};

export type ConfidentialTransferWithFeeInstructionDataArgs = {
  /** The new source decryptable balance if the transfer succeeds. */
  newSourceDecryptableAvailableBalance: DecryptableBalanceArgs;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextLo: EncryptedBalanceArgs;
  /** The transfer amount encrypted under the auditor ElGamal public key. */
  transferAmountAuditorCiphertextHi: EncryptedBalanceArgs;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `TransferWithFee` instruction in the transaction. If the offset
   * is `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  transferAmountCiphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::VerifyPercentageWithFee`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  feeSigmaProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext2HandlesValidity`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  feeCiphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU256Data`
   * instruction to the `TransferWithFee` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the
   * proof.
   */
  rangeProofInstructionOffset: number;
};

export function getConfidentialTransferWithFeeInstructionDataSerializer(): Serializer<
  ConfidentialTransferWithFeeInstructionDataArgs,
  ConfidentialTransferWithFeeInstructionData
> {
  return mapSerializer<
    ConfidentialTransferWithFeeInstructionDataArgs,
    any,
    ConfidentialTransferWithFeeInstructionData
  >(
    struct<ConfidentialTransferWithFeeInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        [
          'newSourceDecryptableAvailableBalance',
          getDecryptableBalanceSerializer(),
        ],
        ['transferAmountAuditorCiphertextLo', getEncryptedBalanceSerializer()],
        ['transferAmountAuditorCiphertextHi', getEncryptedBalanceSerializer()],
        ['equalityProofInstructionOffset', i8()],
        ['transferAmountCiphertextValidityProofInstructionOffset', i8()],
        ['feeSigmaProofInstructionOffset', i8()],
        ['feeCiphertextValidityProofInstructionOffset', i8()],
        ['rangeProofInstructionOffset', i8()],
      ],
      { description: 'ConfidentialTransferWithFeeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 13,
    })
  ) as Serializer<
    ConfidentialTransferWithFeeInstructionDataArgs,
    ConfidentialTransferWithFeeInstructionData
  >;
}

// Args.
export type ConfidentialTransferWithFeeInstructionArgs =
  ConfidentialTransferWithFeeInstructionDataArgs;

// Instruction.
export function confidentialTransferWithFee(
  context: Pick<Context, 'programs'>,
  input: ConfidentialTransferWithFeeInstructionAccounts &
    ConfidentialTransferWithFeeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    sourceToken: {
      index: 0,
      isWritable: true as boolean,
      value: input.sourceToken ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    destinationToken: {
      index: 2,
      isWritable: true as boolean,
      value: input.destinationToken ?? null,
    },
    instructionsSysvar: {
      index: 3,
      isWritable: false as boolean,
      value: input.instructionsSysvar ?? null,
    },
    equalityRecord: {
      index: 4,
      isWritable: false as boolean,
      value: input.equalityRecord ?? null,
    },
    transferAmountCiphertextValidityRecord: {
      index: 5,
      isWritable: false as boolean,
      value: input.transferAmountCiphertextValidityRecord ?? null,
    },
    feeSigmaRecord: {
      index: 6,
      isWritable: false as boolean,
      value: input.feeSigmaRecord ?? null,
    },
    feeCiphertextValidityRecord: {
      index: 7,
      isWritable: false as boolean,
      value: input.feeCiphertextValidityRecord ?? null,
    },
    rangeRecord: {
      index: 8,
      isWritable: false as boolean,
      value: input.rangeRecord ?? null,
    },
    authority: {
      index: 9,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfidentialTransferWithFeeInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'omitted',
    programId
  );

  // Data.
  const data =
    getConfidentialTransferWithFeeInstructionDataSerializer().serialize(
      resolvedArgs as ConfidentialTransferWithFeeInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
