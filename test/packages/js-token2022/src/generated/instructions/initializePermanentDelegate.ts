/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializePermanentDelegateInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializePermanentDelegateInstructionData = {
  discriminator: number;
  /** Authority that may sign for `Transfer`s and `Burn`s on any account */
  delegate: PublicKey;
};

export type InitializePermanentDelegateInstructionDataArgs = {
  /** Authority that may sign for `Transfer`s and `Burn`s on any account */
  delegate: PublicKey;
};

export function getInitializePermanentDelegateInstructionDataSerializer(): Serializer<
  InitializePermanentDelegateInstructionDataArgs,
  InitializePermanentDelegateInstructionData
> {
  return mapSerializer<
    InitializePermanentDelegateInstructionDataArgs,
    any,
    InitializePermanentDelegateInstructionData
  >(
    struct<InitializePermanentDelegateInstructionData>(
      [
        ['discriminator', u8()],
        ['delegate', publicKeySerializer()],
      ],
      { description: 'InitializePermanentDelegateInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 35 })
  ) as Serializer<
    InitializePermanentDelegateInstructionDataArgs,
    InitializePermanentDelegateInstructionData
  >;
}

// Args.
export type InitializePermanentDelegateInstructionArgs =
  InitializePermanentDelegateInstructionDataArgs;

// Instruction.
export function initializePermanentDelegate(
  context: Pick<Context, 'programs'>,
  input: InitializePermanentDelegateInstructionAccounts &
    InitializePermanentDelegateInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializePermanentDelegateInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializePermanentDelegateInstructionDataSerializer().serialize(
      resolvedArgs as InitializePermanentDelegateInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
