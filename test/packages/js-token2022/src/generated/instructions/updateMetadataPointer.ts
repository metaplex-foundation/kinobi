/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type UpdateMetadataPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
  /** The metadata pointer authority or its multisignature account. */
  metadataPointerAuthority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateMetadataPointerInstructionData = {
  discriminator: number;
  metadataPointerDiscriminator: number;
  /** The new account address that holds the metadata. */
  metadataAddress: Option<PublicKey>;
};

export type UpdateMetadataPointerInstructionDataArgs = {
  /** The new account address that holds the metadata. */
  metadataAddress: OptionOrNullable<PublicKey>;
};

export function getUpdateMetadataPointerInstructionDataSerializer(): Serializer<
  UpdateMetadataPointerInstructionDataArgs,
  UpdateMetadataPointerInstructionData
> {
  return mapSerializer<
    UpdateMetadataPointerInstructionDataArgs,
    any,
    UpdateMetadataPointerInstructionData
  >(
    struct<UpdateMetadataPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['metadataPointerDiscriminator', u8()],
        ['metadataAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'UpdateMetadataPointerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 39,
      metadataPointerDiscriminator: 1,
    })
  ) as Serializer<
    UpdateMetadataPointerInstructionDataArgs,
    UpdateMetadataPointerInstructionData
  >;
}

// Args.
export type UpdateMetadataPointerInstructionArgs =
  UpdateMetadataPointerInstructionDataArgs;

// Instruction.
export function updateMetadataPointer(
  context: Pick<Context, 'programs'>,
  input: UpdateMetadataPointerInstructionAccounts &
    UpdateMetadataPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    metadataPointerAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.metadataPointerAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateMetadataPointerInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateMetadataPointerInstructionDataSerializer().serialize(
    resolvedArgs as UpdateMetadataPointerInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
