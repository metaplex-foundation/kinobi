/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DisableCpiGuardInstructionAccounts = {
  /** The token account to update. */
  token: PublicKey | Pda;
  /** The account's owner/delegate or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type DisableCpiGuardInstructionData = {
  discriminator: number;
  cpiGuardDiscriminator: number;
};

export type DisableCpiGuardInstructionDataArgs = {};

export function getDisableCpiGuardInstructionDataSerializer(): Serializer<
  DisableCpiGuardInstructionDataArgs,
  DisableCpiGuardInstructionData
> {
  return mapSerializer<
    DisableCpiGuardInstructionDataArgs,
    any,
    DisableCpiGuardInstructionData
  >(
    struct<DisableCpiGuardInstructionData>(
      [
        ['discriminator', u8()],
        ['cpiGuardDiscriminator', u8()],
      ],
      { description: 'DisableCpiGuardInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 34, cpiGuardDiscriminator: 1 })
  ) as Serializer<
    DisableCpiGuardInstructionDataArgs,
    DisableCpiGuardInstructionData
  >;
}

// Instruction.
export function disableCpiGuard(
  context: Pick<Context, 'programs'>,
  input: DisableCpiGuardInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    owner: {
      index: 1,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDisableCpiGuardInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
