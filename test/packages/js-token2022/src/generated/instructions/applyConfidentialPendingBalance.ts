/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  getDecryptableBalanceSerializer,
} from '../types';

// Accounts.
export type ApplyConfidentialPendingBalanceInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ApplyConfidentialPendingBalanceInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /**
   * The expected number of pending balance credits since the last successful
   * `ApplyPendingBalance` instruction
   */
  expectedPendingBalanceCreditCounter: bigint;
  /**
   * The new decryptable balance if the pending balance is applied
   * successfully
   */
  newDecryptableAvailableBalance: DecryptableBalance;
};

export type ApplyConfidentialPendingBalanceInstructionDataArgs = {
  /**
   * The expected number of pending balance credits since the last successful
   * `ApplyPendingBalance` instruction
   */
  expectedPendingBalanceCreditCounter: number | bigint;
  /**
   * The new decryptable balance if the pending balance is applied
   * successfully
   */
  newDecryptableAvailableBalance: DecryptableBalanceArgs;
};

export function getApplyConfidentialPendingBalanceInstructionDataSerializer(): Serializer<
  ApplyConfidentialPendingBalanceInstructionDataArgs,
  ApplyConfidentialPendingBalanceInstructionData
> {
  return mapSerializer<
    ApplyConfidentialPendingBalanceInstructionDataArgs,
    any,
    ApplyConfidentialPendingBalanceInstructionData
  >(
    struct<ApplyConfidentialPendingBalanceInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['expectedPendingBalanceCreditCounter', u64()],
        ['newDecryptableAvailableBalance', getDecryptableBalanceSerializer()],
      ],
      { description: 'ApplyConfidentialPendingBalanceInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 8,
    })
  ) as Serializer<
    ApplyConfidentialPendingBalanceInstructionDataArgs,
    ApplyConfidentialPendingBalanceInstructionData
  >;
}

// Args.
export type ApplyConfidentialPendingBalanceInstructionArgs =
  ApplyConfidentialPendingBalanceInstructionDataArgs;

// Instruction.
export function applyConfidentialPendingBalance(
  context: Pick<Context, 'programs'>,
  input: ApplyConfidentialPendingBalanceInstructionAccounts &
    ApplyConfidentialPendingBalanceInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ApplyConfidentialPendingBalanceInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getApplyConfidentialPendingBalanceInstructionDataSerializer().serialize(
      resolvedArgs as ApplyConfidentialPendingBalanceInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
