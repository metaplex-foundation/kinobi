/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i16,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeInterestBearingMintInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeInterestBearingMintInstructionData = {
  discriminator: number;
  interestBearingMintDiscriminator: number;
  /** The public key for the account that can update the rate */
  rateAuthority: Option<PublicKey>;
  /** The initial interest rate */
  rate: number;
};

export type InitializeInterestBearingMintInstructionDataArgs = {
  /** The public key for the account that can update the rate */
  rateAuthority: OptionOrNullable<PublicKey>;
  /** The initial interest rate */
  rate: number;
};

export function getInitializeInterestBearingMintInstructionDataSerializer(): Serializer<
  InitializeInterestBearingMintInstructionDataArgs,
  InitializeInterestBearingMintInstructionData
> {
  return mapSerializer<
    InitializeInterestBearingMintInstructionDataArgs,
    any,
    InitializeInterestBearingMintInstructionData
  >(
    struct<InitializeInterestBearingMintInstructionData>(
      [
        ['discriminator', u8()],
        ['interestBearingMintDiscriminator', u8()],
        ['rateAuthority', zeroableOption(publicKeySerializer())],
        ['rate', i16()],
      ],
      { description: 'InitializeInterestBearingMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 33,
      interestBearingMintDiscriminator: 0,
    })
  ) as Serializer<
    InitializeInterestBearingMintInstructionDataArgs,
    InitializeInterestBearingMintInstructionData
  >;
}

// Args.
export type InitializeInterestBearingMintInstructionArgs =
  InitializeInterestBearingMintInstructionDataArgs;

// Instruction.
export function initializeInterestBearingMint(
  context: Pick<Context, 'programs'>,
  input: InitializeInterestBearingMintInstructionAccounts &
    InitializeInterestBearingMintInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeInterestBearingMintInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeInterestBearingMintInstructionDataSerializer().serialize(
      resolvedArgs as InitializeInterestBearingMintInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
