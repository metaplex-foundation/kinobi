/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findAssociatedTokenPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RecoverNestedAssociatedTokenInstructionAccounts = {
  /** Nested associated token account, must be owned by `ownerAssociatedAccountAddress`. */
  nestedAssociatedAccountAddress?: PublicKey | Pda;
  /** Token mint for the nested associated token account. */
  nestedTokenMintAddress: PublicKey | Pda;
  /** Wallet's associated token account. */
  destinationAssociatedAccountAddress?: PublicKey | Pda;
  /** Owner associated token account address, must be owned by `walletAddress`. */
  ownerAssociatedAccountAddress?: PublicKey | Pda;
  /** Token mint for the owner associated token account. */
  ownerTokenMintAddress: PublicKey | Pda;
  /** Wallet address for the owner associated token account. */
  walletAddress: Signer;
  /** SPL Token program. */
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type RecoverNestedAssociatedTokenInstructionData = {
  discriminator: number;
};

export type RecoverNestedAssociatedTokenInstructionDataArgs = {};

export function getRecoverNestedAssociatedTokenInstructionDataSerializer(): Serializer<
  RecoverNestedAssociatedTokenInstructionDataArgs,
  RecoverNestedAssociatedTokenInstructionData
> {
  return mapSerializer<
    RecoverNestedAssociatedTokenInstructionDataArgs,
    any,
    RecoverNestedAssociatedTokenInstructionData
  >(
    struct<RecoverNestedAssociatedTokenInstructionData>(
      [['discriminator', u8()]],
      { description: 'RecoverNestedAssociatedTokenInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 2 })
  ) as Serializer<
    RecoverNestedAssociatedTokenInstructionDataArgs,
    RecoverNestedAssociatedTokenInstructionData
  >;
}

// Instruction.
export function recoverNestedAssociatedToken(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: RecoverNestedAssociatedTokenInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'associatedToken',
    'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
  );

  // Accounts.
  const resolvedAccounts = {
    nestedAssociatedAccountAddress: {
      index: 0,
      isWritable: true as boolean,
      value: input.nestedAssociatedAccountAddress ?? null,
    },
    nestedTokenMintAddress: {
      index: 1,
      isWritable: false as boolean,
      value: input.nestedTokenMintAddress ?? null,
    },
    destinationAssociatedAccountAddress: {
      index: 2,
      isWritable: true as boolean,
      value: input.destinationAssociatedAccountAddress ?? null,
    },
    ownerAssociatedAccountAddress: {
      index: 3,
      isWritable: false as boolean,
      value: input.ownerAssociatedAccountAddress ?? null,
    },
    ownerTokenMintAddress: {
      index: 4,
      isWritable: false as boolean,
      value: input.ownerTokenMintAddress ?? null,
    },
    walletAddress: {
      index: 5,
      isWritable: true as boolean,
      value: input.walletAddress ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = publicKey(
      'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
    );
  }
  if (!resolvedAccounts.ownerAssociatedAccountAddress.value) {
    resolvedAccounts.ownerAssociatedAccountAddress.value =
      findAssociatedTokenPda(context, {
        owner: expectPublicKey(resolvedAccounts.walletAddress.value),
        tokenProgram: expectPublicKey(resolvedAccounts.tokenProgram.value),
        mint: expectPublicKey(resolvedAccounts.ownerTokenMintAddress.value),
      });
  }
  if (!resolvedAccounts.nestedAssociatedAccountAddress.value) {
    resolvedAccounts.nestedAssociatedAccountAddress.value =
      findAssociatedTokenPda(context, {
        owner: expectPublicKey(
          resolvedAccounts.ownerAssociatedAccountAddress.value
        ),
        tokenProgram: expectPublicKey(resolvedAccounts.tokenProgram.value),
        mint: expectPublicKey(resolvedAccounts.nestedTokenMintAddress.value),
      });
  }
  if (!resolvedAccounts.destinationAssociatedAccountAddress.value) {
    resolvedAccounts.destinationAssociatedAccountAddress.value =
      findAssociatedTokenPda(context, {
        owner: expectPublicKey(resolvedAccounts.walletAddress.value),
        tokenProgram: expectPublicKey(resolvedAccounts.tokenProgram.value),
        mint: expectPublicKey(resolvedAccounts.nestedTokenMintAddress.value),
      });
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRecoverNestedAssociatedTokenInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
