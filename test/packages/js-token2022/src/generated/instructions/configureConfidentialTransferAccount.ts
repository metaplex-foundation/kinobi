/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  getDecryptableBalanceSerializer,
} from '../types';

// Accounts.
export type ConfigureConfidentialTransferAccountInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /**
   * Instructions sysvar if `VerifyPubkeyValidity` is included in
   * the same transaction or context state account if
   * `VerifyPubkeyValidity` is pre-verified into a context state
   * account.
   */

  instructionsSysvarOrContextState?: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfigureConfidentialTransferAccountInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /** The decryptable balance (always 0) once the configure account succeeds. */
  decryptableZeroBalance: DecryptableBalance;
  /**
   * The maximum number of despots and transfers that an account can receiver
   * before the `ApplyPendingBalance` is executed
   */
  maximumPendingBalanceCreditCounter: bigint;
  /**
   * Relative location of the `ProofInstruction::ZeroCiphertextProof`
   * instruction to the `ConfigureAccount` instruction in the
   * transaction. If the offset is `0`, then use a context state account
   * for the proof.
   */
  proofInstructionOffset: number;
};

export type ConfigureConfidentialTransferAccountInstructionDataArgs = {
  /** The decryptable balance (always 0) once the configure account succeeds. */
  decryptableZeroBalance: DecryptableBalanceArgs;
  /**
   * The maximum number of despots and transfers that an account can receiver
   * before the `ApplyPendingBalance` is executed
   */
  maximumPendingBalanceCreditCounter: number | bigint;
  /**
   * Relative location of the `ProofInstruction::ZeroCiphertextProof`
   * instruction to the `ConfigureAccount` instruction in the
   * transaction. If the offset is `0`, then use a context state account
   * for the proof.
   */
  proofInstructionOffset: number;
};

export function getConfigureConfidentialTransferAccountInstructionDataSerializer(): Serializer<
  ConfigureConfidentialTransferAccountInstructionDataArgs,
  ConfigureConfidentialTransferAccountInstructionData
> {
  return mapSerializer<
    ConfigureConfidentialTransferAccountInstructionDataArgs,
    any,
    ConfigureConfidentialTransferAccountInstructionData
  >(
    struct<ConfigureConfidentialTransferAccountInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['decryptableZeroBalance', getDecryptableBalanceSerializer()],
        ['maximumPendingBalanceCreditCounter', u64()],
        ['proofInstructionOffset', i8()],
      ],
      { description: 'ConfigureConfidentialTransferAccountInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 2,
    })
  ) as Serializer<
    ConfigureConfidentialTransferAccountInstructionDataArgs,
    ConfigureConfidentialTransferAccountInstructionData
  >;
}

// Args.
export type ConfigureConfidentialTransferAccountInstructionArgs =
  ConfigureConfidentialTransferAccountInstructionDataArgs;

// Instruction.
export function configureConfidentialTransferAccount(
  context: Pick<Context, 'programs'>,
  input: ConfigureConfidentialTransferAccountInstructionAccounts &
    ConfigureConfidentialTransferAccountInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    instructionsSysvarOrContextState: {
      index: 2,
      isWritable: false as boolean,
      value: input.instructionsSysvarOrContextState ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfigureConfidentialTransferAccountInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.instructionsSysvarOrContextState.value) {
    resolvedAccounts.instructionsSysvarOrContextState.value = publicKey(
      'Sysvar1nstructions1111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getConfigureConfidentialTransferAccountInstructionDataSerializer().serialize(
      resolvedArgs as ConfigureConfidentialTransferAccountInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
