/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type TransferCheckedWithFeeInstructionAccounts = {
  /** The source account. May include the `TransferFeeAmount` extension. */
  source: PublicKey | Pda;
  /** The token mint. May include the `TransferFeeConfig` extension. */
  mint: PublicKey | Pda;
  /** The destination account. May include the `TransferFeeAmount` extension. */
  destination: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority?: PublicKey | Pda | Signer;
};

// Data.
export type TransferCheckedWithFeeInstructionData = {
  discriminator: number;
  transferFeeDiscriminator: number;
  /** The amount of tokens to transfer. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
  /**
   * Expected fee assessed on this transfer, calculated off-chain based
   * on the transfer_fee_basis_points and maximum_fee of the mint. May
   * be 0 for a mint without a configured transfer fee.
   */
  fee: bigint;
};

export type TransferCheckedWithFeeInstructionDataArgs = {
  /** The amount of tokens to transfer. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
  /**
   * Expected fee assessed on this transfer, calculated off-chain based
   * on the transfer_fee_basis_points and maximum_fee of the mint. May
   * be 0 for a mint without a configured transfer fee.
   */
  fee: number | bigint;
};

export function getTransferCheckedWithFeeInstructionDataSerializer(): Serializer<
  TransferCheckedWithFeeInstructionDataArgs,
  TransferCheckedWithFeeInstructionData
> {
  return mapSerializer<
    TransferCheckedWithFeeInstructionDataArgs,
    any,
    TransferCheckedWithFeeInstructionData
  >(
    struct<TransferCheckedWithFeeInstructionData>(
      [
        ['discriminator', u8()],
        ['transferFeeDiscriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
        ['fee', u64()],
      ],
      { description: 'TransferCheckedWithFeeInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 26, transferFeeDiscriminator: 1 })
  ) as Serializer<
    TransferCheckedWithFeeInstructionDataArgs,
    TransferCheckedWithFeeInstructionData
  >;
}

// Args.
export type TransferCheckedWithFeeInstructionArgs =
  TransferCheckedWithFeeInstructionDataArgs;

// Instruction.
export function transferCheckedWithFee(
  context: Pick<Context, 'identity' | 'programs'>,
  input: TransferCheckedWithFeeInstructionAccounts &
    TransferCheckedWithFeeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    source: {
      index: 0,
      isWritable: true as boolean,
      value: input.source ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    destination: {
      index: 2,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TransferCheckedWithFeeInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTransferCheckedWithFeeInstructionDataSerializer().serialize(
    resolvedArgs as TransferCheckedWithFeeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
