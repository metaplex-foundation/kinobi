/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  mapSerializer,
  string,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveTokenMetadataKeyInstructionAccounts = {
  metadata: PublicKey | Pda;
  updateAuthority: Signer;
};

// Data.
export type RemoveTokenMetadataKeyInstructionData = {
  discriminator: Uint8Array;
  /**
   * If the idempotent flag is set to true, then the instruction will not
   * error if the key does not exist
   */
  idempotent: boolean;
  /** Key to remove in the additional metadata portion. */
  key: string;
};

export type RemoveTokenMetadataKeyInstructionDataArgs = {
  /**
   * If the idempotent flag is set to true, then the instruction will not
   * error if the key does not exist
   */
  idempotent?: boolean;
  /** Key to remove in the additional metadata portion. */
  key: string;
};

export function getRemoveTokenMetadataKeyInstructionDataSerializer(): Serializer<
  RemoveTokenMetadataKeyInstructionDataArgs,
  RemoveTokenMetadataKeyInstructionData
> {
  return mapSerializer<
    RemoveTokenMetadataKeyInstructionDataArgs,
    any,
    RemoveTokenMetadataKeyInstructionData
  >(
    struct<RemoveTokenMetadataKeyInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['idempotent', bool()],
        ['key', string()],
      ],
      { description: 'RemoveTokenMetadataKeyInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([234, 18, 32, 56, 89, 141, 37, 181]),
      idempotent: value.idempotent ?? false,
    })
  ) as Serializer<
    RemoveTokenMetadataKeyInstructionDataArgs,
    RemoveTokenMetadataKeyInstructionData
  >;
}

// Args.
export type RemoveTokenMetadataKeyInstructionArgs =
  RemoveTokenMetadataKeyInstructionDataArgs;

// Instruction.
export function removeTokenMetadataKey(
  context: Pick<Context, 'programs'>,
  input: RemoveTokenMetadataKeyInstructionAccounts &
    RemoveTokenMetadataKeyInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    metadata: {
      index: 0,
      isWritable: true as boolean,
      value: input.metadata ?? null,
    },
    updateAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.updateAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveTokenMetadataKeyInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveTokenMetadataKeyInstructionDataSerializer().serialize(
    resolvedArgs as RemoveTokenMetadataKeyInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
