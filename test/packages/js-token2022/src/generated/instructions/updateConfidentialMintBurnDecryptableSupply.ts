/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  getDecryptableBalanceSerializer,
} from '../types';

// Accounts.
export type UpdateConfidentialMintBurnDecryptableSupplyInstructionAccounts = {
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
  /** The confidential mint authority or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateConfidentialMintBurnDecryptableSupplyInstructionData = {
  discriminator: number;
  confidentialMintBurnDiscriminator: number;
  /** The new decryptable supply. */
  newDecryptableSupply: DecryptableBalance;
};

export type UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs = {
  /** The new decryptable supply. */
  newDecryptableSupply: DecryptableBalanceArgs;
};

export function getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataSerializer(): Serializer<
  UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs,
  UpdateConfidentialMintBurnDecryptableSupplyInstructionData
> {
  return mapSerializer<
    UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs,
    any,
    UpdateConfidentialMintBurnDecryptableSupplyInstructionData
  >(
    struct<UpdateConfidentialMintBurnDecryptableSupplyInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialMintBurnDiscriminator', u8()],
        ['newDecryptableSupply', getDecryptableBalanceSerializer()],
      ],
      {
        description:
          'UpdateConfidentialMintBurnDecryptableSupplyInstructionData',
      }
    ),
    (value) => ({
      ...value,
      discriminator: 42,
      confidentialMintBurnDiscriminator: 2,
    })
  ) as Serializer<
    UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs,
    UpdateConfidentialMintBurnDecryptableSupplyInstructionData
  >;
}

// Args.
export type UpdateConfidentialMintBurnDecryptableSupplyInstructionArgs =
  UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs;

// Instruction.
export function updateConfidentialMintBurnDecryptableSupply(
  context: Pick<Context, 'programs'>,
  input: UpdateConfidentialMintBurnDecryptableSupplyInstructionAccounts &
    UpdateConfidentialMintBurnDecryptableSupplyInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateConfidentialMintBurnDecryptableSupplyInstructionArgs =
    { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataSerializer().serialize(
      resolvedArgs as UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
