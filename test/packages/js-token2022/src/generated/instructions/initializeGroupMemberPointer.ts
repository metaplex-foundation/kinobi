/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeGroupMemberPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeGroupMemberPointerInstructionData = {
  discriminator: number;
  groupMemberPointerDiscriminator: number;
  /** The public key for the account that can update the group member address. */
  authority: Option<PublicKey>;
  /** The account address that holds the member. */
  memberAddress: Option<PublicKey>;
};

export type InitializeGroupMemberPointerInstructionDataArgs = {
  /** The public key for the account that can update the group member address. */
  authority: OptionOrNullable<PublicKey>;
  /** The account address that holds the member. */
  memberAddress: OptionOrNullable<PublicKey>;
};

export function getInitializeGroupMemberPointerInstructionDataSerializer(): Serializer<
  InitializeGroupMemberPointerInstructionDataArgs,
  InitializeGroupMemberPointerInstructionData
> {
  return mapSerializer<
    InitializeGroupMemberPointerInstructionDataArgs,
    any,
    InitializeGroupMemberPointerInstructionData
  >(
    struct<InitializeGroupMemberPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['groupMemberPointerDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
        ['memberAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializeGroupMemberPointerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 41,
      groupMemberPointerDiscriminator: 0,
    })
  ) as Serializer<
    InitializeGroupMemberPointerInstructionDataArgs,
    InitializeGroupMemberPointerInstructionData
  >;
}

// Args.
export type InitializeGroupMemberPointerInstructionArgs =
  InitializeGroupMemberPointerInstructionDataArgs;

// Instruction.
export function initializeGroupMemberPointer(
  context: Pick<Context, 'programs'>,
  input: InitializeGroupMemberPointerInstructionAccounts &
    InitializeGroupMemberPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeGroupMemberPointerInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeGroupMemberPointerInstructionDataSerializer().serialize(
      resolvedArgs as InitializeGroupMemberPointerInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
