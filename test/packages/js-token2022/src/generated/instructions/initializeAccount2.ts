/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeAccount2InstructionAccounts = {
  /** The account to initialize. */
  account: PublicKey | Pda;
  /** The mint this account will be associated with. */
  mint: PublicKey | Pda;
  /** Rent sysvar. */
  rent?: PublicKey | Pda;
};

// Data.
export type InitializeAccount2InstructionData = {
  discriminator: number;
  /** The new account's owner/multisignature. */
  owner: PublicKey;
};

export type InitializeAccount2InstructionDataArgs = {
  /** The new account's owner/multisignature. */
  owner: PublicKey;
};

export function getInitializeAccount2InstructionDataSerializer(): Serializer<
  InitializeAccount2InstructionDataArgs,
  InitializeAccount2InstructionData
> {
  return mapSerializer<
    InitializeAccount2InstructionDataArgs,
    any,
    InitializeAccount2InstructionData
  >(
    struct<InitializeAccount2InstructionData>(
      [
        ['discriminator', u8()],
        ['owner', publicKeySerializer()],
      ],
      { description: 'InitializeAccount2InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 16 })
  ) as Serializer<
    InitializeAccount2InstructionDataArgs,
    InitializeAccount2InstructionData
  >;
}

// Args.
export type InitializeAccount2InstructionArgs =
  InitializeAccount2InstructionDataArgs;

// Instruction.
export function initializeAccount2(
  context: Pick<Context, 'programs'>,
  input: InitializeAccount2InstructionAccounts &
    InitializeAccount2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    account: {
      index: 0,
      isWritable: true as boolean,
      value: input.account ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    rent: { index: 2, isWritable: false as boolean, value: input.rent ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeAccount2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.rent.value) {
    resolvedAccounts.rent.value = publicKey(
      'SysvarRent111111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeAccount2InstructionDataSerializer().serialize(
    resolvedArgs as InitializeAccount2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
