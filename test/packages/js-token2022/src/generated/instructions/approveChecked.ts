/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ApproveCheckedInstructionAccounts = {
  /** The source account. */
  source: PublicKey | Pda;
  /** The token mint. */
  mint: PublicKey | Pda;
  /** The delegate. */
  delegate: PublicKey | Pda;
  /** The source account owner or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type ApproveCheckedInstructionData = {
  discriminator: number;
  /** The amount of tokens the delegate is approved for. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export type ApproveCheckedInstructionDataArgs = {
  /** The amount of tokens the delegate is approved for. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export function getApproveCheckedInstructionDataSerializer(): Serializer<
  ApproveCheckedInstructionDataArgs,
  ApproveCheckedInstructionData
> {
  return mapSerializer<
    ApproveCheckedInstructionDataArgs,
    any,
    ApproveCheckedInstructionData
  >(
    struct<ApproveCheckedInstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
      ],
      { description: 'ApproveCheckedInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 13 })
  ) as Serializer<
    ApproveCheckedInstructionDataArgs,
    ApproveCheckedInstructionData
  >;
}

// Args.
export type ApproveCheckedInstructionArgs = ApproveCheckedInstructionDataArgs;

// Instruction.
export function approveChecked(
  context: Pick<Context, 'programs'>,
  input: ApproveCheckedInstructionAccounts & ApproveCheckedInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    source: {
      index: 0,
      isWritable: true as boolean,
      value: input.source ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    delegate: {
      index: 2,
      isWritable: false as boolean,
      value: input.delegate ?? null,
    },
    owner: {
      index: 3,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ApproveCheckedInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getApproveCheckedInstructionDataSerializer().serialize(
    resolvedArgs as ApproveCheckedInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
