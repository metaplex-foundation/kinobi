/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateNativeMintInstructionAccounts = {
  /** Funding account (must be a system account) */
  payer: Signer;
  /** The native mint address */
  nativeMint: PublicKey | Pda;
  /** System program for mint account funding */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateNativeMintInstructionData = { discriminator: number };

export type CreateNativeMintInstructionDataArgs = {};

export function getCreateNativeMintInstructionDataSerializer(): Serializer<
  CreateNativeMintInstructionDataArgs,
  CreateNativeMintInstructionData
> {
  return mapSerializer<
    CreateNativeMintInstructionDataArgs,
    any,
    CreateNativeMintInstructionData
  >(
    struct<CreateNativeMintInstructionData>([['discriminator', u8()]], {
      description: 'CreateNativeMintInstructionData',
    }),
    (value) => ({ ...value, discriminator: 31 })
  ) as Serializer<
    CreateNativeMintInstructionDataArgs,
    CreateNativeMintInstructionData
  >;
}

// Instruction.
export function createNativeMint(
  context: Pick<Context, 'programs'>,
  input: CreateNativeMintInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    nativeMint: {
      index: 1,
      isWritable: true as boolean,
      value: input.nativeMint ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = publicKey(
      '11111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateNativeMintInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
