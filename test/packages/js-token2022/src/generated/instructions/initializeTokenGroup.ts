/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeTokenGroupInstructionAccounts = {
  group: PublicKey | Pda;
  mint: PublicKey | Pda;
  mintAuthority: Signer;
};

// Data.
export type InitializeTokenGroupInstructionData = {
  discriminator: Uint8Array;
  /** Update authority for the group */
  updateAuthority: Option<PublicKey>;
  /** The maximum number of group members */
  maxSize: bigint;
};

export type InitializeTokenGroupInstructionDataArgs = {
  /** Update authority for the group */
  updateAuthority: OptionOrNullable<PublicKey>;
  /** The maximum number of group members */
  maxSize: number | bigint;
};

export function getInitializeTokenGroupInstructionDataSerializer(): Serializer<
  InitializeTokenGroupInstructionDataArgs,
  InitializeTokenGroupInstructionData
> {
  return mapSerializer<
    InitializeTokenGroupInstructionDataArgs,
    any,
    InitializeTokenGroupInstructionData
  >(
    struct<InitializeTokenGroupInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['updateAuthority', zeroableOption(publicKeySerializer())],
        ['maxSize', u64()],
      ],
      { description: 'InitializeTokenGroupInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([121, 113, 108, 39, 54, 51, 0, 4]),
    })
  ) as Serializer<
    InitializeTokenGroupInstructionDataArgs,
    InitializeTokenGroupInstructionData
  >;
}

// Args.
export type InitializeTokenGroupInstructionArgs =
  InitializeTokenGroupInstructionDataArgs;

// Instruction.
export function initializeTokenGroup(
  context: Pick<Context, 'programs'>,
  input: InitializeTokenGroupInstructionAccounts &
    InitializeTokenGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    mintAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.mintAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeTokenGroupInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeTokenGroupInstructionDataSerializer().serialize(
    resolvedArgs as InitializeTokenGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
