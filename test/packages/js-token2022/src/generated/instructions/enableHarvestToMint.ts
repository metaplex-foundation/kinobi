/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type EnableHarvestToMintInstructionAccounts = {
  /** The token mint. */
  mint: PublicKey | Pda;
  /** The confidential transfer fee authority */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type EnableHarvestToMintInstructionData = {
  discriminator: number;
  confidentialTransferFeeDiscriminator: number;
};

export type EnableHarvestToMintInstructionDataArgs = {};

export function getEnableHarvestToMintInstructionDataSerializer(): Serializer<
  EnableHarvestToMintInstructionDataArgs,
  EnableHarvestToMintInstructionData
> {
  return mapSerializer<
    EnableHarvestToMintInstructionDataArgs,
    any,
    EnableHarvestToMintInstructionData
  >(
    struct<EnableHarvestToMintInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferFeeDiscriminator', u8()],
      ],
      { description: 'EnableHarvestToMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 37,
      confidentialTransferFeeDiscriminator: 4,
    })
  ) as Serializer<
    EnableHarvestToMintInstructionDataArgs,
    EnableHarvestToMintInstructionData
  >;
}

// Instruction.
export function enableHarvestToMint(
  context: Pick<Context, 'programs'>,
  input: EnableHarvestToMintInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getEnableHarvestToMintInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
