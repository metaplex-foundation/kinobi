/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeTransferFeeConfigInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeTransferFeeConfigInstructionData = {
  discriminator: number;
  transferFeeDiscriminator: number;
  /** Pubkey that may update the fees. */
  transferFeeConfigAuthority: Option<PublicKey>;
  /** Withdraw instructions must be signed by this key. */
  withdrawWithheldAuthority: Option<PublicKey>;
  /** Amount of transfer collected as fees, expressed as basis points of the transfer amount. */
  transferFeeBasisPoints: number;
  /** Maximum fee assessed on transfers. */
  maximumFee: bigint;
};

export type InitializeTransferFeeConfigInstructionDataArgs = {
  /** Pubkey that may update the fees. */
  transferFeeConfigAuthority: OptionOrNullable<PublicKey>;
  /** Withdraw instructions must be signed by this key. */
  withdrawWithheldAuthority: OptionOrNullable<PublicKey>;
  /** Amount of transfer collected as fees, expressed as basis points of the transfer amount. */
  transferFeeBasisPoints: number;
  /** Maximum fee assessed on transfers. */
  maximumFee: number | bigint;
};

export function getInitializeTransferFeeConfigInstructionDataSerializer(): Serializer<
  InitializeTransferFeeConfigInstructionDataArgs,
  InitializeTransferFeeConfigInstructionData
> {
  return mapSerializer<
    InitializeTransferFeeConfigInstructionDataArgs,
    any,
    InitializeTransferFeeConfigInstructionData
  >(
    struct<InitializeTransferFeeConfigInstructionData>(
      [
        ['discriminator', u8()],
        ['transferFeeDiscriminator', u8()],
        ['transferFeeConfigAuthority', option(publicKeySerializer())],
        ['withdrawWithheldAuthority', option(publicKeySerializer())],
        ['transferFeeBasisPoints', u16()],
        ['maximumFee', u64()],
      ],
      { description: 'InitializeTransferFeeConfigInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 26, transferFeeDiscriminator: 0 })
  ) as Serializer<
    InitializeTransferFeeConfigInstructionDataArgs,
    InitializeTransferFeeConfigInstructionData
  >;
}

// Args.
export type InitializeTransferFeeConfigInstructionArgs =
  InitializeTransferFeeConfigInstructionDataArgs;

// Instruction.
export function initializeTransferFeeConfig(
  context: Pick<Context, 'programs'>,
  input: InitializeTransferFeeConfigInstructionAccounts &
    InitializeTransferFeeConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeTransferFeeConfigInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeTransferFeeConfigInstructionDataSerializer().serialize(
      resolvedArgs as InitializeTransferFeeConfigInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
