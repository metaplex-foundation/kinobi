/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeGroupPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeGroupPointerInstructionData = {
  discriminator: number;
  groupPointerDiscriminator: number;
  /** The public key for the account that can update the group address. */
  authority: Option<PublicKey>;
  /** The account address that holds the group. */
  groupAddress: Option<PublicKey>;
};

export type InitializeGroupPointerInstructionDataArgs = {
  /** The public key for the account that can update the group address. */
  authority: OptionOrNullable<PublicKey>;
  /** The account address that holds the group. */
  groupAddress: OptionOrNullable<PublicKey>;
};

export function getInitializeGroupPointerInstructionDataSerializer(): Serializer<
  InitializeGroupPointerInstructionDataArgs,
  InitializeGroupPointerInstructionData
> {
  return mapSerializer<
    InitializeGroupPointerInstructionDataArgs,
    any,
    InitializeGroupPointerInstructionData
  >(
    struct<InitializeGroupPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['groupPointerDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
        ['groupAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializeGroupPointerInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 40, groupPointerDiscriminator: 0 })
  ) as Serializer<
    InitializeGroupPointerInstructionDataArgs,
    InitializeGroupPointerInstructionData
  >;
}

// Args.
export type InitializeGroupPointerInstructionArgs =
  InitializeGroupPointerInstructionDataArgs;

// Instruction.
export function initializeGroupPointer(
  context: Pick<Context, 'programs'>,
  input: InitializeGroupPointerInstructionAccounts &
    InitializeGroupPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeGroupPointerInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeGroupPointerInstructionDataSerializer().serialize(
    resolvedArgs as InitializeGroupPointerInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
