/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type UpdateGroupMemberPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
  /** The group member pointer authority or its multisignature account. */
  groupMemberPointerAuthority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateGroupMemberPointerInstructionData = {
  discriminator: number;
  groupMemberPointerDiscriminator: number;
  /** The new account address that holds the member. */
  memberAddress: Option<PublicKey>;
};

export type UpdateGroupMemberPointerInstructionDataArgs = {
  /** The new account address that holds the member. */
  memberAddress: OptionOrNullable<PublicKey>;
};

export function getUpdateGroupMemberPointerInstructionDataSerializer(): Serializer<
  UpdateGroupMemberPointerInstructionDataArgs,
  UpdateGroupMemberPointerInstructionData
> {
  return mapSerializer<
    UpdateGroupMemberPointerInstructionDataArgs,
    any,
    UpdateGroupMemberPointerInstructionData
  >(
    struct<UpdateGroupMemberPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['groupMemberPointerDiscriminator', u8()],
        ['memberAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'UpdateGroupMemberPointerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 41,
      groupMemberPointerDiscriminator: 1,
    })
  ) as Serializer<
    UpdateGroupMemberPointerInstructionDataArgs,
    UpdateGroupMemberPointerInstructionData
  >;
}

// Args.
export type UpdateGroupMemberPointerInstructionArgs =
  UpdateGroupMemberPointerInstructionDataArgs;

// Instruction.
export function updateGroupMemberPointer(
  context: Pick<Context, 'programs'>,
  input: UpdateGroupMemberPointerInstructionAccounts &
    UpdateGroupMemberPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    groupMemberPointerAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.groupMemberPointerAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateGroupMemberPointerInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGroupMemberPointerInstructionDataSerializer().serialize(
    resolvedArgs as UpdateGroupMemberPointerInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
