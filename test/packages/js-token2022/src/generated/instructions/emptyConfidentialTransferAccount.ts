/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type EmptyConfidentialTransferAccountInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /**
   * Instructions sysvar if `VerifyZeroCiphertext` is included in
   * the same transaction or context state account if
   * `VerifyZeroCiphertext` is pre-verified into a context state
   * account.
   */

  instructionsSysvarOrContextState?: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type EmptyConfidentialTransferAccountInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /**
   * Relative location of the `ProofInstruction::VerifyCloseAccount`
   * instruction to the `EmptyAccount` instruction in the transaction. If
   * the offset is `0`, then use a context state account for the proof.
   */
  proofInstructionOffset: number;
};

export type EmptyConfidentialTransferAccountInstructionDataArgs = {
  /**
   * Relative location of the `ProofInstruction::VerifyCloseAccount`
   * instruction to the `EmptyAccount` instruction in the transaction. If
   * the offset is `0`, then use a context state account for the proof.
   */
  proofInstructionOffset: number;
};

export function getEmptyConfidentialTransferAccountInstructionDataSerializer(): Serializer<
  EmptyConfidentialTransferAccountInstructionDataArgs,
  EmptyConfidentialTransferAccountInstructionData
> {
  return mapSerializer<
    EmptyConfidentialTransferAccountInstructionDataArgs,
    any,
    EmptyConfidentialTransferAccountInstructionData
  >(
    struct<EmptyConfidentialTransferAccountInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['proofInstructionOffset', i8()],
      ],
      { description: 'EmptyConfidentialTransferAccountInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 4,
    })
  ) as Serializer<
    EmptyConfidentialTransferAccountInstructionDataArgs,
    EmptyConfidentialTransferAccountInstructionData
  >;
}

// Args.
export type EmptyConfidentialTransferAccountInstructionArgs =
  EmptyConfidentialTransferAccountInstructionDataArgs;

// Instruction.
export function emptyConfidentialTransferAccount(
  context: Pick<Context, 'programs'>,
  input: EmptyConfidentialTransferAccountInstructionAccounts &
    EmptyConfidentialTransferAccountInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    instructionsSysvarOrContextState: {
      index: 1,
      isWritable: false as boolean,
      value: input.instructionsSysvarOrContextState ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: EmptyConfidentialTransferAccountInstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.instructionsSysvarOrContextState.value) {
    resolvedAccounts.instructionsSysvarOrContextState.value = publicKey(
      'Sysvar1nstructions1111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getEmptyConfidentialTransferAccountInstructionDataSerializer().serialize(
      resolvedArgs as EmptyConfidentialTransferAccountInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
