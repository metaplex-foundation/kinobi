/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SetTransferFeeInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
  /** The mint's fee account owner or its multisignature account. */
  transferFeeConfigAuthority?: PublicKey | Pda | Signer;
};

// Data.
export type SetTransferFeeInstructionData = {
  discriminator: number;
  transferFeeDiscriminator: number;
  /** Amount of transfer collected as fees, expressed as basis points of the transfer amount. */
  transferFeeBasisPoints: number;
  /** Maximum fee assessed on transfers. */
  maximumFee: bigint;
};

export type SetTransferFeeInstructionDataArgs = {
  /** Amount of transfer collected as fees, expressed as basis points of the transfer amount. */
  transferFeeBasisPoints: number;
  /** Maximum fee assessed on transfers. */
  maximumFee: number | bigint;
};

export function getSetTransferFeeInstructionDataSerializer(): Serializer<
  SetTransferFeeInstructionDataArgs,
  SetTransferFeeInstructionData
> {
  return mapSerializer<
    SetTransferFeeInstructionDataArgs,
    any,
    SetTransferFeeInstructionData
  >(
    struct<SetTransferFeeInstructionData>(
      [
        ['discriminator', u8()],
        ['transferFeeDiscriminator', u8()],
        ['transferFeeBasisPoints', u16()],
        ['maximumFee', u64()],
      ],
      { description: 'SetTransferFeeInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 26, transferFeeDiscriminator: 5 })
  ) as Serializer<
    SetTransferFeeInstructionDataArgs,
    SetTransferFeeInstructionData
  >;
}

// Args.
export type SetTransferFeeInstructionArgs = SetTransferFeeInstructionDataArgs;

// Instruction.
export function setTransferFee(
  context: Pick<Context, 'identity' | 'programs'>,
  input: SetTransferFeeInstructionAccounts & SetTransferFeeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    transferFeeConfigAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.transferFeeConfigAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetTransferFeeInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.transferFeeConfigAuthority.value) {
    resolvedAccounts.transferFeeConfigAuthority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetTransferFeeInstructionDataSerializer().serialize(
    resolvedArgs as SetTransferFeeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
