/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeTokenGroupMemberInstructionAccounts = {
  member: PublicKey | Pda;
  memberMint: PublicKey | Pda;
  memberMintAuthority: Signer;
  group: PublicKey | Pda;
  groupUpdateAuthority: Signer;
};

// Data.
export type InitializeTokenGroupMemberInstructionData = {
  discriminator: Uint8Array;
};

export type InitializeTokenGroupMemberInstructionDataArgs = {};

export function getInitializeTokenGroupMemberInstructionDataSerializer(): Serializer<
  InitializeTokenGroupMemberInstructionDataArgs,
  InitializeTokenGroupMemberInstructionData
> {
  return mapSerializer<
    InitializeTokenGroupMemberInstructionDataArgs,
    any,
    InitializeTokenGroupMemberInstructionData
  >(
    struct<InitializeTokenGroupMemberInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'InitializeTokenGroupMemberInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([152, 32, 222, 176, 223, 237, 116, 134]),
    })
  ) as Serializer<
    InitializeTokenGroupMemberInstructionDataArgs,
    InitializeTokenGroupMemberInstructionData
  >;
}

// Instruction.
export function initializeTokenGroupMember(
  context: Pick<Context, 'programs'>,
  input: InitializeTokenGroupMemberInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    member: {
      index: 0,
      isWritable: true as boolean,
      value: input.member ?? null,
    },
    memberMint: {
      index: 1,
      isWritable: false as boolean,
      value: input.memberMint ?? null,
    },
    memberMintAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.memberMintAuthority ?? null,
    },
    group: {
      index: 3,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    groupUpdateAuthority: {
      index: 4,
      isWritable: false as boolean,
      value: input.groupUpdateAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeTokenGroupMemberInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
