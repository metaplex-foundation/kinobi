/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type UpdateTransferHookInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
  /** The transfer hook authority. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateTransferHookInstructionData = {
  discriminator: number;
  transferHookDiscriminator: number;
  /** The program id that performs logic during transfers */
  programId: Option<PublicKey>;
};

export type UpdateTransferHookInstructionDataArgs = {
  /** The program id that performs logic during transfers */
  programId: OptionOrNullable<PublicKey>;
};

export function getUpdateTransferHookInstructionDataSerializer(): Serializer<
  UpdateTransferHookInstructionDataArgs,
  UpdateTransferHookInstructionData
> {
  return mapSerializer<
    UpdateTransferHookInstructionDataArgs,
    any,
    UpdateTransferHookInstructionData
  >(
    struct<UpdateTransferHookInstructionData>(
      [
        ['discriminator', u8()],
        ['transferHookDiscriminator', u8()],
        ['programId', zeroableOption(publicKeySerializer())],
      ],
      { description: 'UpdateTransferHookInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 36, transferHookDiscriminator: 1 })
  ) as Serializer<
    UpdateTransferHookInstructionDataArgs,
    UpdateTransferHookInstructionData
  >;
}

// Args.
export type UpdateTransferHookInstructionArgs =
  UpdateTransferHookInstructionDataArgs;

// Instruction.
export function updateTransferHook(
  context: Pick<Context, 'programs'>,
  input: UpdateTransferHookInstructionAccounts &
    UpdateTransferHookInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateTransferHookInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateTransferHookInstructionDataSerializer().serialize(
    resolvedArgs as UpdateTransferHookInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
