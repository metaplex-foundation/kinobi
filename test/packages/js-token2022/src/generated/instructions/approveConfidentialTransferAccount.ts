/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ApproveConfidentialTransferAccountInstructionAccounts = {
  /** The SPL Token account to approve. */
  token: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /** Confidential transfer mint authority. */
  authority: Signer;
};

// Data.
export type ApproveConfidentialTransferAccountInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
};

export type ApproveConfidentialTransferAccountInstructionDataArgs = {};

export function getApproveConfidentialTransferAccountInstructionDataSerializer(): Serializer<
  ApproveConfidentialTransferAccountInstructionDataArgs,
  ApproveConfidentialTransferAccountInstructionData
> {
  return mapSerializer<
    ApproveConfidentialTransferAccountInstructionDataArgs,
    any,
    ApproveConfidentialTransferAccountInstructionData
  >(
    struct<ApproveConfidentialTransferAccountInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
      ],
      { description: 'ApproveConfidentialTransferAccountInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 3,
    })
  ) as Serializer<
    ApproveConfidentialTransferAccountInstructionDataArgs,
    ApproveConfidentialTransferAccountInstructionData
  >;
}

// Instruction.
export function approveConfidentialTransferAccount(
  context: Pick<Context, 'programs'>,
  input: ApproveConfidentialTransferAccountInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getApproveConfidentialTransferAccountInstructionDataSerializer().serialize(
      {}
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
