/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  EncryptedBalance,
  EncryptedBalanceArgs,
  getDecryptableBalanceSerializer,
  getEncryptedBalanceSerializer,
} from '../types';

// Accounts.
export type ConfidentialBurnInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
  /**
   * (Optional) Instructions sysvar if at least one of the
   * `zk_elgamal_proof` instructions are included in the same
   * transaction.
   */

  instructionsSysvar?: PublicKey | Pda;
  /**
   * (Optional) The context state account containing the pre-verified
   * `VerifyCiphertextCommitmentEquality` proof.
   */

  equalityRecord?: PublicKey | Pda;
  /**
   * (Optional) The context state account containing the pre-verified
   * `VerifyBatchedGroupedCiphertext3HandlesValidity` proof.
   */

  ciphertextValidityRecord?: PublicKey | Pda;
  /**
   * (Optional) The context state account containing the pre-verified
   * `VerifyBatchedRangeProofU128` proof.
   */

  rangeRecord?: PublicKey | Pda;
  /** The account owner or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfidentialBurnInstructionData = {
  discriminator: number;
  confidentialMintBurnDiscriminator: number;
  /** The new decryptable balance of the burner if the burn succeeds. */
  newDecryptableAvailableBalance: DecryptableBalance;
  /** The burn amount encrypted under the auditor ElGamal public key. */
  burnAmountAuditorCiphertextLo: EncryptedBalance;
  /** The burn amount encrypted under the auditor ElGamal public key. */
  burnAmountAuditorCiphertextHi: EncryptedBalance;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `ConfidentialBurn` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `ConfidentialBurn` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the proof.
   */
  ciphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedRangeProofU128` instruction to the
   * `ConfidentialBurn` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export type ConfidentialBurnInstructionDataArgs = {
  /** The new decryptable balance of the burner if the burn succeeds. */
  newDecryptableAvailableBalance: DecryptableBalanceArgs;
  /** The burn amount encrypted under the auditor ElGamal public key. */
  burnAmountAuditorCiphertextLo: EncryptedBalanceArgs;
  /** The burn amount encrypted under the auditor ElGamal public key. */
  burnAmountAuditorCiphertextHi: EncryptedBalanceArgs;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `ConfidentialBurn` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
   * instruction to the `ConfidentialBurn` instruction in the transaction.
   * If the offset is `0`, then use a context state account for the proof.
   */
  ciphertextValidityProofInstructionOffset: number;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyBatchedRangeProofU128` instruction to the
   * `ConfidentialBurn` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export function getConfidentialBurnInstructionDataSerializer(): Serializer<
  ConfidentialBurnInstructionDataArgs,
  ConfidentialBurnInstructionData
> {
  return mapSerializer<
    ConfidentialBurnInstructionDataArgs,
    any,
    ConfidentialBurnInstructionData
  >(
    struct<ConfidentialBurnInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialMintBurnDiscriminator', u8()],
        ['newDecryptableAvailableBalance', getDecryptableBalanceSerializer()],
        ['burnAmountAuditorCiphertextLo', getEncryptedBalanceSerializer()],
        ['burnAmountAuditorCiphertextHi', getEncryptedBalanceSerializer()],
        ['equalityProofInstructionOffset', i8()],
        ['ciphertextValidityProofInstructionOffset', i8()],
        ['rangeProofInstructionOffset', i8()],
      ],
      { description: 'ConfidentialBurnInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 42,
      confidentialMintBurnDiscriminator: 4,
    })
  ) as Serializer<
    ConfidentialBurnInstructionDataArgs,
    ConfidentialBurnInstructionData
  >;
}

// Args.
export type ConfidentialBurnInstructionArgs =
  ConfidentialBurnInstructionDataArgs;

// Instruction.
export function confidentialBurn(
  context: Pick<Context, 'programs'>,
  input: ConfidentialBurnInstructionAccounts & ConfidentialBurnInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mint: { index: 1, isWritable: true as boolean, value: input.mint ?? null },
    instructionsSysvar: {
      index: 2,
      isWritable: false as boolean,
      value: input.instructionsSysvar ?? null,
    },
    equalityRecord: {
      index: 3,
      isWritable: false as boolean,
      value: input.equalityRecord ?? null,
    },
    ciphertextValidityRecord: {
      index: 4,
      isWritable: false as boolean,
      value: input.ciphertextValidityRecord ?? null,
    },
    rangeRecord: {
      index: 5,
      isWritable: false as boolean,
      value: input.rangeRecord ?? null,
    },
    authority: {
      index: 6,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfidentialBurnInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'omitted',
    programId
  );

  // Data.
  const data = getConfidentialBurnInstructionDataSerializer().serialize(
    resolvedArgs as ConfidentialBurnInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
