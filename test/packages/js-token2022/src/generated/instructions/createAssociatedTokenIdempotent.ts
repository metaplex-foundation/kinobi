/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findAssociatedTokenPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateAssociatedTokenIdempotentInstructionAccounts = {
  /** Funding account (must be a system account). */
  payer?: Signer;
  /** Associated token account address to be created. */
  ata?: PublicKey | Pda;
  /** Wallet address for the new associated token account. */
  owner: PublicKey | Pda;
  /** The token mint for the new associated token account. */
  mint: PublicKey | Pda;
  /** System program. */
  systemProgram?: PublicKey | Pda;
  /** SPL Token program. */
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type CreateAssociatedTokenIdempotentInstructionData = {
  discriminator: number;
};

export type CreateAssociatedTokenIdempotentInstructionDataArgs = {};

export function getCreateAssociatedTokenIdempotentInstructionDataSerializer(): Serializer<
  CreateAssociatedTokenIdempotentInstructionDataArgs,
  CreateAssociatedTokenIdempotentInstructionData
> {
  return mapSerializer<
    CreateAssociatedTokenIdempotentInstructionDataArgs,
    any,
    CreateAssociatedTokenIdempotentInstructionData
  >(
    struct<CreateAssociatedTokenIdempotentInstructionData>(
      [['discriminator', u8()]],
      { description: 'CreateAssociatedTokenIdempotentInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<
    CreateAssociatedTokenIdempotentInstructionDataArgs,
    CreateAssociatedTokenIdempotentInstructionData
  >;
}

// Instruction.
export function createAssociatedTokenIdempotent(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: CreateAssociatedTokenIdempotentInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'associatedToken',
    'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    ata: { index: 1, isWritable: true as boolean, value: input.ata ?? null },
    owner: {
      index: 2,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
    mint: { index: 3, isWritable: false as boolean, value: input.mint ?? null },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = publicKey(
      'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
    );
  }
  if (!resolvedAccounts.ata.value) {
    resolvedAccounts.ata.value = findAssociatedTokenPda(context, {
      owner: expectPublicKey(resolvedAccounts.owner.value),
      tokenProgram: expectPublicKey(resolvedAccounts.tokenProgram.value),
      mint: expectPublicKey(resolvedAccounts.mint.value),
    });
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = publicKey(
      '11111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getCreateAssociatedTokenIdempotentInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
