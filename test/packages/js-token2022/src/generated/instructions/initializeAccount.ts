/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeAccountInstructionAccounts = {
  /** The account to initialize. */
  account: PublicKey | Pda;
  /** The mint this account will be associated with. */
  mint: PublicKey | Pda;
  /** The new account's owner/multisignature. */
  owner: PublicKey | Pda;
  /** Rent sysvar. */
  rent?: PublicKey | Pda;
};

// Data.
export type InitializeAccountInstructionData = { discriminator: number };

export type InitializeAccountInstructionDataArgs = {};

export function getInitializeAccountInstructionDataSerializer(): Serializer<
  InitializeAccountInstructionDataArgs,
  InitializeAccountInstructionData
> {
  return mapSerializer<
    InitializeAccountInstructionDataArgs,
    any,
    InitializeAccountInstructionData
  >(
    struct<InitializeAccountInstructionData>([['discriminator', u8()]], {
      description: 'InitializeAccountInstructionData',
    }),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<
    InitializeAccountInstructionDataArgs,
    InitializeAccountInstructionData
  >;
}

// Instruction.
export function initializeAccount(
  context: Pick<Context, 'programs'>,
  input: InitializeAccountInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    account: {
      index: 0,
      isWritable: true as boolean,
      value: input.account ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    owner: {
      index: 2,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
    rent: { index: 3, isWritable: false as boolean, value: input.rent ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.rent.value) {
    resolvedAccounts.rent.value = publicKey(
      'SysvarRent111111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeAccountInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
