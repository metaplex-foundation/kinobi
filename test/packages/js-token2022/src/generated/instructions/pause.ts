/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type PauseInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
  /** The pausable authority that can pause the mint. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type PauseInstructionData = {
  discriminator: number;
  pausableDiscriminator: number;
};

export type PauseInstructionDataArgs = {};

export function getPauseInstructionDataSerializer(): Serializer<
  PauseInstructionDataArgs,
  PauseInstructionData
> {
  return mapSerializer<PauseInstructionDataArgs, any, PauseInstructionData>(
    struct<PauseInstructionData>(
      [
        ['discriminator', u8()],
        ['pausableDiscriminator', u8()],
      ],
      { description: 'PauseInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 44, pausableDiscriminator: 1 })
  ) as Serializer<PauseInstructionDataArgs, PauseInstructionData>;
}

// Instruction.
export function pause(
  context: Pick<Context, 'programs'>,
  input: PauseInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getPauseInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
