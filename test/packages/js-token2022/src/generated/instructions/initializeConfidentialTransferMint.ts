/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializeConfidentialTransferMintInstructionAccounts = {
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeConfidentialTransferMintInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /**
   * Authority to modify the `ConfidentialTransferMint` configuration and to
   * approve new accounts.
   */
  authority: Option<PublicKey>;
  /**
   * Determines if newly configured accounts must be approved by the
   * `authority` before they may be used by the user.
   */
  autoApproveNewAccounts: boolean;
  /** New authority to decode any transfer amount in a confidential transfer. */
  auditorElgamalPubkey: Option<PublicKey>;
};

export type InitializeConfidentialTransferMintInstructionDataArgs = {
  /**
   * Authority to modify the `ConfidentialTransferMint` configuration and to
   * approve new accounts.
   */
  authority: OptionOrNullable<PublicKey>;
  /**
   * Determines if newly configured accounts must be approved by the
   * `authority` before they may be used by the user.
   */
  autoApproveNewAccounts: boolean;
  /** New authority to decode any transfer amount in a confidential transfer. */
  auditorElgamalPubkey: OptionOrNullable<PublicKey>;
};

export function getInitializeConfidentialTransferMintInstructionDataSerializer(): Serializer<
  InitializeConfidentialTransferMintInstructionDataArgs,
  InitializeConfidentialTransferMintInstructionData
> {
  return mapSerializer<
    InitializeConfidentialTransferMintInstructionDataArgs,
    any,
    InitializeConfidentialTransferMintInstructionData
  >(
    struct<InitializeConfidentialTransferMintInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
        ['autoApproveNewAccounts', bool()],
        ['auditorElgamalPubkey', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializeConfidentialTransferMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 0,
    })
  ) as Serializer<
    InitializeConfidentialTransferMintInstructionDataArgs,
    InitializeConfidentialTransferMintInstructionData
  >;
}

// Args.
export type InitializeConfidentialTransferMintInstructionArgs =
  InitializeConfidentialTransferMintInstructionDataArgs;

// Instruction.
export function initializeConfidentialTransferMint(
  context: Pick<Context, 'programs'>,
  input: InitializeConfidentialTransferMintInstructionAccounts &
    InitializeConfidentialTransferMintInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeConfidentialTransferMintInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeConfidentialTransferMintInstructionDataSerializer().serialize(
      resolvedArgs as InitializeConfidentialTransferMintInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
