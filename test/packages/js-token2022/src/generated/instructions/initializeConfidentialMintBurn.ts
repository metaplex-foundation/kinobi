/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  getDecryptableBalanceSerializer,
} from '../types';

// Accounts.
export type InitializeConfidentialMintBurnInstructionAccounts = {
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializeConfidentialMintBurnInstructionData = {
  discriminator: number;
  confidentialMintBurnDiscriminator: number;
  /** The ElGamal pubkey used to encrypt the confidential supply. */
  supplyElgamalPubkey: PublicKey;
  /** The initial 0 supply encrypted with the supply aes key. */
  decryptableSupply: DecryptableBalance;
};

export type InitializeConfidentialMintBurnInstructionDataArgs = {
  /** The ElGamal pubkey used to encrypt the confidential supply. */
  supplyElgamalPubkey: PublicKey;
  /** The initial 0 supply encrypted with the supply aes key. */
  decryptableSupply: DecryptableBalanceArgs;
};

export function getInitializeConfidentialMintBurnInstructionDataSerializer(): Serializer<
  InitializeConfidentialMintBurnInstructionDataArgs,
  InitializeConfidentialMintBurnInstructionData
> {
  return mapSerializer<
    InitializeConfidentialMintBurnInstructionDataArgs,
    any,
    InitializeConfidentialMintBurnInstructionData
  >(
    struct<InitializeConfidentialMintBurnInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialMintBurnDiscriminator', u8()],
        ['supplyElgamalPubkey', publicKeySerializer()],
        ['decryptableSupply', getDecryptableBalanceSerializer()],
      ],
      { description: 'InitializeConfidentialMintBurnInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 42,
      confidentialMintBurnDiscriminator: 0,
    })
  ) as Serializer<
    InitializeConfidentialMintBurnInstructionDataArgs,
    InitializeConfidentialMintBurnInstructionData
  >;
}

// Args.
export type InitializeConfidentialMintBurnInstructionArgs =
  InitializeConfidentialMintBurnInstructionDataArgs;

// Instruction.
export function initializeConfidentialMintBurn(
  context: Pick<Context, 'programs'>,
  input: InitializeConfidentialMintBurnInstructionAccounts &
    InitializeConfidentialMintBurnInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeConfidentialMintBurnInstructionArgs = {
    ...input,
  };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeConfidentialMintBurnInstructionDataSerializer().serialize(
      resolvedArgs as InitializeConfidentialMintBurnInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
