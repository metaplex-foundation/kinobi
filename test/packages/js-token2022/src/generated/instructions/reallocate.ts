/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  ExtensionType,
  ExtensionTypeArgs,
  getExtensionTypeSerializer,
} from '../types';

// Accounts.
export type ReallocateInstructionAccounts = {
  /** The token account to reallocate. */
  token: PublicKey | Pda;
  /** The payer account to fund reallocation. */
  payer: Signer;
  /** System program for reallocation funding. */
  systemProgram?: PublicKey | Pda;
  /** The account's owner or its multisignature account. */
  owner: PublicKey | Pda | Signer;
};

// Data.
export type ReallocateInstructionData = {
  discriminator: number;
  /** New extension types to include in the reallocated account. */
  newExtensionTypes: Array<ExtensionType>;
};

export type ReallocateInstructionDataArgs = {
  /** New extension types to include in the reallocated account. */
  newExtensionTypes: Array<ExtensionTypeArgs>;
};

export function getReallocateInstructionDataSerializer(): Serializer<
  ReallocateInstructionDataArgs,
  ReallocateInstructionData
> {
  return mapSerializer<
    ReallocateInstructionDataArgs,
    any,
    ReallocateInstructionData
  >(
    struct<ReallocateInstructionData>(
      [
        ['discriminator', u8()],
        [
          'newExtensionTypes',
          array(getExtensionTypeSerializer(), { size: 'remainder' }),
        ],
      ],
      { description: 'ReallocateInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 29 })
  ) as Serializer<ReallocateInstructionDataArgs, ReallocateInstructionData>;
}

// Args.
export type ReallocateInstructionArgs = ReallocateInstructionDataArgs;

// Instruction.
export function reallocate(
  context: Pick<Context, 'programs'>,
  input: ReallocateInstructionAccounts & ReallocateInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    owner: {
      index: 3,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ReallocateInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = publicKey(
      '11111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getReallocateInstructionDataSerializer().serialize(
    resolvedArgs as ReallocateInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
