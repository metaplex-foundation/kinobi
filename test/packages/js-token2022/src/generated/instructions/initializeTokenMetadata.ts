/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  string,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeTokenMetadataInstructionAccounts = {
  metadata: PublicKey | Pda;
  updateAuthority: PublicKey | Pda;
  mint: PublicKey | Pda;
  mintAuthority: Signer;
};

// Data.
export type InitializeTokenMetadataInstructionData = {
  discriminator: Uint8Array;
  /** Longer name of the token. */
  name: string;
  /** Shortened symbol of the token. */
  symbol: string;
  /** URI pointing to more metadata (image, video, etc.). */
  uri: string;
};

export type InitializeTokenMetadataInstructionDataArgs = {
  /** Longer name of the token. */
  name: string;
  /** Shortened symbol of the token. */
  symbol: string;
  /** URI pointing to more metadata (image, video, etc.). */
  uri: string;
};

export function getInitializeTokenMetadataInstructionDataSerializer(): Serializer<
  InitializeTokenMetadataInstructionDataArgs,
  InitializeTokenMetadataInstructionData
> {
  return mapSerializer<
    InitializeTokenMetadataInstructionDataArgs,
    any,
    InitializeTokenMetadataInstructionData
  >(
    struct<InitializeTokenMetadataInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['name', string()],
        ['symbol', string()],
        ['uri', string()],
      ],
      { description: 'InitializeTokenMetadataInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([210, 225, 30, 162, 88, 184, 77, 141]),
    })
  ) as Serializer<
    InitializeTokenMetadataInstructionDataArgs,
    InitializeTokenMetadataInstructionData
  >;
}

// Args.
export type InitializeTokenMetadataInstructionArgs =
  InitializeTokenMetadataInstructionDataArgs;

// Instruction.
export function initializeTokenMetadata(
  context: Pick<Context, 'programs'>,
  input: InitializeTokenMetadataInstructionAccounts &
    InitializeTokenMetadataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    metadata: {
      index: 0,
      isWritable: true as boolean,
      value: input.metadata ?? null,
    },
    updateAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.updateAuthority ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    mintAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.mintAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeTokenMetadataInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeTokenMetadataInstructionDataSerializer().serialize(
    resolvedArgs as InitializeTokenMetadataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
