/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type MintToCheckedInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
  /** The account to mint tokens to. */
  token: PublicKey | Pda;
  /** The mint's minting authority or its multisignature account. */
  mintAuthority: PublicKey | Pda | Signer;
};

// Data.
export type MintToCheckedInstructionData = {
  discriminator: number;
  /** The amount of new tokens to mint. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export type MintToCheckedInstructionDataArgs = {
  /** The amount of new tokens to mint. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
};

export function getMintToCheckedInstructionDataSerializer(): Serializer<
  MintToCheckedInstructionDataArgs,
  MintToCheckedInstructionData
> {
  return mapSerializer<
    MintToCheckedInstructionDataArgs,
    any,
    MintToCheckedInstructionData
  >(
    struct<MintToCheckedInstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
      ],
      { description: 'MintToCheckedInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 14 })
  ) as Serializer<
    MintToCheckedInstructionDataArgs,
    MintToCheckedInstructionData
  >;
}

// Args.
export type MintToCheckedInstructionArgs = MintToCheckedInstructionDataArgs;

// Instruction.
export function mintToChecked(
  context: Pick<Context, 'programs'>,
  input: MintToCheckedInstructionAccounts & MintToCheckedInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    token: {
      index: 1,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mintAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.mintAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: MintToCheckedInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getMintToCheckedInstructionDataSerializer().serialize(
    resolvedArgs as MintToCheckedInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
