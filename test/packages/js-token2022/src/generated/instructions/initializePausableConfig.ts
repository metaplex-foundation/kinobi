/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type InitializePausableConfigInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
};

// Data.
export type InitializePausableConfigInstructionData = {
  discriminator: number;
  pausableDiscriminator: number;
  /** The authority that can pause and resume the mint. */
  authority: Option<PublicKey>;
};

export type InitializePausableConfigInstructionDataArgs = {
  /** The authority that can pause and resume the mint. */
  authority: OptionOrNullable<PublicKey>;
};

export function getInitializePausableConfigInstructionDataSerializer(): Serializer<
  InitializePausableConfigInstructionDataArgs,
  InitializePausableConfigInstructionData
> {
  return mapSerializer<
    InitializePausableConfigInstructionDataArgs,
    any,
    InitializePausableConfigInstructionData
  >(
    struct<InitializePausableConfigInstructionData>(
      [
        ['discriminator', u8()],
        ['pausableDiscriminator', u8()],
        ['authority', zeroableOption(publicKeySerializer())],
      ],
      { description: 'InitializePausableConfigInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 44, pausableDiscriminator: 0 })
  ) as Serializer<
    InitializePausableConfigInstructionDataArgs,
    InitializePausableConfigInstructionData
  >;
}

// Args.
export type InitializePausableConfigInstructionArgs =
  InitializePausableConfigInstructionDataArgs;

// Instruction.
export function initializePausableConfig(
  context: Pick<Context, 'programs'>,
  input: InitializePausableConfigInstructionAccounts &
    InitializePausableConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializePausableConfigInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializePausableConfigInstructionDataSerializer().serialize(
    resolvedArgs as InitializePausableConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
