/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i8,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecryptableBalance,
  DecryptableBalanceArgs,
  getDecryptableBalanceSerializer,
} from '../types';

// Accounts.
export type ConfidentialWithdrawInstructionAccounts = {
  /** The SPL Token account. */
  token: PublicKey | Pda;
  /** The corresponding SPL Token mint. */
  mint: PublicKey | Pda;
  /**
   * Instructions sysvar if at least one of the
   * `zk_elgamal_proof` instructions are included in the same
   * transaction.
   */

  instructionsSysvar?: PublicKey | Pda;
  /** (Optional) Equality proof record account or context state account. */
  equalityRecord?: PublicKey | Pda;
  /** (Optional) Range proof record account or context state account. */
  rangeRecord?: PublicKey | Pda;
  /** The source account's owner/delegate or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ConfidentialWithdrawInstructionData = {
  discriminator: number;
  confidentialTransferDiscriminator: number;
  /** The amount of tokens to withdraw. */
  amount: bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
  /** The new decryptable balance if the withdrawal succeeds. */
  newDecryptableAvailableBalance: DecryptableBalance;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `Withdraw` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU64`
   * instruction to the `Withdraw` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export type ConfidentialWithdrawInstructionDataArgs = {
  /** The amount of tokens to withdraw. */
  amount: number | bigint;
  /** Expected number of base 10 digits to the right of the decimal place. */
  decimals: number;
  /** The new decryptable balance if the withdrawal succeeds. */
  newDecryptableAvailableBalance: DecryptableBalanceArgs;
  /**
   * Relative location of the
   * `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
   * to the `Withdraw` instruction in the transaction. If the offset is
   * `0`, then use a context state account for the proof.
   */
  equalityProofInstructionOffset: number;
  /**
   * Relative location of the `ProofInstruction::BatchedRangeProofU64`
   * instruction to the `Withdraw` instruction in the transaction. If the
   * offset is `0`, then use a context state account for the proof.
   */
  rangeProofInstructionOffset: number;
};

export function getConfidentialWithdrawInstructionDataSerializer(): Serializer<
  ConfidentialWithdrawInstructionDataArgs,
  ConfidentialWithdrawInstructionData
> {
  return mapSerializer<
    ConfidentialWithdrawInstructionDataArgs,
    any,
    ConfidentialWithdrawInstructionData
  >(
    struct<ConfidentialWithdrawInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialTransferDiscriminator', u8()],
        ['amount', u64()],
        ['decimals', u8()],
        ['newDecryptableAvailableBalance', getDecryptableBalanceSerializer()],
        ['equalityProofInstructionOffset', i8()],
        ['rangeProofInstructionOffset', i8()],
      ],
      { description: 'ConfidentialWithdrawInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 27,
      confidentialTransferDiscriminator: 6,
    })
  ) as Serializer<
    ConfidentialWithdrawInstructionDataArgs,
    ConfidentialWithdrawInstructionData
  >;
}

// Args.
export type ConfidentialWithdrawInstructionArgs =
  ConfidentialWithdrawInstructionDataArgs;

// Instruction.
export function confidentialWithdraw(
  context: Pick<Context, 'programs'>,
  input: ConfidentialWithdrawInstructionAccounts &
    ConfidentialWithdrawInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    token: {
      index: 0,
      isWritable: true as boolean,
      value: input.token ?? null,
    },
    mint: { index: 1, isWritable: false as boolean, value: input.mint ?? null },
    instructionsSysvar: {
      index: 2,
      isWritable: false as boolean,
      value: input.instructionsSysvar ?? null,
    },
    equalityRecord: {
      index: 3,
      isWritable: false as boolean,
      value: input.equalityRecord ?? null,
    },
    rangeRecord: {
      index: 4,
      isWritable: false as boolean,
      value: input.rangeRecord ?? null,
    },
    authority: {
      index: 5,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ConfidentialWithdrawInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'omitted',
    programId
  );

  // Data.
  const data = getConfidentialWithdrawInstructionDataSerializer().serialize(
    resolvedArgs as ConfidentialWithdrawInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
