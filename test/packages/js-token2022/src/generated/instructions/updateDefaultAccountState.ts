/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AccountState,
  AccountStateArgs,
  getAccountStateSerializer,
} from '../types';

// Accounts.
export type UpdateDefaultAccountStateInstructionAccounts = {
  /** The mint. */
  mint: PublicKey | Pda;
  /** The mint freeze authority or its multisignature account. */
  freezeAuthority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateDefaultAccountStateInstructionData = {
  discriminator: number;
  defaultAccountStateDiscriminator: number;
  /** The state each new token account should start with. */
  state: AccountState;
};

export type UpdateDefaultAccountStateInstructionDataArgs = {
  /** The state each new token account should start with. */
  state: AccountStateArgs;
};

export function getUpdateDefaultAccountStateInstructionDataSerializer(): Serializer<
  UpdateDefaultAccountStateInstructionDataArgs,
  UpdateDefaultAccountStateInstructionData
> {
  return mapSerializer<
    UpdateDefaultAccountStateInstructionDataArgs,
    any,
    UpdateDefaultAccountStateInstructionData
  >(
    struct<UpdateDefaultAccountStateInstructionData>(
      [
        ['discriminator', u8()],
        ['defaultAccountStateDiscriminator', u8()],
        ['state', getAccountStateSerializer()],
      ],
      { description: 'UpdateDefaultAccountStateInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 28,
      defaultAccountStateDiscriminator: 1,
    })
  ) as Serializer<
    UpdateDefaultAccountStateInstructionDataArgs,
    UpdateDefaultAccountStateInstructionData
  >;
}

// Args.
export type UpdateDefaultAccountStateInstructionArgs =
  UpdateDefaultAccountStateInstructionDataArgs;

// Instruction.
export function updateDefaultAccountState(
  context: Pick<Context, 'programs'>,
  input: UpdateDefaultAccountStateInstructionAccounts &
    UpdateDefaultAccountStateInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    freezeAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.freezeAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateDefaultAccountStateInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getUpdateDefaultAccountStateInstructionDataSerializer().serialize(
      resolvedArgs as UpdateDefaultAccountStateInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
