/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ApplyConfidentialPendingBurnInstructionAccounts = {
  /** The SPL Token mint. */
  mint: PublicKey | Pda;
  /** The mint authority or its multisignature account. */
  authority: PublicKey | Pda | Signer;
};

// Data.
export type ApplyConfidentialPendingBurnInstructionData = {
  discriminator: number;
  confidentialMintBurnDiscriminator: number;
};

export type ApplyConfidentialPendingBurnInstructionDataArgs = {};

export function getApplyConfidentialPendingBurnInstructionDataSerializer(): Serializer<
  ApplyConfidentialPendingBurnInstructionDataArgs,
  ApplyConfidentialPendingBurnInstructionData
> {
  return mapSerializer<
    ApplyConfidentialPendingBurnInstructionDataArgs,
    any,
    ApplyConfidentialPendingBurnInstructionData
  >(
    struct<ApplyConfidentialPendingBurnInstructionData>(
      [
        ['discriminator', u8()],
        ['confidentialMintBurnDiscriminator', u8()],
      ],
      { description: 'ApplyConfidentialPendingBurnInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 42,
      confidentialMintBurnDiscriminator: 5,
    })
  ) as Serializer<
    ApplyConfidentialPendingBurnInstructionDataArgs,
    ApplyConfidentialPendingBurnInstructionData
  >;
}

// Instruction.
export function applyConfidentialPendingBurn(
  context: Pick<Context, 'programs'>,
  input: ApplyConfidentialPendingBurnInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getApplyConfidentialPendingBurnInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
