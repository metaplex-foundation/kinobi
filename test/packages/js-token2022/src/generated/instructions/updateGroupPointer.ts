/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
  zeroableOption,
} from '../shared';

// Accounts.
export type UpdateGroupPointerInstructionAccounts = {
  /** The mint to initialize. */
  mint: PublicKey | Pda;
  /** The group pointer authority or its multisignature account. */
  groupPointerAuthority: PublicKey | Pda | Signer;
};

// Data.
export type UpdateGroupPointerInstructionData = {
  discriminator: number;
  groupPointerDiscriminator: number;
  /** The new account address that holds the group configurations. */
  groupAddress: Option<PublicKey>;
};

export type UpdateGroupPointerInstructionDataArgs = {
  /** The new account address that holds the group configurations. */
  groupAddress: OptionOrNullable<PublicKey>;
};

export function getUpdateGroupPointerInstructionDataSerializer(): Serializer<
  UpdateGroupPointerInstructionDataArgs,
  UpdateGroupPointerInstructionData
> {
  return mapSerializer<
    UpdateGroupPointerInstructionDataArgs,
    any,
    UpdateGroupPointerInstructionData
  >(
    struct<UpdateGroupPointerInstructionData>(
      [
        ['discriminator', u8()],
        ['groupPointerDiscriminator', u8()],
        ['groupAddress', zeroableOption(publicKeySerializer())],
      ],
      { description: 'UpdateGroupPointerInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 40, groupPointerDiscriminator: 1 })
  ) as Serializer<
    UpdateGroupPointerInstructionDataArgs,
    UpdateGroupPointerInstructionData
  >;
}

// Args.
export type UpdateGroupPointerInstructionArgs =
  UpdateGroupPointerInstructionDataArgs;

// Instruction.
export function updateGroupPointer(
  context: Pick<Context, 'programs'>,
  input: UpdateGroupPointerInstructionAccounts &
    UpdateGroupPointerInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'token2022',
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'
  );

  // Accounts.
  const resolvedAccounts = {
    mint: { index: 0, isWritable: true as boolean, value: input.mint ?? null },
    groupPointerAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.groupPointerAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateGroupPointerInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGroupPointerInstructionDataSerializer().serialize(
    resolvedArgs as UpdateGroupPointerInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
