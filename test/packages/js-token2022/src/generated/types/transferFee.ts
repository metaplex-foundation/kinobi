/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Amount, mapAmountSerializer } from '@metaplex-foundation/umi';
import {
  Serializer,
  struct,
  u16,
  u64,
} from '@metaplex-foundation/umi/serializers';

export type TransferFee = {
  /** First epoch where the transfer fee takes effect. */
  epoch: bigint;
  /** Maximum fee assessed on transfers, expressed as an amount of tokens. */
  maximumFee: bigint;
  /**
   * Amount of transfer collected as fees, expressed as basis points of the
   * transfer amount, ie. increments of 0.01%.
   */
  transferFeeBasisPoints: Amount<'%', 2>;
};

export type TransferFeeArgs = {
  /** First epoch where the transfer fee takes effect. */
  epoch: number | bigint;
  /** Maximum fee assessed on transfers, expressed as an amount of tokens. */
  maximumFee: number | bigint;
  /**
   * Amount of transfer collected as fees, expressed as basis points of the
   * transfer amount, ie. increments of 0.01%.
   */
  transferFeeBasisPoints: Amount<'%', 2>;
};

export function getTransferFeeSerializer(): Serializer<
  TransferFeeArgs,
  TransferFee
> {
  return struct<TransferFee>(
    [
      ['epoch', u64()],
      ['maximumFee', u64()],
      ['transferFeeBasisPoints', mapAmountSerializer(u16(), '%', 2)],
    ],
    { description: 'TransferFee' }
  ) as Serializer<TransferFeeArgs, TransferFee>;
}
