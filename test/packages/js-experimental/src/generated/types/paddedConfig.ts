/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Address,
  getAddressDecoder,
  getAddressEncoder,
} from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';

export type PaddedConfig = {
  maxItems: number;
  padding: Array<number>;
  authority: Address;
};

export type PaddedConfigArgs = { maxItems: number; authority: Address };

export function getPaddedConfigEncoder(): Encoder<PaddedConfigArgs> {
  return mapEncoder(
    getStructEncoder([
      ['maxItems', getU32Encoder()],
      ['padding', getArrayEncoder(getU8Encoder(), { size: 4 })],
      ['authority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, padding: [0, 0, 0, 0] })
  );
}

export function getPaddedConfigDecoder(): Decoder<PaddedConfig> {
  return getStructDecoder([
    ['maxItems', getU32Decoder()],
    ['padding', getArrayDecoder(getU8Decoder(), { size: 4 })],
    ['authority', getAddressDecoder()],
  ]);
}

export function getPaddedConfigCodec(): Codec<PaddedConfigArgs, PaddedConfig> {
  return combineCodec(getPaddedConfigEncoder(), getPaddedConfigDecoder());
}
