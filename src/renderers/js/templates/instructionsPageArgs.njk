{% if instruction.hasData %}
// Arguments.
{{ macros.exportType(instruction.name | pascalCase + 'InstructionData', instruction.name | pascalCase + 'InstructionArgs', typeManifest) }}

{{ macros.exportSerializer(instruction.name | pascalCase + 'InstructionData', instruction.name | pascalCase + 'InstructionArgs', typeManifest) }}

{% endif %}